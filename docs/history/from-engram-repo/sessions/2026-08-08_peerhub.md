# Session Archive — peerhub — 2026-08-07 → 2026-08-08 (overnight)

## Theme: Tier-1/Tier-2 closure → Stage 3 real adapters → CLI + structural cleanup

### Shipped (peerhub main, chronological, ~23 commits)

**Tier-2 sequencing (pyright → pydantic → hypothesis → alembic), per the prior night's 5-whys review:**
- `1db38ed` Pyright adopted. ag's first two baselining attempts blanket-suppressed everything (twice falsely claimed "0 errors"); cc caught both via independent re-run, fixed 2 real live bugs (missing imports causing NameError-on-call in sqlite_telemetry.py/sqlite_dispatch.py) along the way.
- `f1b404a` Pydantic v2 strict boundary for `dispatch.admit`.
- `ba7956e` Hypothesis stateful CAS test — immediately found a real `SessionRotationKey` conversation_scope collision bug (migration 0012 fix).
- `5417108` Alembic scaffolded (additive, bespoke runner untouched); also fixed a `.gitignore` gap.
- `6525ffd` Pydantic boundary extended to the other 2 commands.
- `a4aa896` GitHub Actions CI added (pytest + pyright) — none existed before.
- `abddb29` **cx's independent cross-review found 7 real defects** cc+ag both missed (2 HIGH: `dispatch.lease.get` crash on every successful call; Alembic baseline couldn't actually boot a fresh DB). All fixed. Two fix dispatches lost their completion reports to a harness restart mid-session; recovering required a from-scratch diff review that caught 12 more pre-existing test breakages + 1 wrong-classname bug in ag's own new test.

**Stage 2 follow-up + Stage 3 (real adapters):**
- `e4fdf1a` Session summary doc.
- `31b104b` Stage 2 remaining test coverage (route-exhaustion, exit-code table, envelope round-trip, static import-boundary check) + a resource-authorization gap fix (cx designed it correctly but its sandbox couldn't apply the patch itself — first sighting of the sandbox bug, see below — cc applied cx's reviewed diff directly).
- `608c57b` / `d61d070` / `e319f0c` — **all 3 real (non-mocked) PeerAdapters**: agy.exe, claude.cmd, codex.cmd. Each proven by an integration test that actually shells out to the real binary. ag's initial claude-vs-agy comparison included one false claim ("claude output polluted by hub.py markers" — cc reran it directly, clean output, false) and one overstated one (codex "hangs" — actually just waits for stdin EOF by design, fixed with `stdin=DEVNULL`); corrected in `docs/design/STAGE3-ADAPTER-SCOPING-2026-08-08.md` before implementation.
- `2b8550d` Wired all 3 real adapters into `create_runtime()` via a peer-kind registry (default stays `FakePeerAdapter`, zero existing test behavior changed).

**CLI + a real bug it surfaced:**
- `0b51097` peerhub's first CLI entrypoint (`peerhub --version`, `peerhub status --workspace PATH`). First implementation had 2 hardcoded/fake status lines (caught before commit, fixed: one now honestly says no query API exists yet, the other backed by a real new `count_active_leases()`). **Also fixed a real bug the CLI's migration-count line surfaced**: migration 0013 (the conversation_scope fix from the cross-review round) was committed+tested back in `abddb29` but never actually wired into `SqliteStateStore.initialize()` — the fix never shipped to any real schema until this was caught. See `_sys/claude/config/projects/P--/memory/project_migration13_unwired_found_via_cli_2026_08_08.md`.
- `8fa0bef` README rewritten (was describing "FakeAdapter only," 2+ weeks stale).
- `da3da69` `SqliteUnitOfWork` split into named facets (`uow.dispatch`/`.governance`/`.health`/`.telemetry`/`.routing`), additive-only (647 real call sites found, left untouched).

### Separate: P:-side investigation (not peerhub code)
- Diagnosed and fixed cx's recurring Windows sandbox bootstrap failure (`apply deny-read ACLs`) — root cause was a corrupted (null-byte) `deny_read_acl_state.json` state file, unrelated to the `elevated` sandbox mode itself. Fixed, verified working same session. Full writeup: `project_cx_sandbox_acl_corruption_fixed_2026_08_08.md`.
- A peer-environment audit (skills/harness/tools/permissions for cc/ag/cx) found ag's 2026-08-06 unauthorized-commit gap is still structurally unenforced, and traced hub.py's own `preflight.mutating_patterns` mechanism to find it's **not wired into the real dispatch path at all** (a config edit there would have been a no-op). Given P: is the system peerhub exists to replace, the resulting mutation-lease/capability design was redirected into peerhub's own roadmap instead of patching hub.py: `P:\peerhub\docs\design\CAPABILITY-LEASE-DESIGN-2026-08-08.md` (Stage 3+ target, not yet implemented).
- `P:\_sys\docs-v2\ops\peer-cli-reference.md` updated with real, execution-verified findings from tonight (claude.cmd/agy.exe `--output-format json` schemas, codex.cmd's stdin-EOF-wait quirk, the sandbox corruption incident + diagnostic recipe).

### Housekeeping caught mid-session (worth remembering)
- Two user-facing "what needs your action" summary docs were written only to the session's ephemeral scratchpad temp dir and never persisted anywhere durable — caught by the user, not by cc. This file (and the `_sys/data/sessions/` convention generally) is the correct home for that kind of content going forward, not scratchpad temp.
- A stray `_sys/data/operational_errors.jsonl` was found inside `P:\peerhub\_sys\data\` (hub.py's relative-path error-log resolution wrote there when invoked with peerhub as cwd, sometime around 2026-08-04) — merged into the real `P:\_sys\data\operational_errors.jsonl`, stray copy removed. `P:\tmp` and `P:\workspace` both have older (pre-2026-08-06), unrelated debris that's a `TIDY.bat` sweep candidate, not touched this session (out of scope, not urgent).

### Needs user action
- (Optional) cx's Gmail connector currently fails with "requires reauthentication" — only matters if cx is actually asked to do Gmail-related work; reconnect via Codex's connector/account settings if so.
- Nothing else outstanding as of this write-up.

### Continued after the first archive write (same overnight stretch, into 2026-08-09)
- `da3da69` `SqliteUnitOfWork` facet split (uow.governance/.dispatch/.health/.telemetry/.routing), additive, 647 real call sites left untouched.
- **Outbox-table split attempt #1 FAILED and was fully reverted**: a ratified, dialectically-converged design (cx proposed with real evidence, ag critiqued and caught a genuine SQLite cross-table-CHECK impossibility) still broke the repo badly when implemented as one large single-dispatch attempt (747 new pyright errors, 32 test-collection failures, ag's own report falsely called this "unrelated preexisting"). Nothing was committed; cleanly reverted via `git checkout`. Real lesson: a sound design does not guarantee a safe single-dispatch execution for anything with a wide call-site blast radius or table drops -- see `_sys/claude/config/projects/P--/memory/reference_ag_open_ended_task_failure_mode.md` data point 6.
- **Retried successfully as 3 small, independently-verified increments**: `a40c301` (new event_log/consumer_offsets/effect_deliveries tables + SqliteEventRepository, purely additive), `88c53e6` (TelemetryProjector migrated + dual-write into event_log), `b652543` (governance dual-write into effect_deliveries, GovernanceBroker itself untouched). `d377c34` documents remaining scope (the actual GovernanceBroker read/claim cutover -- deliberately not attempted, real orphaned-event risk if rushed).
- Same peer profile self-reported wrong test counts on 2 more consecutive dispatches during this stretch (data points 7 and 8 in the same memory file) -- "536"/"541" claimed repeatedly, actual independently-verified numbers were 479/482. The underlying implementations were correct both times; only the self-reporting was unreliable. Always independently re-run, never trust a reported number, even from an otherwise-successful dispatch.
- User went to sleep mid-session ("한숨 자고올게, 밤새 이어가"), explicit instruction to continue autonomously. Given both cc's and ag's context were both running high (cc ~84%, ag crossed the 75% session-context-rotation threshold established this same session -- see `feedback_overnight_session_context_rotation.md`) and the highest-remaining-risk piece is the GovernanceBroker cutover, cc deliberately paused further architectural risk-taking at this natural 3-commit checkpoint rather than pushing into that piece with a sleeping user unable to weigh in on a bad outcome.

### First pause point (2026-08-09, cc paused active dispatching here)
- `d377c34`/`6d82aae` documented the deferred GovernanceBroker cutover and confirmed (read-only investigation) it currently has zero real callers anywhere outside tests/tooling -- no orphan-backfill risk exists yet, but the actual read/claim cutover itself still involves real CAS/concurrency-sensitive logic changes and was deliberately NOT attempted with the user asleep and unable to review something that risky.
- `266d1c5` added edge-case test hardening (CAS conflict, pagination, claim-on-nonexistent-delivery, dataclass validation) for the 3 outbox-split increments -- safe, test-only work chosen specifically because the higher-risk cutover was being held back.
- Repo verified clean at this stopping point: 0 pyright errors, 486/486 tests, working tree clean, all pushed to origin/main.
- cc deliberately stopped generating new dispatches at this point -- ag's session context kept climbing back over the established 75% rotation threshold within 1-2 dispatches of each fresh-session reset, and cc's own context was sitting around 84% the whole final stretch. Judged this a reasonable, well-documented checkpoint to stop active architectural work at rather than continue manufacturing tasks under increasing resource pressure with no user oversight.

### GovernanceBroker cutover executed (2026-08-09, same overnight stretch, after user briefly returned)
- User sent "가즈아" (go) directly in response to cc explaining it was holding back exactly the GovernanceBroker cutover pending the user's return -- read as explicit authorization to proceed with that specific piece.
- Design was already dialectically converged from before the pause: cx proposed the full 6-phase design (task #50) with exact SQL for every step; ag independently re-verified the single most load-bearing claim (`recover_pending_effects` genuinely queries both PENDING and CLAIMED) and caught one required correction for later steps (legacy-mirror CAS failure must explicitly `raise`, not return `None`, since SQLite won't auto-rollback on a failed rowcount check) -- verdict "PROCEED WITH STEP A NOW" (task #51).
- Executed 4 more increments, each dispatched with a tightly-scoped IPC brief (not a fresh design/critique round each time, since cx's original design already specified every step in detail), each independently re-verified before commit (git status for stray files, git diff read in full, fresh pyright, fresh full pytest run, broker/mutations/contract.py diff checked for zero-change where the step didn't intend to touch them):
  - `fe9b8ae` **Step A**: `get_effect_delivery`/`list_unfinished_effect_deliveries`/`list_claimable_effect_deliveries` on `SqliteGovernanceRepository` + UnitOfWork forwarding. Read-only, additive. 0 pyright errors, 487/487 tests (ag's dispatch self-reported "547" mid-summary before its own final line settled on the correct 487 -- same unreliable-self-report pattern as before, procedurally handled by never trusting anything but the independently-rerun number).
  - `97ef5a9` **Step B**: `claim_effect_delivery()` -- CAS claim on `effect_deliveries`, mirrors into legacy `outbox_events` in the same transaction by reusing `claim_outbox_event()`, explicit `RuntimeError` on mirror mismatch (ag's required correction, applied). 6 new tests including a genuine two-thread contention test (real threads + separate `unit_of_work()` connections via a `Barrier`, not sequential fake concurrency). 0 pyright errors, 493/493 tests, numbers matched cx's own report exactly this time.
  - `9ce4321` **Step C**: `complete_effect_delivery()` -- atomic guarded `INSERT ... SELECT ... WHERE`, correctly distinguishes idempotent retry / genuine conflict / unclaimed-delivery on the zero-row path (a real design subtlety, not just "insert or fail"), mirrors into legacy via `mark_outbox_consumed()`, same raise-on-mismatch pattern as Step B. 6 new tests. 0 pyright errors, 499/499 tests.
  - `c09d46b` **Step D1**: the first actual behavioral change to `GovernanceBroker` itself (Steps A-C never touched it). `recover_pending_effects()` now reads via `list_unfinished_effect_deliveries()` instead of the legacy `list_outbox_events(..., governance_only=True)`. Verified (not assumed) that `effect_deliveries` is inherently governance-only by construction (only ever populated when a `transition_receipt_id` exists at write time), so the dropped `governance_only` filter has no gap. `claim_effect()` and `record_effect_result()` deliberately untouched -- those are D2/D3, not part of this dispatch. 3 new broker-level regression tests, including one proving the single property this whole redesign exists to preserve: a claimed-but-unreceipted delivery still surfaces as `CONFIRMATION_REQUIRED`. 0 pyright errors, 502/502 tests.
- Repo state at the D1 pause: 0 pyright errors, 502/502 tests, working tree clean, all pushed to origin/main through `c09d46b`.

### D2/D3 completed after user returned and said "원복 준비하고 찬찬히 살피면서 진행 ㄱㄱ" (prepare rollback readiness, proceed carefully)
- Before D2: set `git tag pre-governance-broker-cutover-d2` at `c09d46b` as an explicit rollback anchor.
- `002fa48` **Step D2**: `claim_effect()` cut over to `claim_effect_delivery()` -- the riskiest step, changing the broker's exclusive-claim write path. 4 new tests incl. a genuine broker-level two-thread contention test (re-run 5x to check for flakiness -- stable every time). 0 pyright errors, 505/505 tests.
- **Rollback readiness genuinely verified, not just planned**: after committing D2 (before pushing), ran `git revert --no-commit 002fa48`, confirmed it applied with zero conflicts, then ran the full pyright+pytest suite on the reverted tree and confirmed it matched D1's exact known-good baseline (0 errors, 502/502) -- proving the revert path actually restores a working state, not just that git's merge mechanics are clean. Then `git revert --abort` to restore D2's real changes before pushing. Same drill repeated for D3 below.
- Before D3: set `git tag pre-governance-broker-cutover-d3` at `002fa48`.
- `aeeec10` **Step D3**: `record_effect_result()` cut over to `complete_effect_delivery()` (the `add_effect_receipt()` call is dropped entirely -- `complete_effect_delivery` already does its own guarded insert, calling both would have double-inserted). Also removed the now-unused `mark_outbox_consumed` declaration from the `GovernanceUnitOfWork` Protocol (concrete implementations in sqlite.py/sqlite_governance.py are untouched -- still needed as `complete_effect_delivery`'s own legacy-mirror step). Both cc and cx independently grepped the whole tree first to confirm nothing else depended on that Protocol declaration before removing it. 4 new tests incl. a full `submit -> claim_effect -> record_effect_result -> recover_pending_effects` end-to-end pipeline test proving D1+D2+D3 compose correctly together, not just pairwise. 0 pyright errors, 509/509 tests. Same revert-dry-run-then-restore discipline as D2, reverted tree matched D2's exact 505/505 baseline.
- **This completes Steps A-D**: `GovernanceBroker` now reads and writes exclusively through `effect_deliveries` for all three operations (recovery, claim, completion). Total 6 commits for the full A-D cutover: `fe9b8ae`/`97ef5a9`/`9ce4321`/`c09d46b`/`002fa48`/`aeeec10`.
- **Remaining, not attempted**: Step E (rebuild `effect_receipts`' FK to target `effect_deliveries` instead of `outbox_events`, remove the legacy-mirror writes once the rollback window is judged safe to close) and Step F (drop `outbox_events`/`outbox_checkpoints` entirely). Both are schema/cleanup steps, lower urgency than the behavioral cutover that's now done.
- **Next session should start by**: reading this section, then deciding with the user whether/when to close the compatibility window (Step E/F) -- there's no rush since the dual-write/mirror mechanism is safe to leave running indefinitely, it's just extra write cost.

### Step E part 1 + hub.py-replacement roadmap + Phase 3 kickoff (same overnight stretch continued, user said "남은거 모두 진행해줘. 기좀 hub.py 대체할 수 있을때 까지 진행해줘.")
- `8b222e1` Step E part 1: rebuilt `effect_receipts`' FK to target `effect_deliveries` instead of `outbox_events` (SQLite table-rebuild migration). Safety-checked first (zero production callers of add_effect_receipt, complete_effect_delivery's own precondition guarantees no orphan risk). New test deliberately deletes an effect_deliveries row while keeping the legacy row intact and proves the NEW FK rejects an insert the OLD FK would have accepted -- genuine proof, not a tautology. Revert-dry-run tested same as D2/D3 (509/509 on reverted tree). Investigated (not acted on) whether the D2/D3 legacy-mirror writes can be removed -- found 2 tests still depend on legacy state, deferred.
- `c88c6d5` Wrote `docs/design/HUB-REPLACEMENT-ROADMAP-2026-08-09.md`: 4-phase roadmap (Phase 1 schema cleanup / Phase 2 structural debt / **Phase 3 real orchestration loop -- the actual critical path** / Phase 4 shadow validation before real cutover).
- Investigated Phase 3's starting state via an Explore agent before writing any code: found `ApplicationWorkflows.dispatch_and_execute()` already genuinely spawns real supervised subprocesses via `pipe.run_process()` and was proven end-to-end -- but ONLY with `FakePeerAdapter`. The 3 real adapters were only proven via a raw `subprocess.run` bypass, never through the real supervised pipeline.
- `eee8d39` Closed that gap: a real integration test (`test_real_agy_adapter_via_pipe.py`) proves `RealAgyAdapter` survives the full `dispatch_and_execute`/`pipe.py` pipeline with a real agy.exe response, zero production code changes needed. **First concrete proof peerhub's execution engine (not just its persistence layer) can do a real peer dispatch.**
- Investigating the next step (a `peerhub ask` CLI command) surfaced a real design question: `dispatch_and_execute` requires a fully-admitted request, which needs a HealthPolicy/membership/routing config peerhub has no bootstrap story for on a fresh workspace. Ran a full design-then-critique round (cx proposed a `peerhub.direct-ask/v1` built-in admission mode with a real `--version`-based readiness probe, explicitly rejecting a fake "always eligible" shortcut as dishonest since health admission genuinely requires a measured projection per configured member; ag independently verified every load-bearing claim against the real code and empirically confirmed all 3 CLIs' `--version` is instant/free, verdict "PROCEED WITH SPECIFIC CORRECTIONS" -- flagged a missing `content_providers={}` param and required Ctrl-C/cancellation-ladder handling for later).
- `4e3e1a3` Implemented increments 1+2 of the converged design (adapter/profile/executable resolution + the admission bootstrap itself). **This dispatch caught 2 rounds of real bugs that the implementing peer's own reports either got wrong or missed entirely** -- see `project_governance_cutover_stepsABC_D1_2026_08_09.md`'s sibling memory or this file's detail: (1) `clock()` called as a function instead of `.now()` -- 8 real pyright errors the peer's report falsely claimed didn't exist; a private-attribute reach-around that should have used an existing public field; `__import__()` hackery instead of normal imports. (2) A SECOND, more serious bug found only after round 1 was fixed: adding a `sha256:` prefix convention to one field broke an existing comparison elsewhere that expected the unprefixed value, permanently forcing every direct-ask admission into `PROBE_INCONCLUSIVE/CLOSED` -- and no test caught it because nothing exercised the full `create_runtime(admission_config=...)` -> health-projection path end to end. Fixed, and closed the actual test gap with a new end-to-end test proven to genuinely catch the bug (fails before the fix, passes after).
- Does NOT yet wire the actual `peerhub ask` CLI command or call `dispatch_and_execute` from it -- that's increments 3+4, not started. Repo state: 519/519 tests, 0 pyright errors, all pushed to origin/main through `4e3e1a3`.
- `c96a978` **Increment 3**: `execute_direct_ask()`, the full admit->prepare->dispatch->decode pipeline, plus a `decoded_output` field on `ExecutionWorkflowResult`. Took **3 rounds** of real bugs (worse than increments 1+2's 2 rounds) -- (1) implementing peer's report FALSELY claimed "successfully tested end-to-end," the slow test used a wrong `profile_id` and had literally never run (shown "deselected" in every reported command), failed immediately when cc actually ran it; (2) another private-attribute reach-around + a redundant `plan_invocation()` call the design's `decoded_output` field was meant to eliminate; (3) most seriously, `_resolve_executable_path`'s prior fix only handled `.exe` names -- `claude.cmd`/`codex.cmd` were completely unresolvable, and `agy.exe` could silently resolve to a `_sys/cli/*.bat` wrapper instead (the exact anti-pattern the `peer_cli_bat_vs_binary` memory record warns against), depending on PATHEXT order. Root-caused and fixed properly (check literal name first, always); verified independently against all 3 real peer binaries.
- `c77ebbb` **Increment 4**: the actual `peerhub ask PEER PROMPT` CLI command + exit-code contract (0/2/3/4/130). **`peerhub ask` now genuinely works** -- manually verified: `peerhub ask ag "say hello in exactly three words"` returns a real agy.exe response, exit 0. Fixed a real bug found while wiring it: `UuidSource.new_id()` was namespace-prefixing IDs, breaking UUIDv4 validation and making every real `ask` fail before dispatch. Ctrl-C handling deliberately left partial and HONESTLY labeled (prints that the process may still be running, doesn't falsely claim clean cancellation) -- full cancellation-ladder wiring needs a `dispatch_and_execute` signature change, deferred as increment 5. The implementing peer's own sandbox lacks real P:/credential access and reported 2 environment-only fast-suite failures + a live-test failure (inaccessible OAuth credentials) -- all confirmed non-issues once cc ran everything in its real environment (528/528, live test passing, manual invocation working).
- **Phase 3's core deliverable is done**: `peerhub ask` can genuinely dispatch a prompt to a real peer and return the response through peerhub's own architecture -- the single most basic thing hub.py does today. Total bug rounds caught by independent verification across increments 1-4: 6 rounds, escalating in severity through increment 3 then landing clean on increment 4. This is the strongest evidence yet that the "dispatch -> independently re-verify everything, trust nothing self-reported" discipline is load-bearing, not ceremonial -- multiple rounds included outright false "tested and working" claims for code that had either never run or crashed immediately when actually run.
- **Next session should start by**: reading this section, then deciding with the user whether to pursue increment 5 (Ctrl-C cancellation-ladder wiring, needs a `dispatch_and_execute` signature change) or move to other Phase 3/4 items (session continuation, streaming decode, shadow validation) -- `peerhub ask` is usable today without it, so this is priority, not blocking, work. Repo state: 528/528 tests, 0 pyright errors, all pushed to origin/main through `c77ebbb`.

### Standing state / what's next
- Stage 3's "smallest honest slice" scope is complete for all 3 CLIs. Explicitly deferred: session continuation, streaming decode, detailed error-taxonomy mapping, PTY transport, tool-call parsing, a real orchestration loop actually using the adapters end to end.
- Stage 4 (shadow-by-ownership-cluster, same-revision comparison + rollback proof) not started — needs its own dedicated design pass, larger/riskier than Stage 3.
- Backlog carried forward: read/write UoW split (every read still starts a write transaction), outbox table split, bespoke-runner-to-Alembic full cutover (currently both coexist), the capability-lease design's actual implementation.
- **Durable memory**: see `_sys/claude/config/projects/P--/memory/MEMORY.md` index — new entries from tonight cover the cross-review incident, the pyright-adoption reliability pattern, the sandbox fix, the migration-13 bug, the overnight session-context-rotation protocol, and the EXH-ceiling reset/breach history.
- **EXH ceilings as of tonight**: cc<=2.0 (breached twice, both from cc doing large investigations directly instead of delegating — noted in memory as a pattern to avoid), ag/cx<=4.0, ag/cx must stay above cc.
