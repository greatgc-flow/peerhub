# Session Archive — Engram — 2026-07-13 → 2026-07-15

## Theme: Capability-Leveling framework — design → implement → measure → audit → integrate

### Shipped (39 commits pushed to origin/main, 64201f2..97f8dd0; unit suite 1096 green)
- **Design (Phase 0):** `ops/capability-leveling.md` (evidence-qualified per-axis vector: measured>operational>declared>absent) + `ops/capability-leveling-decisions.md` (T41-T45 TDD-ready) + `ops/profile-policy*.md` + `ops/intelligence-scores.md`.
- **Implementation T41-T45 + T44a/b/c + T47:** fingerprint v2 (T41); declarations+resolver+overlay+check (T43); ag PTY canary harness (T42); fail-closed canary_budget ledger (T44a); capability-core + long_context canaries (T44b) + real invoker/CLI (T44c); shadow purpose-fitness + D5 gate (T45, live route byte-identical); discriminating reasoning + PTY invoker (T47).
- **Measurement (real --execute):** cx context = 272k/1MiB (the "1.05M" is unreachable via CLI); agentic file-write fidelity cx.deepthink=100 / cc.deepthink=68(UTF-8) / ag.deepthink=80(CRLF, stable_failed range 0 after harness fix); reasoning+code canaries do NOT discriminate frontier models (all 100).
- **T48 decision:** DECOUPLE D1 from a measured reasoning edge (frontier tie = ceiling; hard benchmark is open research) → Sol arbiter is an architecture/provider-diversity R:10 call; code-exec has no admin-free jail (restricted-DSL/absent).
- **Harness honesty (T49→T52):** the "agy flaky 10-100" was a HARNESS artifact (a PTY deadline timeout mis-scored; prompt-file slowdown) — fixed timeout classification, reverted to inline delivery, 600s ceiling; agy is stable_failed at 80.
- **4-peer cross-audit (ag/cx/ag.opus/cc):** found ~11 confirmed defects a single-terminal review missed (capability-core↔resolver never integrated end-to-end; incomplete T49/T50 fixes; standard profiles unmeasurable) → fixed 9 (T52) + completed + verified the per-axis overlay integration (T53A).

### Standing state
- **Backlog open:** T51 (agy PTY umbrella — urgent part done), T53B (AGY_CONFIG_HOME, latent), F1 (backlog automation, deferred), D4 (diag failover, deferred).
- **Durable memory:** `project_capability_leveling.md`, `measured_cx_context_capacity.md`, `feedback_peer_delegation_quarantine.md` (updated: after a governed-write delegation, recover file AND peer-recover).
- **Key lesson:** a single-terminal review of heavily-delegated work misses cross-file integration + incomplete-fix bugs — a multi-peer domain-split audit is high-ROI before declaring a big delegated body done.
