# Session Checkpoint — peerhub / Engram separation (2026-08-19)

Continuation from `2026-08-16_peerhub.md` (last touched 2026-08-17
~17:04). A large autonomous stretch happened between that checkpoint
and this session's visible start that this session has no direct
memory of (context compaction) -- reconstructed from git history,
installed package state, and AG's own persistent `brain/` planning
docs. Recorded here so it isn't lost again.

## Major milestone reconstructed: the real hub.py -> peerhub cutover already happened

Between the last checkpoint and this session, peerhub was built out to
v0.1.0-v0.1.7 as an **installed pip package** from a **separate GitHub
repo** (`github.com/greatgc-flow/peerhub.git`) -- distinct from the old
`P:\peerhub` source-checkout-inside-Engram layout this session's
earlier work assumed. P: itself was productized as "Engram": MIT
license, `github.com/greatgc-flow/Engram`, `Engram.exe` installer,
Winget manifests, portable release zips, `register.bat`/`unregister.bat`
virtual-drive integration, unified `engram.cmd` CLI.

`_sys/cli/hub.bat`/`diag.bat` now unconditionally forward to
`python -m peerhub.cli` (commit `375ac01`) -- the legacy 12,351-line
`_sys/core/hub.py`/2,593-line `_sys/cli/diag.py` are dead code, unused
by any live command, though still present in the repo. Confirmed via
direct testing: `peerhub.cli` only implements 5 subcommands
(`status`/`diag`/`broadcast`/`ask`/`statusline`) vs. hub.py's original
~90 actions -- the other ~85 (consensus, terminal-role rotation,
task/file locks, feedback/lessons/proposals, broker/credits) are
unreachable via `hub` today (fail cleanly, exit 2, not silent).

## This session's work (2026-08-19)

1. **"현황 확인" investigation** -- surfaced the above reconstruction,
   plus a live, real risk: `protocol.json`'s `collab_rate.current = 10`
   (max level, its own note says "R:10 enforcement is hardcoded in
   hub.py") but hub.py's consensus subsystem is unreachable via CLI and
   no peerhub-native replacement exists.
2. **User's actual direction, once "Encore" was clarified as a typo for
   "Engram"**: fully separate Engram (portable dev env) from peerhub
   (all peer/profile communication); drop unnecessary governance
   (`collab_rate` etc.) rather than porting it; fix `diag`/`--live` to
   show genuinely real-time data; push + release; pace ag/cx/ag.opus to
   full exhaustion.
3. **Planning**: found AG's own prior plan
   (`_sys/antigravity/config/brain/2c8ac180.../engram_streamlining_and_cleanup_plan.md`)
   already scoping this exact separation. Dispatched cx.deepthink for
   an independent round-1 proposal (`P:\workspace\engram_peerhub_separation_proposal.md`,
   ~270 lines, 12-phase safety-gated sequence) -- materially more
   rigorous than AG's plan (catches real risks AG's plan missed: e.g.
   `protocol.json` must be deleted whole and last, not piecemeal).
   Reconciled both into `P:\workspace\engram_peerhub_separation_RATIFIED_scope.md`.
4. **User scoped tonight down** to "Peerhub-side prerequisite work
   only" (proposal section 3, rows 3.1-3.10) after being shown the real
   scale (~150+ files for the full Engram-side cutover) and a real
   self-undermining-sequencing risk (the terminal was still delegating
   via legacy `hub.py ask`, so deleting hub.py first would cut off its
   own delegation channel).
5. **Delegated implementation, with real infra friction**: AG hit
   genuine G-pool AND 3P-pool exhaustion mid-session (reproduced
   directly via `agy.exe --model ...` returning immediate
   `status:ERROR, duration:0` -- not a bug); CX hit a ~24h
   `execution_uncertain` rate-limit after a large *successful* dispatch
   (verified real via `diag --fresh` showing X-pool genuinely at
   99.99x, not a false positive this time); two rounds of parallel
   background-bash dispatches got silently `killed` (root cause not
   found -- sequential single-task dispatches worked fine after
   switching). See new memory notes:
   `reference_gemini_exhaustion_looks_like_bug.md`,
   `reference_hub_false_rate_limit_long_dispatch.md`.
6. **Landed** (2 commits in `P:\workspace\peerhub`, a fresh clone of
   the real repo, NOT pushed):
   - `974754e` (ag.opus + cx.deepthink two-peer effort): removed
     hardcoded Engram `_sys`/`P:`/`D:` paths from
     `presenter.py`/`quota_polling.py`/`cli.py`; fixed the real bug the
     user originally reported (CC's stale 7D window showing 100% via a
     silent `.get(..., 100.0)` default) and a worse one found along the
     way (CX's entire quota block was hardcoded literals, not reading
     any live data at all).
   - `259512b` (done directly by the terminal -- both AG and CX were
     exhausted by this point): removed the fabricated `room-efde`
     room-ID literal (6 occurrences across `presenter.py`/`statusline.py`/`cli.py`),
     fixed the statusline log to write under peerhub's own
     `.peerhub/statusline/` instead of a hardcoded `_sys/data/temp/`
     path, bumped `pyproject.toml` 0.1.7 -> 0.1.8. Full suite: 1012
     passed / 0 failed (started this task at 1004/2-pre-existing-failing,
     fixed those 2 stale assertions too since they were adjacent and
     quick). pyright: 126 errors on both clean HEAD and the diff (git
     stash verified) -- zero new errors introduced.
7. **Follow-up "전체적으로 점검" of `diag`/`--live` found a much bigger,
   pre-existing architectural gap** (not something tonight's fixes
   caused): `--fresh` is a complete no-op (defined in argparse, never
   referenced anywhere); `poll_claude_usage()`/`poll_codex_usage()`/
   `record_usage_observations()` have **zero callers** anywhere in the
   live package (confirmed by grep); `TelemetryPresenter` is
   constructed in `_run_diag()` without `usage_projections`, so CC/CX
   quota is *always* empty in any real deployment, not just stale.
   AG's data is the one genuinely-live path (reads a raw file kept
   fresh by `agy`'s own statusline hook). Full writeup + fix
   requirements recorded in the backlog (see below) -- needs a small
   design decision (re-poll cadence for `--live`, given real API
   latency ~9.4s measured earlier) before implementing, and was not
   started tonight (channels exhausted).
8. **Backlog updated and committed** (`1368817`, peerhub repo):
   - `TelemetryWorker`/health-quota item expanded with the full
     diag/--live wiring-gap finding above, plus a call for an
     empirical (not mocked) CLI parameter test suite.
   - New entry: Engram/peerhub full separation, pointing at the
     ratified plan + proposal docs, recording exactly what's done
     (974754e + 259512b only) vs. what remains (the ~150-file
     Engram-side cluster; proposal rows 3.1-3.3/3.9 on the peerhub
     side).

## Explicitly deferred by user request ("일단 여기서 정리")

- Push of the 2 landed commits to `github.com/greatgc-flow/peerhub` --
  **not decided**, open question left for next session (push now vs.
  wait for the fuller separation to land as one release).
- The `diag`/`--live` real-data wiring fix itself -- documented, not
  implemented.
- The ~150-file Engram-side separation cutover -- documented
  (proposal + ratified scope), not started.
- `P:\_sys\claude\config\CLAUDE.md`'s collab_rate-driven "Multi-Peer
  Collaboration Protocol" section -- explicitly out of scope for any
  repo-level cutover (it's the user's own global config, outside both
  repos); needs its own rewrite once the Engram-side work actually
  removes collab_rate, per proposal row 9.4's suggested replacement
  concept ("use peerhub for deliberate peer communication; no
  collab-rate/R-level/Final Call/leader gate").

## Final state at this checkpoint

- `P:\workspace\peerhub`: clean, `main`, 3 commits ahead of
  `origin/main` (`974754e`, `259512b`, `1368817`), not pushed.
- `P:` (Engram) root: unchanged this session other than the read-only
  investigation; existing uncommitted items from before this session
  (`LICENSE`, `engram.cmd`, `output/`, modified
  `check_root_hygiene.py`) untouched, still pending a decision.
- Full peerhub test suite: 1012 passed, 0 failed as of `259512b`.
- Quota: AG (both G-pool and 3P-pool) and CX both confirmed genuinely
  exhausted by end of session -- the user's "소진해도 좋아" pacing goal
  was achieved.
