# Session Checkpoint — peerhub (2026-08-16, updated 2026-08-17 midday)

Continuation of the same file (originally written after T1 closed;
updated in place to cover everything since). Git clean, all work
committed to `main`, 48 commits ahead of `origin/main` (not pushed,
awaiting explicit confirmation).

## Latest stretch (2026-08-17 midday, under standing "proceed in order,
quality improvement, back in 4h" authorization)

Health/quota tracking's remaining scope (`TelemetryWorker` process-host)
correctly resolved to AWAITING-USER-DECISION rather than a unilateral
choice — peerhub has no persistent daemon process at all today, so a
60s-cadence background poller has no process to live in. 3 options
documented, poll-on-demand recommended; added to the backlog doc
(`d92b670`) alongside the pre-existing Windows Reducers entry.

With health/quota's non-decision scope exhausted, did a fresh sweep for
independently-verifiable quality work (no user judgment required) rather
than inventing new architecture-track scope:
- **Fixed a real flaky test** (`0ab6d17`): `test_soft_cancel_on_signal_ignoring_process`
  raced a fixed 200ms sleep against a spawned child's own Windows
  console-ctrl-handler registration; under this session's RAM pressure
  the race could lose, making the process actually terminate instead of
  staying RUNNING. Delegated to ag.deepthink; fix replaces the guess
  with a real READY-line handshake. Independently re-verified: 5/5
  isolated runs + full `tests/unit/dispatch/` (231 passed) + pyright
  clean, before trusting ag's own "fixed" report.
- **Closed a real drift-detector finding** (`491b60b`): `tools/peerhub_facts`
  flagged `peer.ag.version` DRIFT (1.1.12 -> 1.1.13, agy.exe moved
  versions on this workstation). Ran the tool's own `--live` adapter
  conformance probe against the real CLI (4 passed, 3 deselected) before
  trusting the version bump; added 1.1.13 to `verified_versions` per the
  drift-closure procedure. The tool's other flagged drift
  (`dependency.pip_check`: torch/setuptools) was checked and confirmed
  NOT peerhub's concern — torch belongs to `easyocr`/`torchvision` in the
  shared portable venv, not a peerhub dependency; left alone.
- **Fixed a stale backlog entry** (`1178366`): "Tool-call parsing — Not
  yet handled" was actually closed by T1 increment 4 (`9ff7813`);
  verified against real source (`DecoderEventKind.TOOL_CALL` in
  `peerhub/adapters/contract.py`, wired in `codex_adapter.py`) before
  correcting the doc.
- Full suite re-run clean twice after all of the above: 987 passed, 9
  deselected, 0 failures.
- Swept `peerhub/` for stray `TODO`/`FIXME`/`XXX`: none found — no more
  mechanical hygiene items outstanding.

**Deliberately did not start**: Track T7a / Phase 4 scoping. It's the
next item in the original ordered candidate list, but it's new
forward-looking architecture-track work, not "quality improvement" in
the sense the standing instruction asked for — held rather than
unilaterally expanding scope. Remains the natural next step whenever the
user wants to open that front.

## Second stretch (2026-08-17 mid-afternoon): user extended to "ag,
ag.opus EXH 5 by midnight" -- T7a started + 2-round doc audit

User explicitly asked to push both G-pool (ag.deepthink) AND 3P-pool
(ag.opus) toward EXH 5, extending the runway to midnight tonight.

- **T7a Phase 4 scoping memo done** (`ce38e13`): read-only investigation
  of hub.py's dispatch entry points, ownership-cluster signal
  (`scope_key`), same-revision comparison needs, and safe-disable
  mechanism. Every cited hub.py line/function independently re-verified
  against real source (all correct, at most one line off by 1) before
  committing. `docs/design/PHASE4-SCOPING-MEMO-2026-08-17.md`.
- **2-round design-doc staleness audit** (ag.opus round 1 + ag.deepthink
  round 2, 12 of 33 docs individually checked): found and fixed 8 real
  stale status-header/claim mismatches across 5 docs (broadcast design
  doc x5, capability-lease design, dispatch-loop-contract design,
  retry-loop design), all independently re-verified against real source
  (migrations, commits, line numbers) before touching anything. 3 docs
  confirmed clean (errata, 5C outer-loop plan, Windows Reducers). Every
  fix preserves the original historical text and appends a dated
  "Update" annotation rather than rewriting -- same pattern as the
  T1/Alembic hygiene fixes earlier in the session.
- **Important pacing finding**: a single ag.opus dispatch (round-1 audit,
  476s) spiked 3P-pool EXH from 0.00x to **19.93x** -- ~4x past the
  user's 5.0x target, from ONE dispatch. 3P-pool's 5H window is much
  narrower than G-pool's, so it does not scale the same way per-dispatch.
  Saved as memory (`reference_3p_pool_narrow_5h_window_spike.md`).
  Stopped further ag.opus dispatches once this was noticed; G-pool
  remained healthy at ~1.06x throughout (many more ag.deepthink
  dispatches, far smaller EXH movement per call).
- Considered but did NOT dispatch: a "broadcast fan_out() test coverage"
  task (fewer test functions than capability.py) -- checked myself first
  and found it's not actually undertested (5 large integration tests +
  separate schema-level cycle-safety tests already cover it well); a
  manufactured task would have wasted quota for no real finding.

## Third stretch (2026-08-17 ~16:00-17:00 KST): "check EXH and continue"
-- 2 more doc-audit rounds + README refresh, backlog now exhausted

G-pool/3P-pool both reset fresh mid-stretch (0.22x/0.00x). Continued:
- **Round 2 doc audit** (ag.deepthink, 6 docs not covered by round 1):
  found 3 more real stale claims -- CAPABILITY-LEASE-DESIGN-2026-08-08.md
  ("proposed, NOT implemented" when it's fully shipped),
  PHASE3-DISPATCH-LOOP-CONTRACT-DESIGN-2026-08-12.md (header said
  increments 4-5 not started; body already said 4 was done, header just
  wasn't updated), PHASE3-T1-INCREMENT5-RETRY-LOOP-DESIGN-R1-2026-08-13.md
  ("5B and 5C not started" -- both long done). All fixed (`4c21f40`),
  all independently re-verified against real source first.
- **Round 3 doc audit** (ag.deepthink, 8 "living reference" docs,
  explicitly excluding intentional historical snapshots): found 3
  claimed findings, verified 2 real + **caught 1 hallucination**:
  ATOMICITY-MATRIX-2026-08-06.md's claim that 3 fault-boundary test
  files "do not exist" was FALSE -- all three exist in
  `tests/integration/persistence/`; rejected, no fix applied. The 2 real
  ones: ARCHITECTURE.md (still said "not yet implemented... No code,
  tests, or scaffolding exist yet" from the 2026-07-27 pre-implementation
  debate -- the single most stale claim found in 3 audit rounds, given
  virtually the entire package has since been built) and
  TDD-READINESS-INVENTORY-R1.md (fixtures listed `V1_SPEC_ONLY` that were
  promoted to `V1_CAPTURE`/`SPEC_FAITHFUL` per the gate-closure doc).
  Fixed (`a079cf5`).
- **README.md refresh** (`a8ba667`): was 5 days stale with several false
  claims (fan_out() and the T1 outer loop "not yet built", Ctrl-C ladder
  "not wired", migration count stuck at 20 vs real 24). Dispatched a full
  rewrite to ag.deepthink grounded in the roadmap doc's DONE markers;
  spot-verified the new `--peer`/`--all` flags and `cancellation_hook`
  wiring against `peerhub/cli.py` directly before trusting it (same
  session that just caught a hallucination from this peer -- verified
  anyway, no exceptions).
- **peerhub_facts drift check + full suite** re-run clean after all of
  the above: 20 PASS / 1 DRIFT (the pre-existing, confirmed-unrelated
  torch/setuptools note), 987 passed / 0 failed.
- **Backlog reviewed end-to-end**: every UNSCHEDULED-READY item is now
  done-and-committed or correctly deferred-with-a-named-trigger; every
  SCHEDULED-DEFERRED item's trigger is unfired; the one BLOCKED item
  (ConsensusRound) stays blocked by design; all 4 AWAITING-USER-DECISION
  items genuinely need the user's own judgment call. **No further
  legitimately-delegable, non-user-decision work remains in the backlog
  as of this checkpoint** -- G-pool sitting well under the 5.0x target
  (only ~1.04x after 3 solid audit rounds + README refresh) reflects that
  the real work ran out, not under-effort; manufacturing filler tasks to
  run the number up was explicitly avoided (see the fan_out() example
  above) and would go against this session's own established discipline.

## Everything since the last checkpoint version

**EvidenceArtifact — now fully complete, all 3 increments**:
- Increment 1 (`baa6a04`): `EvidenceArtifact` dataclass + migration
  `0023_evidence_artifacts.sql` (data layer). Landing this migration
  surfaced 3 pre-existing tests that hardcoded the prior latest-
  migration version (22) instead of deriving it -- fixed.
- Increment 2 (`d744ce9`): Claude adapter offload/substitution wiring.
  Implementer's own "pyright clean" claim was false (4 real errors,
  fixed by adding a proper `list[ArtifactSpec]` type annotation instead
  of an untyped `[]`) -- caught by independent re-run.
- Increment 3 (`89e6d2a`): Codex adapter wiring, mirroring Claude's
  pattern exactly, including a RESUME-branch-specific test (Codex has
  two prompt-construction paths, Claude only one). This dispatch failed
  once with `execution_state=uncertain` after a 24-minute timeout (not
  the usual RAM-kill pattern) but the implementation had already fully
  landed in the working tree; independently re-verified and committed
  as-is.
- agy adapter wiring was never in scope (the ratified design only
  covers Claude + Codex) -- not started, would need its own design
  check first if ever wanted.
- Final state: 969 passed, pyright clean.

**Health/quota tracking (T2) — canary passed, no longer implementation-
gated**:
- Real empirical probes run via `RealAgyAdapter` and direct subprocess
  calls from a peerhub-context process: Codex app-server PASS (real
  rate-limit JSON), Antigravity statusline log PASS (real before/after
  mtime+content diff through an actual `RealAgyAdapter` dispatch),
  Claude initially INCONCLUSIVE (`claude.cmd /usage` timed out at 15s).
- Root-caused the Claude timeout properly rather than accepting
  INCONCLUSIVE: under any non-TTY subprocess context (which peerhub
  always is -- P:'s hub.py benefits from an inherited TTY that peerhub
  will never have), `claude.exe` adds a ~3s stdin-wait on top of its own
  real ~9.4s API latency, reliably exceeding a 12s deadline. Confirmed
  by 4 failing probes (full-env copy, minimal-env, direct import from
  both `P:\peerhub` and `P:\_sys\core` -- all failed identically) and
  one succeeding probe (`stdin=subprocess.DEVNULL` -> 9.39s). This is a
  genuine, permanent architectural fact about peerhub, not a probe
  artifact -- folded into the design doc as a hard implementation
  requirement (`acb6635`): `stdin=subprocess.DEVNULL` + >=15s deadline
  for this specific probe.
- **Health/quota tracking is now fully unblocked and ready to
  implement** -- not yet started.

**cc token-consumption root cause, investigated with real data**
(`reference_cc_shared_quota_with_terminal.md`): `protocol.json` pins
`"human_interface_peer": "cc"` -- the terminal session IS a cc.*
instance, so cc.effort/cc.deepthink peer dispatches draw the SAME
C-pool quota the terminal itself consumes just by existing, unlike
ag/cx which are genuinely separate. Confirmed via `diag.py`'s RECENT
TOKEN CONSUMPTION table: a single unfreshed cc.effort session
(`f284af78`) accumulated 1,100,974 total tokens across ~6-7 dispatches
in the prior work stretch, because `--session-policy fresh` had been
used consistently for ag.deepthink but never for cc.effort -- each
reused-session call re-paid for the full accumulated history. C-pool
EXH climbed 3.75x -> 6.29x across the two sessions covered by this
checkpoint, continuing to climb even with zero further cc.effort
dispatches (since the terminal itself keeps drawing the same pool).
Going forward: always pass `--session-policy fresh` for cc.effort too;
prefer cx/ag for independent-critique roles when quota allows since
cc.effort is never actually "free" relative to the terminal's own
budget.

## Current backlog state (`BACKLOG-CONSOLIDATED-2026-08-16.md`)

- **Ready to implement, not started**: health/quota tracking (T2, no
  longer gated).
- **Ratified, still gated**: Windows-native Brokered Read-Only Reducers
  -- 3 empirical preconditions unresolved (spawn-privilege availability,
  `agy.exe`'s real write footprint, network-egress policy). Not
  investigated yet.
- **Done**: EvidenceArtifact (all 3 increments), CLI Ctrl-C wiring, both
  real-agy test fixes.
- **AWAITING-USER-DECISION, untouched**: inter-agent message monitoring
  (MAPLE-Guard pattern), adaptive/structural loop termination.
- **Untouched all session**: Track T7a (Phase 4 scoping), the 3 Phase 4
  design items, `agy` adapter wiring for EvidenceArtifact (never scoped
  in the ratified design).

## Standing pattern, reconfirmed yet again across this whole stretch

Every single implementation dispatch this session had at least one real,
verifiable gap between what the implementer's own report claimed
("done"/"clean"/"passing") and what independent re-verification actually
found, when the terminal bothered to check directly rather than trust
the report. This happened enough times (agy tests, Ctrl-C wiring,
EvidenceArtifact increments 2 and 3, the Codex 24-minute-timeout survival)
that it should be treated as the reliable default expectation, not an
occasional surprise -- budget the verification step into every dispatch,
every time, no exceptions.

## Final state at this checkpoint

- Git: clean, `main`, 66 commits ahead of `origin/main`, not pushed.
- RAM: still tight, ~2.6-3.5GB free throughout; no dispatch kills this
  round (every ag/ag.opus dispatch completed cleanly).
- EXH (as of 2026-08-17 ~17:00 KST): **C-pool 99.99x CRIT** (capped;
  this is the terminal's own long-session token footprint, not a peer
  dispatch -- no cc.* dispatches happened this stretch at all; worth a
  fresh-session rotation if this checkpoint is picked up later rather
  than resumed inline); G-pool 1.04x (still well under the 5.0x target
  after 4 solid rounds of ag.deepthink work -- real backlog work simply
  ran out, see above); 3P-pool 4.07x (settled after the earlier 19.93x
  post-reset spike, not actively climbing since no new ag.opus
  dispatches followed); X-pool still CRIT (9.14x) -- cx avoided
  throughout, as in every prior stretch.
- Full suite independently re-run clean after every commit this
  stretch, most recently: 987 passed, 9 deselected, 0 failed.

## Natural next steps (paused here at explicit user request to slow down
and consolidate, not started)

1. Windows-native Brokered Read-Only Reducers: investigate the 3 gating
   preconditions (same empirical-probe discipline as the health/quota
   canary).
2. Implement health/quota tracking (now unblocked).
3. The 2 AWAITING-USER-DECISION backlog items -- need the user's own
   go/no-go, not further peer investigation.
4. Track T7a / Phase 4 design items -- untouched, lowest priority per
   the roadmap's own sequencing (Phase 4 work is explicitly gated behind
   Phase 3/T1, which is now fully done, so this becomes viable whenever
   there's appetite for it).
