
#### `report-error` Auto-quarantine Gap Assessment (2026-08-31)

**RESEARCH FINDINGS:**
1. **`HealthService.classify_and_open_circuit()` requirements:** Direct inspection (`peerhub/health/service.py:1057-1072`) confirms this method strictly requires typed evidence (`attempted_trace: tuple[HealthStageObservation, ...] | list[HealthStageObservation]`) and an optional `evidence_subject: EvidenceSubject | None` and `receipt: PolicyReceipt | None`. There is genuinely no legitimate way to construct these types from a free-form `report-error` string (peer_key, pattern, severity, detail, actor_id, count). Doing so would fabricate evidence, repeating the exact authority-model mistake rejected for `peer-quarantine`.
2. **Legacy `report-error` auto-quarantine behavior:** Legacy (`P:/_sys/core/hub.py:9532-9533`) blindly checks if the threshold is crossed and directly calls `action_peer_quarantine(ai_root, event["peer"], f"operational_error:{event['pattern']}")`. It uses the exact same `peer-quarantine` false-friend machinery which bypasses proper evidence/authority models.
3. **The honest boundary:** The `OperationalErrorService`'s current behavior of creating a distinct immutable `quarantine-review` target (`peerhub/governance/operational_errors.py:224-249`) already represents the CORRECT honest boundary. A full search of the `peerhub` directory confirms that **nothing consumes `quarantine-review` targets today**. The real gap is not inside `report-error`, but the lack of a review-consumer (a coordinator that reads these targets and routes them to an authority-bearing process like `authorize_administrative_recovery()`).

**VERDICT:**
There is no legitimate honest mapping from a free-form string to typed evidence without inventing evidence. Therefore, **`report-error`'s auto-quarantine must stay permanently deferred** as originally decided. The review-request creation already implemented is the correct and complete native equivalent. The remaining work belongs to a separate, future review-consumer domain, not `report-error`.

# peerhub Backlog (consolidated, 2026-08-27)

> Single source of truth for "what's left." Supersedes hunting across `docs/design/HUB-REPLACEMENT-TDD-PROGRESS-2026-08-27.md`, the README's status lists, and per-gap design docs to answer "what's next" — those documents remain the detailed record of *why* each decision was made; this one is the current, flat *what's outstanding* list, organized by how ready each item is to pick up. Update this doc's tiers as items move, rather than letting the README's own "Designed but not built" / "Explicitly deferred" / "Not yet implemented" lists drift out of sync again (see the "Known drift caught while writing this doc" note at the bottom — that's exactly the failure mode this doc exists to prevent).

## Where things stand right now

**DONE (2026-08-31): `PeerProfileBinding` implemented, `model-status` now translates and executes end-to-end (50/90).** Adds `peer-profile-binding:{node_id}:{profile_id}` targets and `PeerRegistryService.bind_profile()`/`get_profile_binding()`/`list_profile_bindings()`, filling in the previously-ratified-but-never-built Round 9 SSOT (`ARCHITECTURE.md` §6.1a). `model-status` joins node registrations with their profile bindings and a `HealthService` projection fallback for capabilities/context, matching real legacy's documented principle that model/effort/cost/context come from the configured profile, not from potentially-stale `health.json` fields (verified against `_sys/core/hub.py:11748-11773`). `register_node()` deliberately does not auto-create a binding -- `bind_profile()` is a separate, purely-additive native command with no legacy counterpart, so it does not itself move the `LEGACY_CATALOG` count. The implementing dispatch (cx.deepthink) reported `execution_state=uncertain` and got rate-limited, but had completed the real, correct work before that -- verified fresh by the terminal: full suite 1279 passed (only the pre-existing unrelated manifest-snapshot test fails), 0 new pyright errors.

**DONE (Gap A, 2026-08-30): Centralized read-time freshness in `HealthService` (commit 75ff364).** The design for `read_health_projection(..., evaluated_at)` is now fully implemented, tested, and committed to main. Note: Gap B (Health-Evidence Producer outside bootstrap) remains RESEARCH-STAGE / BLOCKED pending a core policy decision on manual quarantine authorization. The `LEGACY_CATALOG` count stays 44/90 for this round -- Gap A was purely an internal correctness fix for routing and read-time freshness. It did not directly unblock any new legacy actions (`health-check` and `peer-status` remain blocked on Gap B).

**FINDING (2026-08-29, real correctness gap in already-counted work): `leader-claim`/`leader-yield` implement the wrong semantic entirely -- 2 of the 46 "backed" actions execute successfully but do not match legacy behavior.** Discovered while scoping `elect-leader` (which delegates to `action_leader_claim`, so its design depends on `leader-claim` being real). Verified directly: `routing.leadership.claim`'s handler (`peerhub/application/api.py`, the `_register_duty` method) is literally `d.create_lease(DutyLeaseCreateRequest(room_id, "leader", DutyOwnerIdentity(instance_id, profile_id), owner_principal_id, 60000, authority_epoch))` -- the exact same generic `DutyLeaseCoordinator.create_lease()` used for any ordinary room-scoped role (`implementer`, `verifier`, etc.), just with the string `"leader"` as the role name. `routing.leadership.yield` is the matching generic `close_lease()`. Real legacy `leader-claim`/`leader-yield` (docs/design/PHASE1-PARITY-LEDGER-BATCH3-2026-08-20.md sections 3-4) are workspace-global (no `room_id` concept at all), require an AP-20 consecutive-term monopoly guard checked against `coordinator_history`, gate a new claim on the *existing* leader's live health (reject if healthy and outside its challenge window; allow immediate takeover if the leader is RED/STALE), track a `PENDING`-during-challenge-window status distinct from ordinary immediate-`ACTIVE` role leases, and maintain a `coordinator_history` audit trail -- none of which the current implementation has in any form. This is not a new gap; it predates this session's TDD-catalog-counting phase entirely -- `docs/design/HUB-REPLACEMENT-GAP4-HEALTH-LEADERSHIP-2026-08-24.md` (written 5 days before the TDD phase started) already correctly specified that leadership needed "the SAME underlying assignment substrate as gap-3's terminal-duty leases, but with different semantic types/policies" layered on top -- the extra policy layer was the part that never got built; only the bare substrate-reuse shipped. It was never caught because `leader-claim`/`leader-yield` were wired and counted as "backed" before this session started cross-referencing every action against the real parity ledger one by one.

**Not fixed on 2026-08-29, at the user's explicit direction ("오늘은 문서화만하고 마무리") -- picked back up, ratified, AND FIXED on 2026-08-30.** See the "`LeadershipService` RATIFIED" section below for the full design (two-round research-then-critique cycle, cx.deepthink research + cc.deepthink critique) and its implementation. `leader-claim`/`leader-yield` now correctly implement workspace-global leadership. The `LEGACY_CATALOG` count stays 44/90 for this round -- both actions were already counted before (they always translated and executed *something*), so this was a correctness fix for 2 already-counted actions, not new coverage.

**Bookkeeping note, unrelated to tonight's work, found while recounting:** the `grep -oE 'if call.action == "[a-z-]+"' ... | wc -l` formula used throughout this session to derive the `LEGACY_CATALOG` count has a small, longstanding, self-consistent imprecision -- it counts `ask`/`ask-all`/`ask-coordinator`'s translate() branches (which exist and produce a `TranslatedCommand`, but whose target methods, `dispatch.submit`/`.submit_many`/`.submit_coordinator`, are never registered as real `CommandDescriptor`s, so they don't actually execute end-to-end -- see the earlier `test_legacy_translation_ask` finding that established this convention in the first place) alongside the +1 manual adjustment for the `terminal-heartbeat` dual-branch quirk. Net effect: every count reported this session has likely been 2 higher than the strictly-correct "genuinely executes end-to-end" number (3 over-counted ask-variants, 1 under-counted from the +1 adjustment). **Clean recount performed on 2026-08-30:** the strictly-correct count of genuinely-executing legacy actions is 44. All documentation has been updated to reflect this correct absolute number.

### `LeadershipService` RATIFIED (2026-08-30): a real workspace-global singleton, deliberately NOT built on DutyLeaseCoordinator

Research (cx.deepthink) then critique (cc.deepthink), both independently spot-checked by the terminal directly against real `_sys/core/hub.py` source -- every checked claim, including two genuinely counterintuitive ones, confirmed accurate: **an unexpired `challenge_until` window PERMITS a competing claim to overwrite the pending claimant -- it does NOT protect the incumbent** (health-gating only protects a leader once the window has expired or never existed; the opposite of the natural-sounding assumption); and **`DutyLeaseCoordinator`'s existing AP-20-style guard has a genuine off-by-one vs. real legacy** (rejects the 3rd same-peer claim; legacy allows claims 1-3 and rejects the 4th) -- separate confirming evidence, not a bug to fix in `duty_lease.py` itself, that `LeadershipService` must not reuse that machinery.

**Placement: `peerhub/application/leadership.py`, NOT built on `DutyLeaseCoordinator` at all.** Confirmed incompatible on every axis: room-scoped vs. legacy's workspace-global; immediately-`ACTIVE` vs. legacy's `PENDING`-with-challenge-window; heartbeat-expiring vs. legacy's non-expiring; fence-strict-yield-rejection vs. legacy's warn-and-vacate-for-anyone. The currently-shipped `routing.leadership.claim`/`.yield` (the bug this replaces) are exactly this wrong reuse.

**Schema: one singleton `leadership:workspace` target** (matching `arbiter-budget:workspace`'s precedent for when a singleton is justified -- a genuine workspace-wide at-most-one-leader invariant, and AP-20's check-then-append must be atomic with the leadership write in one `broker.submit()`). State: `status` (`VACANT|PENDING|ACTIVE` -- `ACTIVE` is reserved vocabulary this round, nothing in this round's scope ever writes it, see below), `term` (native-only audit convenience, explicitly non-authoritative, nothing may gate on it), `claim_id` (fresh every successful claim), `leader` (null, or `{peer_node_id, peer_node_target_id, peer_node_revision, peer_kind, profile_id, adapter_version}` -- a frozen registry binding, requiring `peer_registry.get_node()` to resolve; unregistered-text leaders are rejected, closing a real legacy pathology where an unresolvable agent name becomes permanently unchallengeable since it always reads as health `UNKNOWN`), `claimed_at`, `claimed_by`, `challenge_until`, `domain`, `reason` (both with legacy's exact fallbacks: `domain = domain or reason or "general"`, `reason = reason or "manual_claim"`), `claim_basis` (null, or `{disposition: VACANT_CLAIM|SELF_RECLAIM|OPEN_WINDOW_CHALLENGE|FAILED_INCUMBENT_TAKEOVER, incumbent_peer_node_id, projection_id, projection_revision, availability_state, admission_state, stale_at_read}`), `yielded_by`, `yielded_at`, `yield_reason`, `coordinator_history` (embedded, bounded to last 10, matching legacy's own `history[-10:]`), `updated_at`.

**Claim predicate** (re-evaluated fresh on every CAS retry): AP-20 first, unconditionally (applies even to a self-reclaim, matching source ordering) -- reject iff the last `threshold` history entries all belong to the candidate. Then: `VACANT`/absent -> `VACANT_CLAIM`; candidate equals current leader -> `SELF_RECLAIM`, no incumbent health check; `challenge_until is not None and now < challenge_until` (gated purely on the timestamp, no `status` condition -- a narrowing cx's draft had that critique removed) -> `OPEN_WINDOW_CHALLENGE`, unconditional; otherwise evaluate the *incumbent's* health via `HealthService.get_health_projection()` on its stored `peer_kind`/`profile_id` -- replaceable (`FAILED_INCUMBENT_TAKEOVER`) only if a projection exists AND (availability in `{UNAVAILABLE,STALE}` OR admission in `{QUARANTINED,COOLDOWN,RECOVERY_REQUIRED}` OR stale past `readiness_freshness_seconds`); otherwise raise `LeadershipIncumbentProtectedError` -- absent evidence protects the incumbent (the mirror image of role-assignment's fail-open-admits-a-candidate polarity: same deny-list shape, opposite user-visible outcome, since this gates an incumbent rather than admitting a candidate). The new candidate itself is never health-gated -- legacy only ever gates the incumbent.

**Yield**: always mutates (even already-`VACANT`, no target, or a non-leader yielding -- returns `owner_mismatch`/`previous_leader_peer_node_id` as warning metadata, never raises). Legacy fully *replaces* the `leadership` sub-object rather than partially updating it, so yield must null `leader`/`claim_id`/`challenge_until`/`claimed_at`/`claimed_by`/`domain`/`claim_basis`, not just set `status=VACANT` -- retaining a stale `challenge_until` after a yield (cx's original draft) would leave actively misleading residue. `term` and `coordinator_history` are retained (legacy siblings of `leadership` in `state.json`, untouched by yield).

**No `activate_pending_leadership()` this round** -- decisive new evidence found in critique: grepping every real legacy write of `leadership.status = "ACTIVE"` found exactly one call site, and it belongs to `action_terminal_handoff`, a *different* action in a domain peerhub already has (`TerminalDutyService`) -- not a timeout-driven settle step. Nothing in legacy ever transitions `PENDING` to `ACTIVE` on elapsed time; there is no legacy sweep/settle/activate operation at all. Building one now would be an invented state transition with no legacy counterpart and no caller. The `ACTIVE` <- `terminal-handoff` cross-domain coupling is recorded as a real, separate scoping question for later, not designed now.

**Retry**: bounded 16-attempt CAS loop (mirroring `ArbiterBudgetManager`), full predicate re-evaluated fresh each attempt -- justified because legacy serializes these operations under one real global lock, making CAS-retry-with-fresh-reevaluation the correct analog of sequential processing (a second concurrent claim genuinely should see the first's new `PENDING` state and be able to challenge it within the window), not a shortcut. Only `StaleRevisionError` may consume a retry attempt -- AP-20 and incumbent-protection rejections raise immediately, so a losing claimant doesn't burn 16 attempts re-deriving the same refusal. Yield's warning metadata must be computed from the winning attempt's read, not a stale earlier one.

**Errors**: two distinct types, both subclassing `InvalidMutationError` (matching `RoleAssigneeUnavailableError`'s precedent) -- `LeadershipMonopolyError` (`ErrorCode.ACTOR_UNAUTHORIZED`, the actor is specifically barred) and `LeadershipIncumbentProtectedError` (default `INVALID_PARAMS`; `PEER_UNAVAILABLE` would be the wrong code here since the incumbent IS available -- that's precisely why the claim fails).

**Policy**: `LeadershipPolicy(challenge_window_seconds=60, monopoly_threshold=3, history_limit=10)` injected with hardcoded defaults, validated in `__post_init__` like `FinalArbiterPolicy`. No new `.peerhub/leadership.json` this round -- `arbiter.json`'s file-presence-as-enablement-switch pattern doesn't transfer (leadership has no on/off semantics, just tuning constants legacy itself reads from the *shared* protocol config, never a leadership-specific file), and inventing a third bespoke config loader would pre-empt the still-open "Configuration/telemetry scouting" cluster's own eventual decision.

**Deferred, recorded but explicitly out of scope this round**: pressure-reason task checkpointing on yield (`_checkpoint_active_tasks`, would drag `TaskService` into the constructor for an effect legacy performs outside its state lock, no bearing on leadership correctness); the AP-20-violation host runtime-directive write (same class as the already-flagged `directive-add/list/clear` host-level cluster); a legacy normalization sweep that vacates leadership when the leader is no longer a routable root peer (no peerhub counterpart yet).

**`elect-leader` remains separately blocked**, unlocked but not solved by this round: it needs `claim_leadership(..., domain=...)` as its landing seam (confirmed a clean fit) but ALSO needs real capability-matching scoring (`_matching_peers(needs, effort)`) that peerhub still lacks entirely -- `RoutingService.select_route()` only does boolean eligibility with equal weights, not scored matching. Separate prerequisite, separate round.

Ready for an implementation round.

**Update (2026-08-30): DONE, exactly as ratified above, replacing the buggy `DutyLeaseCoordinator`-based implementation entirely.** `peerhub/application/leadership.py` implements `LeadershipService`; the old wrong `routing.leadership.claim`/`.yield` registrations and `LeaderClaimCommand`/`LeaderYieldCommand` (room-scoped) shapes are fully removed, not left coexisting. One real subtlety the implementer caught independently, not flagged in the dispatch: `TerminalHeartbeatCommand`/`TerminalCloseCommand` (real, correct, unrelated terminal-duty domains) had been *inheriting* their fields from the old `LeaderYieldCommand` -- changing that class's shape would have silently broken them. Correctly re-based onto their own independent dataclass. One small inconsistency the terminal caught and fixed on review: yield correctly nulled every other claim-time field (`domain`, `claimed_at`, `claimed_by`, `challenge_until`, `claim_basis`) but left the claim's `reason` field untouched -- the same class of "misleading residue" mistake the critique round had already caught and fixed for `challenge_until` in the design phase, just recurring once more at implementation time in a field the ratification text itself hadn't explicitly enumerated. Fixed directly, with a strengthened test assertion. Verified: 23 new leadership tests plus all 100 tests across the three touched files (including the pre-existing terminal-duty tests, confirming the inheritance fix didn't regress anything) passed for real, full suite 1229 passed (only the known pre-existing manifest-snapshot failure), 0 new pyright errors, and a full manual CLI smoke test proving the design's most distinctive, counterintuitive behavior end-to-end: a claim by `cx` while `cc`'s claim is still inside its challenge window correctly *succeeds* (`OPEN_WINDOW_CHALLENGE`, overwriting the pending claimant) rather than being blocked.

**Update (2026-08-29, report-error implementation round): 44 of the 90 `LEGACY_CATALOG` actions backed.** `report-error` now translates and executes end-to-end via a new `OperationalErrorService` (`peerhub/governance/operational_errors.py`), implementing the design ratified same-day in the "`report-error` RATIFIED" section below exactly: a shared per-`(peer,pattern)` CAS counter (the one domain this session that deliberately DOES retry on `StaleRevisionError`, justified as a genuine shared-counter exception, unlike every other per-entity domain), and a distinct immutable `quarantine-review` request created for every report whose resulting count is `>= threshold` -- verified directly with a real 4-report sequence (threshold=3) producing exactly two separate review targets with `trigger_count` 3 and 4, not one overwritten record, confirming the corrected repeat-trigger behavior actually works. Third implementation round in a row with zero bugs found on independent review. Verified: 9 real pytest cases, full suite 1203 passed (only the known pre-existing manifest-snapshot failure), 0 new pyright errors, full manual CLI smoke test matching the ratified design exactly (three `peerhub error report` calls correctly producing count=1, count=2, then count=3 with `quarantine_review_id` populated). Full narrative in `docs/design/HUB-REPLACEMENT-TDD-PROGRESS-2026-08-27.md`.

**Update (2026-08-29, feedback-journal implementation round): 43 of the 90 `LEGACY_CATALOG` actions backed.** `feedback-add`/`feedback-list`/`feedback-resolve` now translate and execute end-to-end via a new `FeedbackService` (`peerhub/governance/feedback.py`, placed in `governance/` not `application/` -- it's a self-contained journal with no cross-domain coordination, matching `LessonService`/`ConsensusService`'s placement rather than `PeerRegistryService`/`RoleAssignmentService`'s). Implements the design ratified same-day in the "Governance artifacts / feedback scouting" section below exactly: sequential `GAP-YYYYMMDD-NNN` IDs allocated from a UTC (not host-local) date component, `status` as validated free text rather than a restricted enum, no retry-with-next-number loop on a create-time CAS collision (surfaces as a real `StaleRevisionError` like every other per-entity domain), and `resolve_feedback()` correctly refreshing `resolved_at`/`updated_at` on every call including a repeat call to an already-resolved item while preserving the existing `owner` when none is supplied. The implementing dispatch (`cc.deepthink`) reported a 900s timeout with no captured response text -- but had, in fact, silently finished the complete implementation (all 7 files) before the timeout; found via `git status` per the standing "a killed/timed-out dispatch may still write" lesson, with no self-reported test claims to cross-check against, so this round's verification was entirely fresh. Zero bugs found on independent review -- the second implementation round in a row with none (role-assignment was the first) -- verified via 15 real pytest cases, full suite 1194 passed (only the known pre-existing manifest-snapshot failure), 0 new pyright errors, and a full manual CLI smoke test (`peerhub feedback add|list|resolve`) that matched legacy's exact TSV list format (`id\tstatus\tseverity\tcategory\ttitle`) and default-value/alias handling (`--peer`/`--from`, `--subject`/`--msg`, `--feedback-id`/`--round-id`, `--agent`/`--peer` for owner) precisely. Full narrative in `docs/design/HUB-REPLACEMENT-TDD-PROGRESS-2026-08-27.md`.

**Update (2026-08-29, role-assignment implementation round): 40 of the 90 `LEGACY_CATALOG` actions backed.** `assign-role`/`release-role`/`role-status` now translate and execute end-to-end via a new `RoleAssignmentService` (`peerhub/application/role_assignment.py`), implementing a two-round-ratified design (research by cx.deepthink, critique by cc.deepthink) that overturned a real, load-bearing assumption: legacy's health gate is fail-*open* on absent/unknown health evidence, not fail-closed, verified directly against real `_sys/core/hub.py` source (a peer with no `health.json` on disk defaults to status "UNKNOWN", which is not in `_healthy_peer`'s blocked set, so `assign-role` succeeds). Implemented as a deny-list (reject only on `UNAVAILABLE`/`STALE` availability, `QUARANTINED`/`COOLDOWN`/`RECOVERY_REQUIRED` admission, or a projection older than the policy's `readiness_freshness_seconds`, itself equal to legacy's `health_stale_minutes` default of 7200s) rather than requiring evidence to exist at all. Two small prerequisites landed first in commit `921e938`: a real, independently-reproduced bug fix (`PeerRegistryService.get_node("cc")` previously always raised `RecordNotFoundError` since `register_node` correctly rejects "cc" as a storable node_id, while `list_nodes()` correctly includes it -- now `get_node()` falls back to the same base-adapter synthesis `list_nodes()` uses) and two new public `HealthService` reads (`get_health_projection`, `policy`) needed by the gate. The implementation itself (cx.deepthink) shipped with zero bugs found on independent review this round -- a first for a peer implementation this session -- verified via 16 real pytest cases (7 covering the full health-gate deny-list matrix plus staleness, non-idempotent reassignment, ownership-mismatch rejection, and RELEASED-but-still-queryable release semantics), full suite 1179 passed (only the known pre-existing manifest-snapshot failure), 0 new pyright errors, and a full manual CLI smoke test (`peerhub role assign|release|status`) matching legacy parity exactly including the graceful no-op-warning on a repeat release. Full narrative in `docs/design/HUB-REPLACEMENT-TDD-PROGRESS-2026-08-27.md`.

**Update (2026-08-29, peer-registry implementation round): 37 of the 90 `LEGACY_CATALOG` actions backed.** `register-node`/`list-nodes` now translate and execute end-to-end, implementing the ratified `peer-registry` design below in full: `PeerRegistryService` (`peerhub/application/peer_registry.py`), `RegisterNodeCommand`/`ListNodesCommand` + translate() branches in `legacy.py`, `configuration.instance.register`/`configuration.instance.list` `CommandDescriptor`s in `api.py`, runtime wiring, and a `peerhub node register`/`peerhub node list` CLI. First dispatch (`ag.deepthink`) was killed mid-flight again but left real, partial writes to `legacy.py`/`api.py`/a new untracked `peer_registry.py` -- reviewed rather than discarded per the standing lesson, and found genuinely broken on import (`Clock`/`IdAllocator` imported from the wrong module, a raw `uuid4()` bypassing the injected `IdSource`, `idempotency_key` generated separately from `request_id` instead of reusing it, `.adapter_version` misspelled as `.version`) -- rewritten directly against the real `ConsensusService._submit()` precedent rather than re-dispatched a fourth time, then completed (CLI/runtime wiring, 11 new integration tests, 2 translate-and-execute tests) and independently verified: full suite 1162 passed (only the known pre-existing manifest-snapshot failure), 0 new pyright errors, plus a real CLI smoke test (register/list/collision-reject all correct). Full narrative in `docs/design/HUB-REPLACEMENT-TDD-PROGRESS-2026-08-27.md`.

**Update (2026-08-28, arbiter-review wiring round): 35 of the 90 `LEGACY_CATALOG` actions backed -- Tier 3 is now fully closed (7/7).** `arbiter-review`'s CLI + legacy-translation + execution-dispatcher wiring (steps 6-8 of the ratified 9-step plan) landed on top of the already-committed core service layer: `ArbiterReviewCommand`/the `if call.action == "arbiter-review":` translate() branch in `legacy.py`, a `consensus.arbiter.review` `CommandDescriptor` (`Mutability.MUTATING`, `IdempotencyPolicy.DOMAIN_ATOMIC_REQUIRED` -- re-calling `.review()` on an already-fired round would spend budget again, so this deliberately isn't `READ_ONLY`) in `api.py`, a `peerhub consensus arbiter-review --round-id <id>` CLI subcommand, and `create_runtime()` now threading an injectable `arbiter_executor` (defaults to the real `execute_direct_ask` in production; tests inject a fake so no test path can ever spawn a real peer subprocess). First dispatch of this wiring round was killed mid-flight by the environment; the terminal found it had still written real (syntactically valid but incomplete) changes to 4 files -- per the established "a killed dispatch may still write" lesson, these were reviewed rather than discarded. Deep independent review caught two real bugs before commit: a circular import (`runtime.py` -> `api.py` -> `arbiter_review.py` -> `direct_ask.py` -> `runtime.py`, fixed by making `direct_ask.py`'s `create_runtime` import local) and an `AttributeError` in two `test_bootstrap.py` tests whose minimal `DummyPaths` double doesn't implement `workspace_root` (fixed by deriving it from `database_path.parent.parent` when the attribute is absent, rather than requiring every `RuntimeContext.paths` caller to carry it). The terminal also wrote the missing deliverable -- a `test_legacy_arbiter_review_translates_and_executes` integration test exercising the full fire path with a real dissenting round and an injected fake executor -- since it hadn't landed before the kill. Verified: full suite 1151 passed (only the known pre-existing manifest-snapshot failure), 0 new pyright errors, plus two manual CLI smoke tests (missing round -> clean `RecordNotFoundError`/exit 2; disabled config -> `{"fired": false, "reason": "arbiter_disabled"}`/exit 0) confirming no real peer dispatch fires outside the one real production code path. Tier 1, Tier 2, and all 7 of Tier 3's items are now fully closed. Next: `model-status`/`lesson-inject` (each need one small prerequisite decision first, see the Tier 4 scouting section), or the rest of Tier 4's ~53 remaining actions (now known to be fully behavior-specced via the Parity Ledger discovery, not undesigned -- see below).

**Update (2026-08-30, lesson-inject wiring round): 45 of the 90 `LEGACY_CATALOG` actions backed (up from 44).** `lesson-inject` now translates and executes end-to-end for the first time (verified: real translate-and-execute integration test passes). The logic is wired through `ApplicationAPI` to call the `inject_lessons()` coordinator utilizing `list_active_lessons()` and resolving context boundaries like OS, Shell, and task types natively.

**Update (2026-08-30, alert-raise implementation round): 46 of the 90 `LEGACY_CATALOG` actions backed (up from 45).** `alert-raise` is DONE and now translates and executes end-to-end for the first time. `AlertRaiseCoordinator` owns the CAS-retried `room-alert:{room_id}` current slot, snapshots live room-participation sessions on every retry, and delivers priority-`CRITICAL` `ALERT` inbox messages to every distinct live identity other than the exact raiser. The additive `RoomsService.send_message(priority=None)` field preserves all existing callers. SQLite-backed tests cover the full state shape, P0/P1 normalization and rejection, empty messages, singleton overwrite, deduplicated fan-out, a real intervening-write CAS retry with a refreshed winning session snapshot, CLI execution, and legacy translation through the real command bus.

**Update (same day, Tier 2 round): 28 of the 90 now backed -- Tier 2 is fully closed.** `thread-react`, `checkpoint`/`append-handoff` (the handoff-projection generator), and `context-fill` (reusing the checkpoint aggregation via a shared `_build_continuity_projection()` helper, read-only, no event recorded) are all done. `room.session_bindings` is also done -- an on-demand rebuild (`peerhub room rebuild-session-bindings`), not a legacy-catalog action so it doesn't move the count, but closes out that Tier 2 item. **All 4 of Tier 2's items are now closed.** Next up: Tier 3 (needs its own dialectical round per item) or Tier 4 (entirely undesigned domains).

**Prior update (same day, Tier 1 backlog round): 24 of the 90 now backed** (up from 22) -- `terminal-close --close-session` completed (duty-close and session-close now reported as two independent outcomes, verified end-to-end manually including the partial-failure path: duty released even when session-close fails), `terminal-duty-sweep` implemented (new `DutyLeaseCoordinator.sweep_expired_leases()` + a new cross-room expired-lease query), and `thread-append` wired to the already-existing `RoomsService.append_message()`. `lesson-broadcast` was investigated and deliberately left unbacked -- real legacy behavior broadcasts messages to every other room member, while `LessonService.record_delivery_pending()` only marks one peer's delivery as pending; a regression test (`test_legacy_lesson_broadcast_stays_unbacked_without_broadcast_semantics`) documents this is intentional, not an oversight. Tier 1 is now fully closed except that one confirmed mismatch, which moves to Tier 3 (needs its own design round for a real per-peer-vs-broadcast decision) rather than staying in Tier 1.

Original state before this round: 24 of the 90 `LEGACY_CATALOG` actions translate and execute end-to-end, across 6 real domains with full CLI + native service + legacy-translation + execution-dispatcher coverage: consensus, task, lesson, room (partial), duty-lease, room-participation-session. Full detail: `docs/design/HUB-REPLACEMENT-TDD-PROGRESS-2026-08-27.md`. This doc inventories everything still unbacked plus the older, pre-TDD-phase backlog items from the README that never got folded into the TDD tracking.

## MAJOR FINDING (2026-08-28): Tier 4 is NOT "entirely undesigned" -- a complete legacy behavior spec for all 90 actions already exists

While researching Tier 3's `thread-promote`, cx cited `docs/design/PHASE1-PARITY-LEDGER-BATCH1..5-2026-08-20.md` -- five docs, 18 entries each, **90 total, one per `LEGACY_CATALOG` action** (verified: `grep -c "^## [0-9]" docs/design/PHASE1-PARITY-LEDGER-BATCH*.md` gives exactly 18×5). Each entry is an exhaustive, line-number-precise specification of the REAL legacy `hub.py` behavior for that action: input schema, exact success/error output strings, before/after state changes with real file paths and JSON shapes, idempotency semantics, concurrency/crash behavior, and test fixtures. Spot-checked one entry (`thread-promote`, Batch 5 §7) directly against the real doc -- it precisely matches what cx cited, down to legacy source line numbers.

**This changes the whole backlog's risk/effort assessment.** What this doc's Tier 4 called "entirely undesigned domains" is actually "the legacy BEHAVIOR is completely and precisely specified; what's missing is the mapping from that spec into peerhub's native TargetState/service architecture" -- the same kind of work already done successfully for every domain built this session, not blank-slate design work. The `HUB-REPLACEMENT-GAP3-SESSION-CONTINUITY` doc's own narrative descriptions (used for `send`/`mark-read`/`thread-promote`'s command-table entries) turn out to have been written WITHOUT consulting this parity ledger, and at least one of them (`thread-promote`) was factually wrong as a result -- see the ratification below.

**Action for whoever picks up any Tier 3 or Tier 4 item next: read the matching Parity Ledger entry FIRST**, before assuming a design round is needed from scratch. Many "undesigned" Tier 4 items may turn out to need only the same "map real legacy behavior to a TargetState/service shape" exercise already proven this session, not a genuine open design question. This doesn't mean zero design work remains (peerhub's native shape for a given legacy behavior is still a real choice, as `send`'s ratification below shows), but the *behavioral* ambiguity that made Tier 4 look intimidating is mostly illusory -- it was never actually undocumented, just unread.

## Tier 3 RATIFIED (2026-08-28): all 7 items resolved via one cc/cx research-then-critique round

cx researched all 7 against real Parity Ledger entries + real service code (not speculation); the terminal independently verified the single most surprising claim (`thread-promote`) directly against the source doc and confirmed it precisely. No disagreement found this round -- cx's research was thorough enough that the terminal's role was verification, not correction.

1. **`send`** — RATIFIED: a new `inbox-message:<id>` `TargetState` family (NOT a reuse of `RoomsService.append_message()`, which would force every private notification into public thread history -- exactly what gap-3's "transient... doesn't duplicate substantive thread content" language was trying to avoid). Real legacy behavior (Parity Ledger Batch 1 §3-5) uses `mailbox.json` + a per-recipient maildir -- durable, not literally ephemeral, "transient" means "not substantive thread content," not "non-persisted." Shape: `sender`, `recipient` (composite instance+profile identity), `body`, optional `thread_ref`/`resource_ref`, `correlation_id`. **Real open flag, ratified as a requirement, not deferred**: the generic broker has no per-target read authorization -- whoever implements `message.check` (item 5 below) MUST add an explicit sender-or-recipient check at the command-descriptor/handler level (the broker itself won't do it), or this "private" message isn't actually private.
2. **`mark-read`** — RATIFIED: cursor identity `inbox-cursor:<room_id>:<recipient_instance_id>:<recipient_profile_id>`, tracking a per-recipient monotonic mailbox sequence (needs its own allocator, since UUIDs/timestamps aren't reliable order) -- NOT thread-message read state, confirmed via the real native name `inbox.cursor.advance` in the gap-3 doc's own command table. **Ratified scope decision**: v1 implements a pure high-water-mark cursor ("everything through sequence N is read"), NOT legacy's exact per-message out-of-order mark-as-read capability -- a conscious, documented simplification (matching the "superset-compat, never silently narrow" principle used elsewhere this session would actually argue AGAINST this simplification if taken literally, but per the "don't design speculatively" principle also used this session, add sparse per-message acknowledgements only if real evidence shows out-of-order reads are actually needed, not up front).
3. **`thread-promote`** — RATIFIED, and this is a real correction to the earlier gap-3 doc's own text: legacy `thread-promote` does NOT "attach a thread to a request/proposal/consensus round" (that was the gap-3 doc's own narrative guess, written without the parity ledger). Real legacy behavior (Parity Ledger Batch 5 §7, independently verified against the source doc): promotes ONE MAILBOX MESSAGE (`msg_id`) into a target thread as a `MSG_PROMOTED` entry, marking the source message `promoted_to`. **Split into two, ratified separately**: (a) legacy `thread-promote` belongs with the `send`/mailbox build (items 1/2/5) -- a promoted message becomes a new thread message whose metadata carries `promoted_from_inbox_message_id`; (b) the gap-3 doc's ORIGINAL "attach thread to governance object" idea is a genuinely different, NEW concept with no legacy equivalent at all -- explicitly moved OUT of Tier 3 entirely (not even ratified as Tier 4-ready) because it has a real prerequisite gap: `ConsensusService.propose()` currently creates rounds with `scope: None`, so same-room validation for a thread-to-round link isn't even possible yet. `promoted_from_thread_id` (the field already in the early gap-3 schema draft, absent from the final concrete schema and from the real `create_thread()`) is reserved for thread-to-thread lineage only, per the evidence -- not either of the above concepts.
4. **`update-status`** — RATIFIED: moved to Tier 4. Confirmed via direct repo grep: no "mission" concept exists anywhere in real peerhub code. `room-goal` is NOT a sufficient hook (no `blocked`/`phase` modeling, no lifecycle/authority semantics). Real legacy behavior (Parity Ledger Batch 1 §8) atomically updates `mission`/`blocked`/`phase` on `state.json` -- fully spec'd behaviorally, but genuinely needs a room-schema extension (new fields + their own CAS/authority rules) before it's a quick wiring task, unlike most of this batch.
5. **`check`** — RATIFIED: the read side of `send`'s mailbox (Parity Ledger Batch 1 §9: reads/formats unread mailbox messages, no mutation, no implicit cursor advance, doesn't touch threads). Build together with items 1/2/3(a).
6. **`arbiter-review`** — RATIFIED: stays Tier 3-adjacent but is explicitly NOT a quick wiring task like the rest of this batch. Confirmed neither `ConsensusService.resolve()` nor `request_escalation()` invoke a real arbiter or record an opinion -- real legacy behavior (Parity Ledger Batch 5 §16) evaluates trigger/dissent/budget rules, **invokes the configured arbiter in an isolated subprocess**, and records a final opinion. That subprocess-invocation capability doesn't exist anywhere in peerhub yet (it's a different kind of infrastructure than anything built this session, closer in kind to the `ProcessSupervisor`/peer-dispatch machinery than to a simple CAS-governed service method). Mechanics are drafted (immutable `arbiter-review:<round_id>:<review_id>` request + `arbiter-opinion:<round_id>:<review_id>` result + a new authority-validating consensus transition, consistent with `ARCHITECTURE.md`'s already-ratified "consensus produces intent, application dispatches, consensus records the opinion" split) but this item needs its own dedicated implementation round, not a same-batch wiring pass with the mailbox items.
7. **`lesson-broadcast`** — RATIFIED and now DONE (35 of the 90 `LEGACY_CATALOG` actions backed, up from 34). `RoomsService.list_participants()` added; new `peerhub/application/lesson_broadcast.py` orchestrates: validates the lesson is ACTIVE, excludes the sender from the room's real participant list, creates a `lesson-broadcast:<id>` campaign record, delivers a real mailbox message via `send_message()` to each recipient AND records `record_delivery_pending()` for each -- IMMEDIATE per-recipient delivery, matching real legacy behavior, per this session's "never silently narrow legacy behavior" principle. `LessonService` confirmed to have zero new dependency on room storage (`peerhub/governance/lessons.py`'s imports checked directly -- no `rooms` import). **Resolved a real tension**: this session's earlier lesson-domain ratification (item 37, "pack invalidation + next-dispatch eligibility is the baseline, immediate notification is only an optimization") describes a DIFFERENT mechanism -- how lessons passively surface in an already-active session's context -- not what the explicit `lesson-broadcast` COMMAND does; the two coexist rather than one redefining the other. Independently re-verified by the terminal through the real CLI: broadcasting excludes a sender whose identity matches a real room participant (delivers to the other 2, not 3), and broadcasting an unactivated (`PROPOSED`) lesson is correctly rejected (exit 2, clear message) rather than silently succeeding.

**Net effect on the backlog, updated as items closed**: items 1, 2, 3(a), 5, and 7 are all DONE (`send`/`check`/`mark-read`/promote-from-mailbox/`lesson-broadcast` -- see the update notes below each). Item 4 (`update-status`) moved to Tier 4 (needs a room-schema extension first). Item 6 (`arbiter-review`) is RATIFIED below (2026-08-28) -- overturns the earlier "new subprocess infrastructure" assessment.

## `arbiter-review` RATIFIED (2026-08-28): reuses existing dispatch machinery, one normal-sized round, not a new-infrastructure project

Real legacy behavior (`docs/design/PHASE1-PARITY-LEDGER-BATCH5-2026-08-20.md` §16): requires an existing consensus round; checks arbiter config (disabled -> `{"fired": false, "reason": "arbiter_disabled"}`, no side effects); detects dissent; checks a rolling 5-hour budget window (max 5/5h, exceeded -> `{"fired": false, "reason": "budget_exceeded"}`, no side effects); if it fires, invokes the configured arbiter model, appends a `FINAL_OPINION`, records the budget consumption. Not idempotent when it fires.

**Overturned assessment**: the earlier ratification concluded this needs genuinely new subprocess/`ProcessSupervisor`-class infrastructure. Direct verification found legacy's "isolated subprocess" is conceptually `hub.py ask --to <arbiter>` -- i.e. exactly what peerhub's own already-built, already-proven `execute_direct_ask()` (`peerhub/application/direct_ask.py`) does: resolves a peer/profile, live readiness probe, capability/routing admission, real adapter/`ProcessSupervisor` dispatch, verified response, synchronous. `AuthenticatedSubject` is plain immutable principal/evidence data, not inherently interactive -- `ApplicationAPI` already constructs one from a `RequestContext` for other dispatch admission. **No new process-invocation infrastructure is needed.**

**Real corrections found during verification** (ratified, cite these instead of the parity ledger's shorthand where they conflict):
- Legacy's "rolling" budget is an ANCHORED 5-hour window (`{window_start, count}`, resets 5h after the FIRST counted invocation in the window), not a continuously sliding list of the last 5 timestamps.
- Real legacy source returns `budget_exhausted`; the parity ledger doc (and this ratification's own compat contract) says `budget_exceeded`. Ratified: implement `budget_exceeded` (the already-published compat value), pin it in a test, note the discrepancy rather than silently picking one.
- Dissent includes BOTH explicit non-agree votes AND required voters with no vote at all (`no_vote`) -- not just explicit disagree.
- **Scope boundary, ratified as non-negotiable**: manual `arbiter-review` records a `FINAL_OPINION` only -- it does NOT itself apply an override to the consensus outcome. Override application is a separate, NOT-yet-built auto-finalization path. Do not smuggle automatic consensus-outcome mutation into this round.
- Real legacy triggers on `dissent`/`r10_final`; peerhub's current consensus round schema has no `r10_final` marker -- do not fabricate one, `dissent` alone is real and sufficient for this round.

**One real remaining prerequisite decision, ratified**: legacy's configured arbiter is `cc.fable` (`--model claude-fable-5 --effort high`), which is NOT currently a dispatchable peerhub profile (the Claude adapter only accepts `cc.standard`). Per this session's own memory record (`reference_cc_fable_profile_broken_2026_08_19` -- `cc.fable` is a KNOWN-BROKEN profile in this real environment since ~2026-07-17, fails every real dispatch and cascades cc health to RED, independent of whether peerhub configures it), configuring `cc.fable` as peerhub's arbiter would fail in practice even if wired. **Ratified: use a real, currently-working high-capability profile as the default configured arbiter instead** (`cc.deepthink` or `cc.effort` -- both proven working throughout this session's own dispatches), documented as a deliberate substitution for a literally-broken legacy default, not a silent behavior change -- matches this session's established "superset-compat, real capability over a broken literal match" precedent.

**Update (2026-08-28, core round): DONE (service layer only, steps 1-5 of the 9-step plan).** `peerhub/application/arbiter_review.py` -- `FinalArbiterPolicy` + `.peerhub/arbiter.json` loading, `classify_consensus_dissent()` (explicit non-agree + missing required votes), `build_condensed_arbiter_prompt()` (deterministic, 1200-char capped), strict `parse_arbiter_verdict()` (first non-empty line, exact `VERDICT: APPROVE|REJECT`, case-insensitive), `ArbiterBudgetManager` (CAS-retried anchored-window reservation with `RESERVED`/`CONSUMED`/`RELEASED`/`UNKNOWN` slot states), and `ArbiterReviewCoordinator` sequencing reserve -> immutable request -> `execute_direct_ask()` -> immutable opinion -> budget consumption -> `ConsensusService.record_arbiter_opinion()`. `record_arbiter_opinion()` independently RE-parses and RE-validates the verdict (doesn't just trust the caller), and only ever sets an `arbiter_opinion` reference field -- never touches `resolution`/`status`/`phase`, confirmed by direct code read. Careful failure-path handling: `NOT_STARTED` releases the budget slot, `STARTED`/`TERMINAL` consumes it, genuinely uncertain outcomes mark it `UNKNOWN` pending window rollover (matches the ratified liveness-bounded-to-5h design). Deliberately used `cc.deepthink` as the default arbiter profile, not `cc.fable`, per the ratified substitution decision.

**Update (2026-08-28, wiring round): DONE (steps 6-8 of the 9-step plan) -- `arbiter-review` is now fully end-to-end, 37/90.** Full detail in the "Where things stand right now" section at the top of this doc.

Independently re-verified by the terminal via direct code review (this round's tests were the terminal's only real execution, since cx's sandbox couldn't run pytest at all -- 0 in-sandbox test executions from cx this round): 9 real integration tests, including exact-state-equality proof that a rejected 6th budget reservation is byte-for-byte side-effect-free, precise anchored-window-boundary testing (18999s stays in window, 19000s triggers reset -- off-by-one precision), and a full successful-review scenario proving the round's `resolution` field stays byte-identical while `arbiter_opinion` is added as a reference, budget is consumed, and `READ_ONLY` capability tier is used for the dispatch. pytest -q: 1150 passed (only the known pre-existing failure). pyright: 0 new errors.

CLI + legacy translation + execution-dispatcher wiring (the 9-step plan's steps 6-8) remains an explicit follow-up round -- doesn't move the `LEGACY_CATALOG` count yet.

**Design reference** (full detail, TargetState shapes, budget-reservation algorithm, and the 9-step implementation breakdown with a full test list were drafted by cx and proved sound during implementation): `ArbiterReviewCoordinator` (new, `peerhub/application/arbiter_review.py`, NOT a method on `ConsensusService` itself -- matches `ARCHITECTURE.md`'s already-ratified "consensus produces intent, application invokes the peer, consensus records the opinion" split) sequences: budget reservation (CAS-backed `arbiter-budget:workspace` target) -> immutable `arbiter-review:<round_id>:<review_id>` request -> `execute_direct_ask()` call (READ_ONLY capability tier) -> immutable `arbiter-opinion:<round_id>:<review_id>` result (strict first-line `VERDICT: APPROVE|REJECT` parsing) -> `ConsensusService.record_arbiter_opinion()` CAS-updates the round with an opinion reference (first valid canonical opinion wins, never overwrites the base resolution). A `FinalArbiterPolicy` config (enabled, candidate peer/profile, triggers, max_invocations, window_seconds) loaded from `.peerhub/arbiter.json`, absent-means-disabled. Automatic post-finalization triggering is explicitly OUT of scope for this round (needs a trusted worker identity this session hasn't built) -- only the explicit `arbiter-review` legacy command.

**Update (2026-08-28, mailbox wiring round): CLI + legacy + dispatcher wiring DONE too -- 34 of the 90 now backed** (up from 30). `send`/`check`/`mark-read`/`thread-promote` all now translate and execute end-to-end (`peerhub room send|check-inbox|mark-read|promote-message`, `LegacyTranslator`, `ApplicationAPI`). `coordination.message.check`'s read-only descriptor wraps its `Sequence[TargetState]` result as `{"messages": [...]}`, reusing the exact fix pattern already established for `lessons-list` (never return a bare list from an `encode_result_fn`). Independently re-verified by the terminal through the FULL CLI stack (not just cx's own in-memory smoke): sent 2 private messages to different recipients in the same room, confirmed each recipient's `check-inbox` shows only their own mail and the sender's own inbox is empty. pytest -q: 1136 passed (only the known pre-existing failure). pyright: 0 new errors.

**Update (2026-08-28, mailbox core round): DONE (service layer only).** `RoomsService` (peerhub/governance/rooms.py) gained `send_message()`, `check_inbox()`, `mark_read()`, `promote_message()` -- immutable `inbox-message:<id>` targets, CAS-updated `inbox-cursor:<room>:<recipient>` projections, sequence allocation mirroring the existing thread-message pattern. The non-negotiable privacy requirement is enforced by construction: `check_inbox` only ever queries the CALLER's own inbox (scoped by `recipient` match), never another identity's. Independently re-verified by the terminal with a fresh, separately-written smoke script (not just re-running cx's own tests) proving 3-way privacy isolation (2 recipients each see only their own message, a 3rd bystander sees nothing) -- this was extra-important this round since cx's sandbox couldn't run pytest OR a real SQLite smoke test at all, only an in-memory check. `mark_read` is idempotent (repeat calls to an already-passed sequence leave the cursor's stored state unchanged). `promote_message` creates a real `MSG_PROMOTED` thread message carrying `promoted_from_inbox_message_id` and marks the source inbox-message's `promoted_to` field via CAS (revision bumps, body/sender/recipient stay immutable). CLI + legacy-translation + execution-dispatcher wiring is an explicit follow-up round (matching how every other domain this session was built: core first, wiring later) -- doesn't move the `LEGACY_CATALOG` backed-action count yet.

## Tier 1 — Ready now (real backing method already exists, zero new design)

These need only the same mechanical wiring pattern used repeatedly this session (`LegacyTranslator` branch + `CommandDescriptor` registration + CLI subcommand + tests) — no new service code, no dialectical round.

| Legacy action | Native method | Status |
|---|---|---|
| ~~`terminal-close` (`--close-session` half)~~ | `coordination.terminal.close` (extended) | **DONE.** `RoomParticipationCoordinator.end_session()` wired in; duty-close and session-close report as two independent outcomes (`{"duty_close": {...}, "session_close": {...}}`); verified end-to-end manually, including the partial-failure path (duty released, session-close failure reported separately, CLI exit code 2). |
| ~~`terminal-duty-sweep`~~ | `coordination.terminal.duty_sweep` | **DONE.** New `DutyLeaseCoordinator.sweep_expired_leases(role, ...)` + a new cross-room `list_expired_duty_leases` query, iterating the existing per-lease `expire_and_recover_lease()`. Real test proves selectivity (only the expired lease transitions, the active one is untouched). Does not touch room sessions or replay work, as scoped. |
| ~~`thread-append`~~ | `coordination.thread.append` | **DONE.** Wired to the pre-existing `RoomsService.append_message()`. |
| ~~`lesson-broadcast`~~ | `coordination.lesson.broadcast` | **DONE** (2026-08-28, see Tier 3 ratification item 7 below for the full build) -- the cardinality mismatch flagged here originally is resolved: a new cross-domain orchestration module delivers to every room participant, not a single peer. |

Also worth a **quick verification pass** (not full implementation) before assuming they're Tier 1:
- `thread-new` (`coordination.thread.create`) vs. the already-backed `new-topic` (`coordination.topic.create`) — both may resolve to the same `RoomsService.create_thread()` call; confirm they're genuinely distinct legacy semantics before wiring, or wire `thread-new` as a straight alias.
- `broadcast` (`coordination.message.broadcast`) vs. the existing `BroadcastCoordinator`/`peerhub broadcast` — confirm these are actually the same concept (a room/thread message fan-out) before assuming `BroadcastCoordinator.fan_out()` backs it; the naming overlap could be coincidental (peer-dispatch fan-out vs. room-message fan-out are different things).

## Tier 2 — Ratified design, needs new (but scoped) component code

From the gap-3 dialectical round (`docs/design/HUB-REPLACEMENT-GAP3-SESSION-CONTINUITY-2026-08-24.md`, "RATIFIED (2026-08-27)" sections) — the *design* questions are closed, but the components themselves aren't built.

| Item | What's ratified | What's still missing |
|---|---|---|
| ~~`thread-react`~~ | Immutable append-only reaction events + a maintained "current reaction" projection (not a mutable CAS status flip) | **DONE.** `RoomsService.react()`/`unreact()` write immutable `reaction-event:<id>` targets plus a CAS-updated `reaction-state:<message>:<actor>:<type>` projection, following the room/thread/message `TargetState` family exactly. Reachable through `peerhub room react\|unreact`, `LegacyTranslator` (`thread-react` with an `action: ADD\|REMOVE` field), and `ApplicationAPI`. One real bug caught by the terminal's manual end-to-end smoke test (not unit tests): the first round wired ADD everywhere but left `unreact()` completely unreachable from any external interface (no CLI subcommand, no `action` field on the wire command, the API handler hardcoded `s.react(...)`) -- fixed in a follow-up round, independently re-verified with a fresh manual CLI round-trip (ACTIVE → REMOVED → ACTIVE). |
| ~~`context-fill`~~ | One JSON envelope (`room_id`, `session_id`, `as_of_event_seq`, `sections`, truncation metadata); list-shaped sections carry `{"items":[...],"truncated":bool}` | **DONE.** Reuses `checkpoint()`'s aggregation via a shared, refactored-out `_build_continuity_projection()` helper -- read-only, deterministic, no event recorded (unlike `checkpoint()`, which does record one each call). `session_id` is echoed as context metadata, deliberately NOT validated against `RoomParticipationCoordinator` (keeps the two independent, same principle as `room.session_bindings`). Supports `--sections` filtering to a subset of the 6 real section names (exact uppercase, order preserved), defaults to all 6. Manually verified end-to-end (full + filtered output correct, checkpoint-event count unaffected by repeated context-fill calls, only incremented by a real `checkpoint()` call). |
| ~~`checkpoint` + `append-handoff`~~ | The 6 legacy handoff sections (`GOAL`, `RECENT_COMPLETED`, `PENDING_ISSUES`, `KEY_DECISIONS`, `CONSENSUS_HISTORY`, `ACTIVE_THREADS`) with real ported legacy limits (12000 chars total / 5 / 3 / 3 / 10 / 5 items); required as a generated (never authoritative), deterministic, re-generatable projection | **DONE.** `RoomsService` gained immutable `continuity-note:<id>` events (per-room continuity sequence for deterministic ordering, since timestamps alone could tie), a separate CAS-updated `room-goal:<room_id>` projection for the scalar GOAL section, and `checkpoint()` which aggregates notes into the 6 sections, applies the real caps, and applies the 12000-char budget (trims `RECENT_COMPLETED` first). Exports both JSON (with per-section `truncated`/`source_count` metadata) and Markdown. Checkpoint generation itself is a recorded, idempotency-key-safe event -- retrying the same command returns the same checkpoint rather than duplicating it. Manually verified end-to-end (6 rapid appends to a 5-item-capped section correctly retained the last 5 in order, `truncated: true` reported correctly, Markdown rendered with real section headers). **One small scope gap, not a bug**: no `peerhub room set-goal` CLI command was requested or built this round, so the `GOAL` section has no CLI path to populate it yet (it correctly renders empty rather than erroring) -- worth a small follow-up if GOAL content becomes a real need. |
| ~~`room.session_bindings` wiring~~ | Ratified as an explicit rebuildable projection on the room `TargetState`, not authoritative | **DONE.** Implemented as an ON-DEMAND rebuild (not a synchronous push from session open/heartbeat/close, to keep `RoomParticipationCoordinator` and `RoomsService` independent as ratified): `governance.activity.rebuild_room_session_bindings(broker, room_id, active_sessions)` reads the current room, replaces only `session_bindings`, preserves every other field via CAS. `RoomParticipationCoordinator.list_active_sessions(room_id)` supplies the sessions without either coordinator depending on the other. Exposed as `peerhub room rebuild-session-bindings --room-id ROOM_ID`. Manually verified end-to-end (2 sessions -> both bound; close 1 -> rebuild -> only the survivor remains; every other room field, title/participants/thread_ids/retention/etc., untouched). |

## Tier 3 — Needs its own dialectical round before implementation

Real open design questions, not yet debated. Do not implement before ratifying, per this session's standing architecture-before-implementation rule.

- `send` (`coordination.message.send`) — "directed transient message, optional thread/resource pointer, doesn't duplicate substantive thread content" per the gap-3 doc's own table, but never actually debated: what makes it "transient" vs. a thread message, does it persist at all, who can address whom.
- `mark-read` (`coordination.message.mark_read`) — described as "idempotent read-cursor advance," looks simple, but the underlying read-cursor data model doesn't exist yet (per-participant, per-thread position tracking) — confirm it's actually simple before assuming so.
- `thread-promote` (`coordination.thread.promote`) — "attach thread to a request/proposal/consensus round; no implicit vote/finalization" is a direction, not a mechanism; what the actual link field/target-type validation looks like was never nailed down the way reactions/context-fill were.
- `update-status` (`coordination.mission.update`) — not in the gap-3 doc's command table at all; likely belongs to a different, entirely undesigned "mission/status" concept.
- `check` (`coordination.message.check`) — undiscussed.
- `arbiter-review` (`consensus.arbiter.review`) — flagged during the `approval-request`/`consensus-sweep`/`lessons-list` batch as having no clean 1:1 existing method on `ConsensusService`; DIR-005's arbiter-authority mapping is policy-ratified (ratification item 41) but the actual command mechanics aren't.
- ~~`lesson-broadcast`~~ — DONE, see Tier 3 ratification item 7.

## Tier 4 — Entirely new domains, no design work started

~50 of the 66 remaining actions cluster into domains that were part of `hub.py`'s original 7 functional categories but never got their own gap-N design doc this session (unlike consensus/session-room-thread/duty-lease/task-approval/governance-learning, which each did). Roughly:

- **Health / admission / routing** (~13 actions): `health-update`, `health-check`, `peer-status`, `peer-quarantine`, `peer-recover`, `health-precheck`, `health-sweep`, `freshness-sweep`, `elect-leader`, `discover`, `check-gate`, `lease-status`, `lease-sweep`. This is `hub.py`'s "health/leadership/duty-lease" category minus the duty-lease part (which IS built). No `TargetState` schema, no service, no design doc.
- **Governance artifacts / feedback / proposals / locks** (~14 actions): `append-log`, `archive-file`, `artifact-claim/status/finalize`, `proposal-add/vote/list`, `broker-submit/drain/status`, `file-lock/unlock`, `lock-status`. Part of `hub.py`'s "governance/learning" category beyond lessons (which IS built).

### Governance artifacts / feedback scouting (2026-08-29): 6 previously-unscrutinized actions, 4 real gaps + 2 out of scope, 0 quick wins

Research round (cx.deepthink), the remaining 6 members of this cluster not already covered by the 11 rejections in "Rejected near-matches" above (`artifact-claim/status/finalize`, `proposal-add/vote`, `broker-submit/drain/status` -- those stand, settled). Two citations independently spot-checked by the terminal directly against source (`ArtifactMaterializer`'s "never persist absolute paths, source paths, or raw bytes" safety invariant, `OperationalProjectionSnapshot`'s "deliberately has no availability, admission, quarantine, or completion-assessment field" docstring) -- both accurate.

**OUT OF SCOPE (2):** `append-log` and `archive-file` -- both are generic host/tooling-diagnostic operations (an arbitrary `axis/script/status/detail` log append; a raw arbitrary-file-copy to `_archive/`), not peer-governance state. No native peerhub concept fits either without inventing one merely to preserve the legacy action name -- `TelemetryProjector` only derives typed observations from canonical dispatch terminal events; `GovernanceBroker.submit()` is CAS mutation of one identified target, not a generic logging sink; `ArtifactMaterializer` owns a `DECLARED -> STAGED -> VERIFIED` dispatch-attempt lifecycle and explicitly forbids persisting raw file bytes or paths, the opposite of what `archive-file` needs. Same pattern as `freshness-sweep` in the health/routing cluster -- these belong in `_sys`-side host tooling, not peerhub's domain model.

**REAL GAP, needs its own round:** `report-error` -- the append-and-count half is a clean, small per-`(peer,pattern)` CAS series, but legacy's auto-quarantine-at-threshold half cannot honestly call `HealthService.classify_and_open_circuit()`: that method requires typed `HealthStageObservation` evidence plus an `EvidenceSubject`/`PolicyReceipt` pair, and forcing a free-form error string into that shape would repeat the exact authority-model mistake that already got `peer-quarantine` rejected as a false friend. Deferred -- needs a real health-authority-bridge design decision (a pattern-to-evidence mapping, or a separate quarantine-review target consumed by an authority-bearing coordinator), not implementable as stated.

### `report-error` RATIFIED (2026-08-29): two-round design, an immutable review-request bridge, not a forced circuit-open

Research (ag.deepthink) then critique (cx.deepthink). ag correctly identified the core shape -- when the per-`(peer,pattern)` `operational-error-series` counter crosses its threshold, create an immutable `quarantine-review` request record (mirroring `arbiter-review`'s "create a request, let a real authority-bearing process render the verdict" pattern) rather than directly forcing a circuit-open the way the already-rejected `peer-quarantine` false friend does. Placement: `peerhub/governance/operational_errors.py` (self-contained, doesn't cross the health-service or adapter-registry boundary, matching `feedback.py`'s placement reasoning, not `peer_registry.py`/`role_assignment.py`'s).

cx's critique found and fixed four real problems, three citations independently re-verified by the terminal against source (`peer-quarantine`'s real "not idempotent, refreshes timestamps on every call" behavior, `ArbiterBudgetManager`'s real 16-attempt retry loop, the shared `_receipt()` encoder's real shape) -- all accurate:

1. **Two confirmed schema-field mismatches, fixed.** ag's draft used `"scope": "workspace"` and a `"schema": "peerhub.x.v1"` string field on both new targets -- every other domain shipped this session (`peer_registry.py`, `role_assignment.py`, `feedback.py`) uses `"scope": None` and a bare `"schema_version": 1` integer, with no exceptions. Corrected to match.
2. **The 16-attempt CAS retry loop, kept -- but as an explicitly justified exception, not a default.** `operational-error-series` is a genuine shared-counter aggregate multiple callers can legitimately race to append to (like `arbiter-budget:workspace`), unlike `role_assignment`/`feedback`/`peer_registry`'s per-entity domains, which deliberately do NOT retry because retrying could silently act on stale ownership/assignment facts. Losing a real error report to an unretried conflict here would violate parity with legacy's file-lock serialization, which never drops a report.
3. **Legacy's real "every call where count >= threshold" repeat-trigger, preserved.** ag's draft created the review-request only once, at the exact `count == threshold` transition -- a real narrowing (legacy's `peer-quarantine` equivalent refreshes on every call, per the ledger). Corrected: one new immutable `quarantine-review` request per report whose resulting count is `>= threshold`, deterministically keyed by `{peer_key}:{pattern_hash}:{threshold}:{count}` so each is a distinct, frozen record rather than an overwritten one.
4. **Return type corrected to `MutationSubmission`**, matching every other domain -- the count, revision, and whether a review fired are all derivable from the series' own submission receipt, so no richer ad-hoc mapping return type is needed.
5. **One new bug ag's design didn't catch, found by cx:** creating the review-request *before* the series CAS wins could leave a durable review record referencing a report that never actually committed (if the series CAS then fails and exhausts retries). Corrected sequencing: the review-request is only created *after* the series CAS has already succeeded.

Ratified `OperationalErrorService(broker, *, clock, ids)` with `report_error(*, peer_key, pattern, severity, detail, actor_id, threshold=3) -> MutationSubmission`. `threshold` is pinned per-series on first creation; a later call supplying a different threshold for the same series is rejected rather than silently changing escalation policy mid-series. Full schemas and sequencing recorded in the session transcript; ready for an implementation round.

**Update (2026-08-29): DONE, exactly as ratified above.** `report-error` now translates and executes end-to-end (44/90). Zero bugs found on independent review -- verified directly, including a real 4-report sequence proving the corrected repeat-trigger behavior (a distinct review per threshold-or-above report, not one overwritten record).

**REAL GAP, tractable now:** `feedback-add`/`feedback-list`/`feedback-resolve` -- a genuine, currently-missing lightweight governance-journal domain: sequential `GAP-YYYYMMDD-NNN` IDs, `open -> done|dismissed` status lifecycle, optional owner. Neither `LessonService` nor `ConsensusService` is reusable (different ID/lifecycle vocabulary), but both are close structural templates. Candidate schema: one `feedback:{feedback_id}` target per item (source_peer, category, severity, title, detail, status, owner, created_at/by, resolved_at, updated_at) -- a `FeedbackService(broker, *, clock, ids)` with `add_feedback`/`get_feedback`/`list_feedback`/`resolve_feedback`, following the exact `_submit()` CAS pattern every other domain this session uses (fresh request_id reused as idempotency_key, `expected_revision=0` on create). Sequential ID allocation: scan existing `feedback:*` targets via `broker.list_targets("feedback", None)` for `max(NNN)+1`; a same-ID create collision is a CAS conflict resolved by retrying the next sequence number, no singleton needed.

Two small open questions, resolved here rather than deferred to another critique round given how low-risk and structurally-proven this domain is (unlike `report-error`'s real authority-bridge gap): (1) **UTC, not host-local time**, for the `YYYYMMDD` date component -- derived from the injected `Clock`, matching this codebase's consistent avoidance of host-timezone dependence for determinism and testability; not a real design question, just an obvious default given house convention. (2) **Do not restrict `status` to an enum** -- keep it validated non-empty free text, matching the `node_type` precedent in `peer_registry.py` (validated free text, not an invented enum vocabulary) and cc's own stated house rule when critiquing that field ("a specific enum here would be an invented vocabulary"); the parity ledger's own input schema accepts arbitrary `status: str` and only demonstrates `done`/`dismissed` in fixtures, so an enum would be an unproven narrowing.

Recommendation (cx's, endorsed): `FeedbackService` (add/list/resolve together) is the best next implementation in this cluster -- clean, lightweight, low-risk. `report-error` needs its own separate round once the health-authority-bridge question is resolved. `append-log`/`archive-file` should not be built at all.

**Update (2026-08-29): `FeedbackService` DONE, exactly as ratified above.** See the "Where things stand right now" section at the top of this doc. `feedback-add`/`feedback-list`/`feedback-resolve` now translate and execute end-to-end (43/90). `report-error` remains genuinely blocked on the health-authority-bridge question; `append-log`/`archive-file` remain correctly out of scope.
- **Lessons residual** (2 actions remaining, `lesson-broadcast` now DONE — see Tier 3 above): `lesson-sweep` and `lesson-inject` — `lesson-inject` now has a revised, mostly-ratified design doc (`docs/design/HUB-REPLACEMENT-GAP8-LESSON-INJECT-2026-08-30.md`) incorporating cx.deepthink's critique, which is now FULLY RATIFIED and ready for implementation (open sub-questions resolved). `lesson-sweep` still has no matching method at all.
- **Configuration / telemetry / peerhub-internal** (7 actions): `profile-validate`, `model-status`, `transient-scan`, `preflight`, `context-hash`, `status`, `update-signatures` — miscellaneous, likely each a small independent utility, not one coherent domain.

### Configuration/telemetry scouting (2026-08-29): 5 of 6 remaining (minus `model-status`, already scoped above) are host-level, `status` is real but multi-domain and partially blocked

Quick classification pass (terminal, reading each real parity-ledger entry directly rather than a full peer round, given how clearly each one lands): `preflight` (a regex command-classifier against `_sys`'s own blocked/mutating/read-only guard-matrix patterns), `context-hash` (a SHA-256 digest over specific `_sys` state files with CRLF normalization, for drift detection), `profile-validate` (cross-checks `_sys/ai/model_profiles.json`/`status_checks.json`/`nodes.json` for internal consistency), `transient-scan` (a filesystem hygiene scan for stray temp files matching name patterns), and `update-signatures` (introspects `hub.py`'s *own* `action_*` function signatures and writes a snapshot for `hub.py`'s own contract-drift test suite -- meaningless once `hub.py` is retired, arguably actively wrong to replicate) are all OUT OF SCOPE, the same pattern as `freshness-sweep`/`append-log`/`archive-file`: real `_sys`-side host/tooling diagnostics with no native peerhub-domain concept to back them, not peer-coordination governance state.

`status` is different: a genuine multi-domain aggregation read (state/mailbox/task-registry/locks/consensus rolled into one markdown report), and peerhub does have real native pieces that could back parts of it (`TelemetryPresenter.collect_live_snapshot()`, `TaskService`, `ConsensusService`, `DutyLeaseCoordinator.sweep_expired_leases()` for the legacy action's embedded implicit-lease-sweep side effect). Not scouted to a full verdict -- genuinely complex (a true cross-domain dashboard, not a single-service wiring item) and partially blocked on other still-undesigned domains (locks). Left as an open, real gap for a future round, not attempted this session.

#### `status` re-assessment after locks (2026-08-31): locks are ready, but full parity is still meaningfully blocked

Research-only re-assessment against the real handler and the shipped 49/90 PeerHub tree. **Verdict: do not start a ratified `status` design/implementation round yet.** Completing `file-lock`/`file-unlock`/`lock-status` removes one real dependency, but it does not remove the missing room-summary SSOT, unread-aggregate read, ambient-current-room decision, or legacy process-lease reconciliation dependency. A reduced native dashboard could be designed now, but it would omit several always-present legacy fields plus a real side effect, so it must not be counted as backing the catalogued `status` action (`peerhub/application/legacy.py:139`, `peerhub/application/legacy.py:1787-1792`).

**Exact legacy contract and output.** The parity ledger calls this a no-input, Markdown-formatted report and correctly records the otherwise-read-only action's implicit lease-sweep mutation (`docs/design/PHASE1-PARITY-LEDGER-BATCH1-2026-08-20.md:142-155`). The source is more precise. `action_status()` first calls `_lease_sweep()`, then reads `state.json` and the aggregate `mailbox.json.unread_count` (`P:/_sys/core/hub.py:1680-1684`). Stdout is, in this exact order:

```text
### [ROOM STATUS]
**Room ID**: <room_id or 없음>
**Leader**: <active_coordinator, else leader, else VACANT (공석)>
**Leader Reason**: <leadership.reason or 없음>          # only if leadership is truthy
**Members**: <state.members keys joined by ", ", or 없음>
**Roles**: <role=peer pairs joined by ", ">             # only if roles are nonempty
**Mission**: <mission or 없음>
**Blocked**: <blocked or 없음>
**Phase**: <phase or 없음>
**Updated**: <updated_at or 없음>
**Mailbox**: <unread_count> unread
**Tasks**: <ACTIVE count> active / <registry size> total  # only if registry is nonempty
**Locks**: <registry size> active                        # only if registry is nonempty

### [CONSENSUS — ACTIVE]                                 # only if at least one voting round
  - <round_id>: <subject or ?> [<non-null votes>/<voters>]

### [HANDOFF]                                            # only if current-room handoff.md exists
<the complete handoff.md text, verbatim>
```

Those conditionals and fallbacks come directly from `P:/_sys/core/hub.py:1685-1714`; consensus scans `consensus/*.json` in filename order, includes only `status == "voting"`, and uses the full snapshotted voter count (`P:/_sys/core/hub.py:1716-1725`, with the producer schema at `P:/_sys/core/hub.py:7741-7752`); handoff is an unbounded UTF-8 file read printed verbatim (`P:/_sys/core/hub.py:1727-1732`). Missing or malformed JSON is generally normalized to `{}` by `_read_json`, explaining the ledger's corrupted-registry recovery case (`P:/_sys/core/hub.py:644-659`). This is one ordered Markdown stream, not a JSON snapshot and not the existing PeerHub CLI's unrelated workspace/database/migrations/active-lease status screen (`peerhub/cli.py:2601-2648`).

**It is not a pure read.** `_lease_sweep()` marks expired process leases, quarantines invalid timestamps, and rewrites `leases.json` under its own lock (`P:/_sys/core/hub.py:10953-10990`). After releasing that lock it can kill an OS process tree, record `lease_expired` health/failure state, append ask history, and emit warnings/P2P output (`P:/_sys/core/hub.py:10991-11053`). `action_status()` does not print the explicit `lease-sweep complete` banner, but all of those mutations/external effects still occur before the report. The earlier scouting note's proposed `DutyLeaseCoordinator.sweep_expired_leases()` reuse is therefore **wrong**: that coordinator explicitly owns room/role duty leases (`peerhub/dispatch/duty_lease.py:1-2`, `peerhub/dispatch/duty_lease.py:98-105`) and its sweep is role-filtered recovery-receipt creation with no process identity or process reaper (`peerhub/dispatch/duty_lease.py:189-220`). PeerHub's closer process-bound substrate is `LeaseSnapshot`, whose fence carries an OS process birth identity (`peerhub/dispatch/contract.py:898-917`, `peerhub/dispatch/contract.py:983-1026`), but today it exposes only per-lease recovery supplied with already-observed liveness/identity evidence (`peerhub/dispatch/session_lease.py:278-313`). PeerHub also has an identity-verifying `TreeController.kill_tree()` OS primitive (`peerhub/dispatch/tree_controller.py:368-381`), but no coordinator enumerates expired persisted session leases, reconstructs/locates their managed tree, orders tree termination with lease recovery, and fans the result into native health/history. Full `status` parity is consequently blocked on the separately unbacked **process-lease `dispatch.lease.sweep` coordinator**, or on an explicit ratified parity waiver that makes native status pure read-only.

**Section-by-section native-source audit.** “Ready” below means the authoritative data exists; it does not mean the whole action is wireable.

| Legacy report datum | Real PeerHub source | Readiness / required adaptation |
|---|---|---|
| Room ID | `RoomsService.get_target(room_id)` can read an explicitly named room (`peerhub/governance/rooms.py:43-44`), and room records carry `room_id` (`peerhub/governance/rooms.py:82-101`). | **Blocked for no-input parity.** PeerHub permits multiple rooms and has no workspace-global “current room” selector. Other legacy translations resolve an implicit room from arguments/context/scope (`peerhub/application/legacy.py:65-88`), so a native API can sensibly require `room_id`, but behavior for a truly empty legacy call remains an unresolved compatibility decision. |
| Leader and leader reason | `LeadershipService.get_current_leader()` is a real workspace-global read (`peerhub/application/leadership.py:232-247`); the stored leader binding contains `peer_node_id` and the record contains `reason` (`peerhub/application/leadership.py:509-536`). | **Ready with a small adapter.** Render `leader.peer_node_id`, preserve `VACANT (공석)`, and suppress/emit reason according to the legacy conditional. No model/profile inventory read is involved. |
| Members | `RoomParticipationCoordinator.list_active_sessions(room_id)` filters persisted ACTIVE sessions by heartbeat freshness (`peerhub/dispatch/room_session.py:334-349`); each session exposes its owner instance/profile identity (`peerhub/dispatch/room_session.py:58-70`). | **Ready with composition.** Deduplicate live owner identities in stable session order. The immutable `room.participants` snapshot is not equivalent to legacy's live `state.members`, and the rebuildable `session_bindings` projection is explicitly secondary (`peerhub/governance/activity.py:57-87`). |
| Roles | `RoleAssignmentService.list_roles()` returns only current `ACTIVE` workspace assignments (`peerhub/application/role_assignment.py:307-321`), whose states include `role` and `peer_node_id` (`peerhub/application/role_assignment.py:214-234`). | **Ready with formatting adaptation** to ordered `role=peer_node_id` pairs. |
| Mission | Legacy has an independently mutable `state.mission`; `update-status` changes it without changing handoff (`P:/_sys/core/hub.py:1608-1617`). PeerHub has a `room-goal` continuity projection (`peerhub/governance/rooms.py:573-600`). | **Real gap.** `room-goal` is useful but not equivalent: legacy mission and handoff GOAL can diverge after `update-status`. `coordination.mission.update` remains unbacked, so there is no authoritative native mission datum for this line. |
| Blocked | Legacy's free-text field is changed both by `update-status` and alert raise. PeerHub's room-alert singleton carries only the alert-derived `blocked` string (`peerhub/application/alert_raise.py:93-112`). | **Real gap.** Reading only the alert target would lose non-alert blocked reasons and cannot represent an explicit clear independently of alert history. A room-summary/status projection needs an owning writer first. |
| Phase | Legacy `update-status`, `new-topic`, and `clear-room` write room phase (`P:/_sys/core/hub.py:1608-1617`, `P:/_sys/core/hub.py:3560-3567`, `P:/_sys/core/hub.py:3592-3599`). The native room schema has no room phase (`peerhub/governance/rooms.py:82-101`). | **Real gap.** Task/consensus phases are different domains and cannot be substituted. |
| Updated | Legacy has one status-projection timestamp written with mission/blocked/phase (`P:/_sys/core/hub.py:1608-1615`). | **Real gap coupled to the preceding three rows.** Taking the maximum timestamp across unrelated room, alert, goal, role, and mailbox records would be a new semantic, not parity. |
| Mailbox unread count | Legacy reads one global cached count, not a per-caller inbox (`P:/_sys/core/hub.py:1682-1684`, `P:/_sys/core/hub.py:1705`). PeerHub's public `RoomsService.check_inbox()` is deliberately room- and recipient-scoped and enforces that recipient at read time (`peerhub/governance/rooms.py:302-342`); read state is a separate high-water cursor (`peerhub/governance/rooms.py:344-417`). | **Real read-model gap.** A public aggregate count must define scope (current room vs all rooms), identity deduplication, and authority to inspect other recipients. Direct broker scanning inside a dashboard would bypass the service's explicit read-authorization boundary. |
| Tasks | Native task records carry a lifecycle `state` and optional room scope (`peerhub/governance/tasks.py:37-76`); `list_active_tasks()` already defines active as every nonterminal state (`peerhub/governance/activity.py:28-38`). | **Ready with composition.** Count `broker.list_targets("task", None)` for total and the established nonterminal helper for active. This intentionally adapts legacy's literal `status == "ACTIVE"` to the native lifecycle; whether to report all workspace tasks (closest legacy match) or current-room tasks must be frozen. |
| Locks | `FileLockService.list_active_locks()` now exposes the workspace-global lock targets (`peerhub/governance/file_locks.py:167-168`); release hard-deletes a target, so the returned length is the active count (`peerhub/governance/file_locks.py:131-165`). | **Ready now.** Render the line only when the returned sequence is nonempty. This is the dependency the locks round genuinely closed. |
| Active consensus | `list_active_consensus_rounds()` exposes open rounds (`peerhub/governance/activity.py:16-25`); native states contain proposal text, participant snapshots, votes, phase, and quorum counts (`peerhub/governance/consensus.py:60-101`, `peerhub/governance/consensus.py:341-368`). | **Ready with schema adaptation.** Filter/define the native analogue of legacy `voting`, map subject to proposal title/question, and freeze whether the denominator is required or eligible participants. No health evidence is read by the report. |
| Handoff | `RoomsService.context_fill()` is a pure bounded continuity read (`peerhub/governance/rooms.py:662-705`); its underlying projection owns GOAL, ordered section items, source counts, truncation metadata, and canonical Markdown (`peerhub/governance/rooms.py:707-818`, `peerhub/governance/rooms.py:845-855`). | **Ready with composition, not byte parity.** The coordinator can render the structured projection, but native policy deliberately bounds/truncates continuity whereas legacy prints the whole file verbatim. It must also suppress the whole HANDOFF section when there is neither a goal nor any source note if legacy conditional output is desired. |

`TelemetryPresenter.collect_live_snapshot()` is **not** a backing source for any of those room-report rows. It reads workspace `_sys` orchestration/status files plus injected quota projections (`peerhub/telemetry/presenter.py:269-306`) and returns peer/quota/routing data (`peerhub/telemetry/presenter.py:507-518`); its renderer even carries a stale comment that PeerHub has no room/leader concept (`peerhub/telemetry/presenter.py:538-544`). It is an output-separation precedent only: collect a structured snapshot, then render it (`peerhub/telemetry/presenter.py:520-523`). Likewise, `TaskService` and `ConsensusService` individually expose useful targets, but the existing cross-domain helper/CLI composition is only consensus/tasks/lessons and explicitly leaves duty leases unavailable (`peerhub/cli.py:371-404`, `peerhub/cli.py:456-466`); it is not the legacy status report hiding under another name.

**Dependency verdict.** Ready now: leader/reason, live members, roles, task counts, locks, active-consensus rows, and bounded handoff data. Still blocked: (1) an authoritative room summary owning mission/blocked/phase/updated-at -- most naturally landed with the still-unbacked `coordination.mission.update`; (2) a safe public unread aggregate and its scope/authority rule; (3) the ambient-current-room compatibility rule for a no-input call; and (4) process-lease sweep/reaper orchestration, unless parity explicitly drops that side effect. `PeerProfileBinding` is **not** a dependency because the legacy handler reports no model, effort, capability, or profile-inventory field. Gap B's outside-bootstrap `ReadinessObserved` producer is also **not** a report dependency: Gap B is specifically about generating fresh readiness evidence and manual recovery probes (`docs/design/HUB-REPLACEMENT-GAP7-HEALTH-FRESHNESS-AND-EVIDENCE-PRODUCER-2026-08-30.md:33-46`), while the missing status prerequisite is process-lease enumeration/recovery/reaping. The eventual lease-sweep design must separately decide how its `lease_expired` failure reaches native health/history, but status must not silently depend on `DutyLeaseCoordinator` or claim Gap B itself supplies a process supervisor.

**No ratified candidate design yet.** Omitting only locks would previously have been a plausible scoped non-goal; after this audit, a “v1” would also need to omit or fabricate four central room lines, change mailbox semantics, require a room identifier the legacy command does not take, and drop process cleanup. That is a different native dashboard, not a scoped implementation of legacy `status`. Once the four blockers above are resolved, the likely placement is an application-layer `StatusCoordinator` (the same cross-domain placement pattern used by `AlertRaiseCoordinator`, `peerhub/application/alert_raise.py:31-47`), returning a typed/structured snapshot rather than printing. `ApplicationAPI` read handlers conventionally encode structured targets/collections (`peerhub/application/api.py:413`, `peerhub/application/api.py:420-445`), so a presentation adapter should render the exact legacy Markdown order/conditionals for legacy/CLI parity while JSON/API callers receive the structured snapshot. Do not freeze a method signature or `READ_ONLY` descriptor until the process-sweep waiver/split is decided: preserving legacy makes the orchestration mutating; dropping it permits a clean `collect(room_id: str) -> StatusSnapshot` but is an explicit parity deviation.

### Process-lease scouting (2026-08-31): a real but SMALLER-than-legacy coordinator gap; the reaping half is architecturally constrained and the health half is blocked on the same bridge as `report-error`

Research-only pass (cc.deepthink) on the process-lease domain named as blocker (4) of `status` above, and on the previously-rejected `lease-status`/`lease-sweep` legacy actions. Every legacy claim below was re-derived directly from `P:/_sys/core/hub.py`, and every PeerHub claim from the shipped 49/90 tree. **Verdict: (a) -- a genuine new coordinator is needed -- but with two material scope corrections cx's pass did not have, one of which shrinks the reaping half substantially and one of which shows the health half is not actually unblocked.**

**Legacy contract, verified.** A "process lease" is one *dispatch attempt's* child OS process, not a peer's long-lived process. `_lease_open(ai_root, peer_id, pid, lease_timeout_sec, ask_id, ask_query_file)` writes one entry per ask into `ai_root/leases.json`, keyed by a fresh `uuid4` -- explicitly *not* by `peer_id` (an old bug where concurrent same-peer asks clobbered each other) and *not* by `ask_id` (`_short_id()` is only 4 hex chars, a real collision risk) (`P:/_sys/core/hub.py:10829-10860`). The entry is `{ask_id, peer_id, pid, room_id, started_at, expires_at, heartbeat_at, status, ask_query_file}` with `status="open"`. `peer_id` is the **fully-resolved dispatch target** (`ag.deepthink`), not a root peer. `_lease_renew` and `_lease_close` are keyed by `lease_id` and PID-checked, raising `LeaseOwnershipError` on mismatch rather than silently no-opping (`P:/_sys/core/hub.py:10862-10897`). Lifetimes come from `communication_policy` via `_lease_cfg()`: `lease_timeout_sec` (default 300, the orphan-cleanup window) is deliberately distinct from `zombie_timeout_sec` (default 600, profile-overridable, the in-flight silent-process kill threshold used by the ask heartbeat loop at `P:/_sys/core/hub.py:4579`, `P:/_sys/core/hub.py:4734`) -- `lease-sweep` is the *orphan* path, not the zombie path (`P:/_sys/core/hub.py:10773-10792`).

Liveness is a bare `os.kill(pid, 0)` (`_pid_alive`, `P:/_sys/core/hub.py:8098-8104`), wrapped by `_validated_live_lease_pid` which additionally rejects non-`int`/non-positive PIDs (`P:/_sys/core/hub.py:10822-10827`). **There is no PID-reuse protection at all** -- legacy will happily `kill` whatever process now holds a recycled PID. `_lease_sweep(ai_root)` (`P:/_sys/core/hub.py:10953-11053`) scans every `status == "open"` entry under the `leases` filelock: an unparseable `expires_at` becomes `status="invalid_timestamp"` with `quarantined=True`/`quarantine_reason`/`quarantined_at`; a past `expires_at` becomes `status="expired"`. Entries are **marked, never deleted**, so the sweep is convergent and a second run is a pure no-op. All slow/lock-taking work runs *after* the lease lock is released. For each expired entry it then (i) `_kill_process_tree(pid)` if the PID validates live, else prints `[HUB:WARN] expired lease {id} has invalid pid=...; kill skipped`; (ii) `_record_ask_failure(root_peer_id, "lease_expired", ...)` with `profile_key` split off a composite id; (iii) `_append_ask_history(..., "lease_expired")`; (iv) `_log_p2p("SWEEP", ...)`. `_kill_process_tree` is psutil `children(recursive=True)` + parent kill, falling back to `taskkill /F /T` on Windows or a bare `SIGKILL` on POSIX (`P:/_sys/core/hub.py:10920-10949`). `action_lease_status` is a pure read: `[HUB] No leases.json found.` / `[HUB] No active leases.` / else a fixed-width `Peer/Status/PID/Alive/Expires/Heartbeat` table with a 78-char divider, a live `psutil.pid_exists` probe rendering `YES`/`NO`/`ERR`, and a ` !` or ` !INVALID_TIMESTAMP` suffix on the status column (`P:/_sys/core/hub.py:11713-11746`). Both match the parity ledger (`docs/design/PHASE1-PARITY-LEDGER-BATCH4-2026-08-20.md:136-172`); `lease-sweep` is a zero-token `_SYSTEM_EXEMPT_ACTIONS` watchdog (`P:/_sys/core/hub.py:9169`) and is invoked implicitly by `init-session`, `status`, and every `ask` (`P:/_sys/core/hub.py:1369`, `P:/_sys/core/hub.py:1681`, `P:/_sys/core/hub.py:6528`), which is why `status` inherits the mutation cx flagged.

**CORRECTION 1 (load-bearing): `_lease_sweep`'s own docstring "Updates health to STALE" (`P:/_sys/core/hub.py:10954`) is FALSE, and the ledger's "updates peer health/history" understates it in a way that would produce a wrong port.** `lease_expired` is a member of `_TRANSIENT_REASONS` (`P:/_sys/core/hub.py:1792`). Tracing `_record_ask_failure`: transient reasons increment `transient_failures` and *reset* `consecutive_failures` rather than incrementing it; `critical_reasons.difference_update(_TRANSIENT_REASONS)` and the `not is_transient` guards mean `context_health.status` can never be set to RED or YELLOW by a lease expiry; and because `_lease_sweep` passes a non-`None` `profile_key` for composite targets, `is_profile_scoped` is True and every write lands in `availability.profiles["deepthink"]`, not on the root peer. The one real effect is `close_target_gate = True` (transient and not `terminal_timeout`), which sets **`gate_open = False`** on that profile and synthesizes a `rate_limit_state` exponential backoff of `min(300, 2 ** transient_failures)` seconds. So legacy's actual health semantics are: *an expired process lease closes one profile's admission gate for a short, self-clearing backoff window*. It does not mark anything STALE, does not degrade the root peer, and cannot quarantine. Any PeerHub port that renders this as a health-status downgrade is wrong.

**CORRECTION 2 (new, and it shrinks the reaping half): on Windows, PeerHub-spawned trees are bound with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, so the orphan class legacy reaps largely cannot arise.** `TreeController.bind_spawn` creates a Job Object and sets `LimitFlags = 0x2000` explicitly so "children die automatically if the parent process abruptly exits" (`peerhub/dispatch/tree_controller.py:212-241`). Legacy's `_lease_sweep` reaper exists because a `hub.py` invocation that died mid-ask left an orphaned `agy`/`node` subprocess with nothing to kill it. In PeerHub that subprocess is already dead when the owning process's handle closes. The residual reapable population is narrow and explicitly documented: children spawned by the root *before* `AssignProcessToJobObject` can escape containment, and "RECONCILE_TREE re-observation is the accepted safety net for an escaped child, not a guarantee of prevention" (`peerhub/dispatch/tree_controller.py:20-24`), plus anything not spawned through `bind_spawn` at all. This does not delete the domain -- the *lease-state* recovery and health/history halves are untouched by it -- but it means a PeerHub sweep's reaper is a residual-cleanup safety net, not the primary mechanism it is in legacy, and the design should say so rather than porting the reaper at legacy's weight.

**CORRECTION 3 (new): the kill primitive cannot be driven cross-process, because `TreeHandle` is not persistable.** cx's framing that PeerHub has "an identity-verifying `TreeController.kill_tree()` OS primitive" but no composing coordinator is confirmed exactly -- `verify_process_identity` (`peerhub/dispatch/tree_controller.py:152-175`) has **four call sites, all inside `tree_controller.py` itself** (`:271`, `:328`, `:375`, `:456`) and no external caller anywhere in the tree. But the sharper constraint is what `kill_tree` needs. It takes a `TreeHandle`, and the concrete `ProcessTreeHandle` carries a live `subprocess.Popen` and a raw Windows Job Object `HANDLE` int (`peerhub/dispatch/tree_controller.py:41-48`) -- neither survives the creating process. What *is* persisted is only `owner_process_pid` + `owner_process_creation_time` (`peerhub/persistence/migrations/0003_command_request_attempt.sql:101-102`, rehydrated into a `ProcessBirthIdentity` at `peerhub/persistence/sqlite_dispatch.py:1254-1261`). A fresh `peerhub lease-sweep` process can therefore reconstruct the *identity* and call `verify_process_identity` (both args are plain ints -- this is genuinely reusable), but a `TreeHandle` rebuilt from it has `win_job_handle is None`, and `kill_tree` then falls through to an identity-verified `TerminateProcess` on the **root PID only** (`peerhub/dispatch/tree_controller.py:398-415`) -- a single-process kill, not a tree reap. POSIX degrades the same way to `pgid = ... or root.pid`. So "reuse the existing kill primitive" is only half-true: the identity check ports cleanly, the tree reap does not.

**What PeerHub already has, precisely.** The persisted `leases` table is structurally *stronger* than legacy's `leases.json`: `lease_id`, `session_id`, `command_id`, `attempt_id`, `fencing_token`, `authority_epoch`, `revision`, `owner_principal_id`, `owner_instance_id`, `owner_peer_id`, the dual-key process birth identity, a 10-value `state` enum, and `heartbeat_expires_at` (`peerhub/persistence/migrations/0003_command_request_attempt.sql:91-135`). `expire_and_recover_lease` is a complete pure reducer that already distinguishes `ABANDONED_PRE_SPAWN` / `IDENTITY_MISMATCH` / `FENCED` and emits a `RecoveryReceipt` (`peerhub/dispatch/model.py:1220-1275`), and `SessionLeaseService.recover_lease` commits it under CAS with the receipt in one unit of work (`peerhub/dispatch/session_lease.py:278-330`). The dedicated heartbeat worker already refuses ghost renewals by checking `process.poll()` and the recorded identity (`peerhub/dispatch/heartbeat.py:1-19`). Four things are missing, and only four:

1. **No enumeration.** There are exactly two `FROM leases` queries in the whole persistence layer: `get_lease(lease_id)` (`peerhub/persistence/sqlite_dispatch.py:1240-1246`) and `count_active_leases` (`peerhub/persistence/sqlite_dispatch.py:1337-1352`). There is no expired-lease scan -- contrast `list_expired_duty_leases`, which exists for duty leases (`peerhub/persistence/sqlite_dispatch.py:295-305`). A sweep needs a **new persistence read**, not just a new coordinator.
2. **No observation producer.** `recover_lease` requires the caller to supply `is_process_alive` and `process_identity_matches` already-observed (`peerhub/dispatch/session_lease.py:286-287`), and nothing in the tree produces them for a persisted lease.
3. **No reaping composition**, subject to Correction 3.
4. **No health/history fan-out.** `count_active_leases` is consumed only as a scalar by the CLI dashboard (`peerhub/cli.py:2638`); `recover_lease` reaches the public service surface (`peerhub/dispatch/service.py:1006-1019`) but has **no non-test caller**. The CLI exposes only *duty*-lease verbs (`peerhub/cli.py:1439-1540`); there is no process-lease command at all.

**Other substrate surveyed, and rejected as overlap.** Bootstrap's readiness probe is a different concept: a synchronous `--version` spawn at construction time producing one `ReadinessObserved` admission observation, raising `ReadinessProbeFailedError` on non-zero exit (`peerhub/application/bootstrap.py:157-186`, `peerhub/application/bootstrap.py:34`). It answers "is this adapter invokable", not "is this recorded child still alive", keeps no PID, and never reaps. The health contract has **no `pid` or process field anywhere** -- health projections model admission/availability, not OS liveness. `DutyLeaseCoordinator` is correctly ruled out for the reasons cx gives, and its own cross-room enumeration is likewise still declared unavailable in the status payload (`peerhub/cli.py:399`). `TelemetryPresenter` reads `_sys` files and quota projections, not process state.

**Verdict: (a), scoped.** This is a real domain gap, not host tooling and not something already covered. It is *not* the same concept as `DutyLeaseCoordinator` (room/role duty), and the earlier session rejection of `lease-status`/`lease-sweep` as "genuinely different" was right about the distinction but wrong to stop there -- the concept it names maps almost exactly onto PeerHub's already-persisted, already-fenced `leases` table, which merely lacks a scan, an observer, and a consumer. Skeptically in the other direction: two thirds of legacy's *reaper* value is already provided structurally by `KILL_ON_JOB_CLOSE` (Correction 2), and the remaining cross-process reap is not expressible through the existing primitive (Correction 3). **And the health half is blocked on a known prerequisite**: reproducing Correction 1's real effect requires a writer that can set a profile-scoped `gate_open = False` with a backoff window, which PeerHub's `HealthService` does not expose -- this is the *same* health-authority-bridge gap already recorded above as blocking `report-error`'s auto-quarantine half. So this domain unblocks `status`'s blocker (4) only if the health fan-out is either deferred behind that bridge or explicitly waived.

**Critique-ready candidate design sketch (not ratified; needs a critique round).**

*Placement*: `peerhub/application/process_lease_sweep.py`, `ProcessLeaseSweepCoordinator`. Application layer, not `governance/`, because it composes three distinct owners -- `SessionLeaseService` (dispatch state), `TreeController` (OS), and health/history -- which is the same cross-domain rationale that places `AlertRaiseCoordinator` and `LeadershipService` there, and the opposite of the self-contained test that put `FileLockService`/`FeedbackService` in `governance/`.

*New persistence read* (mirroring the duty-lease precedent at `peerhub/persistence/sqlite_dispatch.py:295-305`): `list_expired_leases(as_of: int, *, limit: int = 100) -> Sequence[LeaseSnapshot]`, `WHERE state IN ('RESERVED','ACTIVE','RENEWED') AND heartbeat_expires_at < :as_of ORDER BY heartbeat_expires_at, lease_id`, added to the dispatch read unit of work and `StateStore`.

*Constructor*: `ProcessLeaseSweepCoordinator(sessions: SessionLeaseService, *, store, tree_controller, clock, ids, policy_id, policy_revision)`.

*Methods*:
- `sweep(*, recovery_actor_principal_id: str, as_of: int | None = None, limit: int = 100, reap: bool = True) -> ProcessLeaseSweepReport`. Per expired lease: rehydrate `ProcessBirthIdentity`; call `verify_process_identity(identity.pid, identity)` to derive `process_identity_matches`, and `is_process_alive` from the same probe; call `sessions.recover_lease(lease_id, trigger=RecoveryTrigger.HEARTBEAT_TIMEOUT, is_process_alive=..., process_identity_matches=..., ...)`, letting the existing reducer pick `FENCED` / `IDENTITY_MISMATCH` / `ABANDONED_PRE_SPAWN`; then, only when `reap` and the identity verified and the process is alive, terminate. Convergent and idempotent by construction: `recover_lease` moves the lease out of the swept state set, so a second run finds nothing -- matching legacy's mark-never-delete behaviour without needing legacy's `changed` flag.
- `status(*, as_of: int | None = None) -> Sequence[ProcessLeaseObservation]` for `lease-status`: one row per non-terminal lease carrying `owner_peer_id`, `state`, `pid`, an `alive` tri-state (`YES`/`NO`/`ERR`, from the same identity probe), `heartbeat_expires_at`, and an `expired` flag. Structured return, rendered by the CLI -- the same collect-then-render split cx recommends for `StatusCoordinator`.

*Reaping, stated honestly*: v1 should add `TreeController.kill_by_identity(identity: ProcessBirthIdentity) -> TreeDispatchReceipt` -- identity-verified single-process termination reusing `verify_process_identity` -- and **must not** be documented as a tree reap. Persisting a re-bindable tree token is the only route to real cross-process tree termination and should be deferred to its own round, with the residual gap recorded rather than papered over.

*Known parity deviations to record up front*, since `lease-status`/`lease-sweep` output cannot be byte-parity: legacy's three statuses (`open`/`expired`/`invalid_timestamp`) do not map onto PeerHub's ten-value `LeaseState` enum; and `invalid_timestamp` **cannot occur** in PeerHub at all, because `heartbeat_expires_at` is a schema-checked `INTEGER ... CHECK (heartbeat_expires_at >= 0)` (`peerhub/persistence/migrations/0003_command_request_attempt.sql:118-119`), so the entire quarantine branch and its `fix-lease-sweep-inv-01` fixture are structurally unreachable -- a deviation, not a bug. Legacy's `peer_id` maps to `owner_peer_id`; legacy's `ask_id`/`room_id`/`ask_query_file` have no lease-table column and would come from the joined command/session rows if the report needs them.

### Manual-quarantine authorization policy PROPOSED (2026-08-30)

**Recommendation: Option C (Budget-gated administrative authorization with authority-class matching).**

Instead of inventing a new RBAC privilege system (which peerhub does not have) or dangerously overloading the automated `authorize_recovery()` path to ignore cooldowns, we should follow the established permission pattern from `arbiter-review`: **budget gating**. Furthermore, we borrow the state-based protection pattern from `LeadershipService` (which protects incumbents based on state) by using a **QuarantineAuthorityClass precedence ladder** (`AUTOMATIC < MANUAL < POLICY < SECURITY`).

**Design:**
Create a distinct, explicitly-named method on `HealthService`:
`authorize_administrative_recovery(self, instance_id: str, profile_id: str, scope: PolicyScope, subject: str, *, authorized_by: str, target_authority_class: QuarantineAuthorityClass) -> RecoveryProbeAuthorization`

1. **Preconditions & Authorization:**
   - Evaluates the circuit's existing `quarantine_authority_class` against the requested `target_authority_class`. If the circuit's class dominates the requested class (e.g., circuit is `SECURITY` but caller asserts `MANUAL`), reject it. This protects higher-order quarantines without needing RBAC user roles.
   - Bypasses the `AdmissionState.RECOVERY_REQUIRED` check. Because this is an explicit administrative override, it accepts `QUARANTINED` (manual) and `COOLDOWN` (automatic) circuits.
   - **The Budget Gate:** To prevent probe-spam abuse, it must acquire a slot from an `AdministrativeRecoveryBudgetManager` using the already-defined `policy.administrative_recovery_probe_limit` (fulfilling the deferred check noted in `peerhub/health/model.py`'s `authorize_recovery_probe`). If the budget is exceeded, the authorization is rejected.

2. **State read/written:**
   - Reads the existing projection, circuit, policy, and a new CAS-backed budget target (e.g., `health-budget:administrative-recovery`).
   - Writes the budget consumption, inserts the unconsumed `RecoveryProbeGrant` (1 probe), and CAS-updates the projection to `PROBE_AUTHORIZED` via the existing `reduce_authorize_recovery()` pure reducer.

3. **Success vs. Failure Pipeline Interaction:**
   - This keeps the *authorization* paths fully separate (automated sweeps call `authorize_recovery` and respect cooldowns; operators call `authorize_administrative_recovery` and spend budget).
   - Once authorized, it feeds into the **exact same recovery pipeline**. The grant is claimed via `claim_recovery_probe` and evaluated via `apply_recovery_probe_result`.
   - On probe success, the reducer transitions the circuit to `CIRCUIT_CLOSED`, safely lifting the manual quarantine *because real health evidence was provided*.
   - On probe failure, the grant is exhausted, backoff increments, and the circuit's state re-evaluates (remaining `QUARANTINED` since its authority class is unchanged).

**Tradeoffs of alternatives considered:**
- **Option A (Distinct gate with just an operator reason):** Fails to mitigate the risk of probe-spam/abuse without a budget limit, and doesn't explicitly handle hierarchical quarantines (like `SECURITY` vs `MANUAL`).
- **Option B (Extending `authorize_recovery` to accept `QUARANTINED`):** Blurs the lines dangerously. An automated `health-sweep` could accidentally lift a manual quarantine if it just scanned for all non-open circuits, and applying budget logic conditionally inside a single method creates branching hazards.

**Explicitly OUT of scope for this decision:**
- The design and placement of the `HealthRevalidationCoordinator` (the application-layer piece that actually executes the probe).
- The definition of the exact budget window duration (requires its own policy configuration like `arbiter-review`'s 5-hour window).
- Any "break-glass" mechanism to write `HEALTHY` directly without a probe. As ratified in `docs/design/phase0/RUNTIME-HEALTH-DRIFT-2026-07-28.md` (lines 45-55), recovery *cannot write HEALTHY directly*; it must produce a successful probe.

#### Critique (cx.deepthink, 2026-08-30)

Independent verification verdict: **REVISION REQUIRED BEFORE RATIFICATION.** The proposal correctly identifies the manual-quarantine deadlock and the right high-level shape (a distinct administrative entry point feeding the existing receipt-fenced probe pipeline, never a direct healthy write), but its proposed authority check is self-asserted rather than authorized, and the current aggregate projection logic would make the new path inoperative even after it persisted a grant.

1. **`QuarantineAuthorityClass`: TYPE CONFIRMED; PRECEDENCE LADDER REFUTED AS EXISTING SUBSTRATE.** The real enum contains all four names, but it is an unordered `str, Enum`, declared `AUTOMATIC`, `MANUAL`, `SECURITY`, `POLICY` (`peerhub/health/contract.py:157-163`). No rank map or dominance operation exists; current reducers only distinguish `AUTOMATIC` from every other class (`peerhub/health/model.py:517-525`, `peerhub/health/model.py:701-709`). Thus `AUTOMATIC < MANUAL < POLICY < SECURITY` is a new policy rule, not something already supplied by the type (and even differs from declaration order, which is not semantic ordering anyway). The existing frozen statement is that manual/security/policy quarantines require their *designated authority* (`docs/design/PHASE1-KICKOFF-R1.md:56-67`), which supports an explicit clearance matrix or exact-class rule more directly than a total order. If cross-class dominance is wanted, it must be separately justified and tested.

2. **`administrative_recovery_probe_limit`: CONFIRMED REAL FIELD, BUT NOT A COMPLETE BUDGET CONTRACT.** `HealthPolicy` defines and positively validates it (`peerhub/health/contract.py:191-205`, `peerhub/health/contract.py:258-265`), SQLite persists it (`peerhub/persistence/migrations/0005_health_routing.sql:10-21`, `peerhub/persistence/sqlite_health.py:105-110`), and the reducer's deferred comment is real (`peerhub/health/model.py:554-565`). The reducer performs no check. No field defines the limit's time window, reset rule, scope (workspace/principal/circuit), reservation lifetime, or failure accounting. Declaring the budget-window duration out of scope while making that budget the safety gate leaves the gate unimplementable. Ratification must at least freeze those semantics and add a versioned window field (or deliberately define a different non-window budget).

3. **RECOVERY FUNCTIONS CONFIRMED, WITH NAMING CORRECTIONS.** The pure reducer is actually `authorize_recovery_probe()`; `reduce_authorize_recovery` is only its import alias inside `service.py` (`peerhub/health/model.py:554-592`, `peerhub/health/service.py:60-64`). The pure `claim_recovery_probe()` and `apply_recovery_probe_result()` functions are real and implement single-use claim plus receipt/revision-fenced failure/success transitions (`peerhub/health/model.py:595-668`). The public service methods are named `claim_probe()` and `apply_probe_result()`, and add transactional CAS/receipt checks (`peerhub/health/service.py:1186-1246`, `peerhub/health/service.py:1248-1315`). A current successful receipt closes the circuit regardless of authority class, so the administrative authorization boundary must itself be trustworthy.

4. **DEADLOCK CONFIRMED; THE PROPOSED BYPASS IS NECESSARY IN PRINCIPLE BUT NOT SUFFICIENTLY SAFE.** A non-automatic open circuit always evaluates to `QUARANTINED`, with no retry boundary, whereas only an automatic circuit can advance through `COOLDOWN` to `RECOVERY_REQUIRED` (`peerhub/health/model.py:694-735`). `HealthService.authorize_recovery()` accepts only `RECOVERY_REQUIRED` (`peerhub/health/service.py:1120-1133`). A distinct way to authorize a measured probe for a non-automatic quarantine is therefore required. However, a caller-supplied `target_authority_class` is not proof: any caller can assert `SECURITY` and dominate every proposed class. The comparison prevents only an accidentally low assertion, not an unauthorized high one. Budgeting limits frequency; it does not confer authority.

5. **REFUTED: THE EXISTING PROJECTION PIPELINE CANNOT YET REPRESENT THIS ADMINISTRATIVE GRANT.** `_aggregate_for_pair()` treats a live grant as `PROBE_AUTHORIZED` only when the same circuit already evaluates to `RECOVERY_REQUIRED` (`peerhub/health/service.py:585-601`). `_recompute_pair()` then overwrites the reducer seed's admission state with that aggregate (`peerhub/health/service.py:681-743`). Consequently, a grant issued while the circuit is `QUARANTINED` would recompute straight back to `QUARANTINED`; one issued while automatic cooldown is active would recompute to `COOLDOWN`. Avoiding recomputation would leave other members of a shared scope inconsistent and a later recomputation would revert the seed. The grant and aggregate contracts need a durable administrative-authorization marker (including authorized circuit revision/authority evidence), and aggregate validity must recognize that marker while still invalidating it on circuit escalation or receipt/revision change. `RecoveryProbeGrant` currently stores neither authorization mode nor authorized circuit revision (`peerhub/health/contract.py:736-747`).

6. **A GLOBAL BUDGET DOES NOT PROVIDE PER-CIRCUIT SINGLE-FLIGHT OR PACING.** Persistence enforces only one *unconsumed* grant per circuit (`peerhub/persistence/migrations/0006_recovery_probe_single_flight.sql:1-10`; `peerhub/persistence/sqlite_health.py:576-587`). As soon as a grant is claimed, another can be issued before the first result arrives. Revision fencing makes later results stale but does not prevent several provider probes from running. On a manual failure, `backoff_count` increments, yet non-automatic evaluation ignores that backoff and stays permanently `QUARANTINED` (`peerhub/health/model.py:644-655`, `peerhub/health/model.py:701-709`). The revision needs a per-circuit unresolved-attempt lease/single-flight rule and a next-administrative-probe boundary (or equivalent cooldown) in addition to an aggregate budget. It must define recovery from an abandoned claimed probe so the circuit is not locked forever.

7. **ACCEPTING `COOLDOWN` IS UNJUSTIFIED SCOPE EXPANSION.** The identified deadlock concerns non-automatic `QUARANTINED` circuits. Automatic circuits already reach `RECOVERY_REQUIRED` after the policy backoff (`peerhub/health/model.py:711-735`). Letting ordinary administrative authorization bypass `COOLDOWN` weakens that separate pacing policy without being necessary to solve the blocker. Exclude `COOLDOWN` from the first policy, or ratify an explicitly stronger early-retry/break-cooldown capability with its own authority and pacing semantics.

8. **SEPARATE PUBLIC PATHS ARE SOUND; FULLY DUPLICATED END-TO-END LOGIC IS NOT. OPTION B'S REJECTION IS PARTLY OVERSTATED.** Keep `authorize_recovery()` and `authorize_administrative_recovery()` as distinct public APIs so automated sweeps cannot opt into administrative behavior accidentally. Both should delegate to one internal authorization transaction/helper for configured-pair membership, projection policy/baseline, circuit/receipt/revision validation, grant conflict, grant construction, member recomputation, and fault boundaries; only the admission/authority/budget precondition should differ. Naively allowing every `QUARANTINED` circuit in today's `authorize_recovery()` would indeed be unsafe. A single typed method requiring an unforgeable administrative authorization object for that branch could also be safe, however, so method branching is not itself the security defect. The decisive controls are authenticated authority evidence and impossible-to-accidentally-select call sites.

9. **`authorized_by` MUST NOT REMAIN FREE TEXT, AND `PeerRegistryService` IS THE WRONG VALIDATOR.** Today `RecoveryProbeGrant` merely requires `authorized_by` to be non-empty (`peerhub/health/contract.py:749-759`), and `HealthService` copies it without authentication (`peerhub/health/service.py:1146-1155`). `PeerRegistryService` resolves peer-node/profile inventory, not human or service principals (`peerhub/application/peer_registry.py:72-130`). The relevant existing boundary type is `AuthenticatedSubject`, which carries a verified opaque principal plus evidence source and has a fail-closed type check (`peerhub/core/identity.py:14-31`, `peerhub/core/identity.py:89-96`). Administrative recovery should accept/derive that subject at a trusted application boundary and separately obtain an unforgeable authority grant/capability mapping the subject to the permitted quarantine class. Persist the principal, evidence source, granted class, authorizer/receipt ID, and reason. Authentication alone still is not authorization; unsupported `POLICY`/`SECURITY` classes must fail closed until an issuer exists.

10. **BUDGET ATOMICITY AND PROBE-EVIDENCE TRUST NEED TO BE EXPLICIT.** A generic CAS governance target and health grant/projection writes do not become one transaction merely because both use CAS. Either make the budget a health-owned row updated in the same `StateStore` unit of work as the grant/projections, or use an application-layer reservation/consume/release/unknown protocol like the real `ArbiterBudgetManager` (`peerhub/application/arbiter_review.py:226-316`, `peerhub/application/arbiter_review.py:319-415`). Also, the current `RecoveryProbeReceipt` contract permits empty evidence references and `apply_probe_result()` trusts the typed receipt it receives (`peerhub/health/contract.py:872-916`, `peerhub/health/service.py:1248-1293`). Reusing this pipeline preserves fencing and the no-direct-healthy rule (`docs/design/phase0/RUNTIME-HEALTH-DRIFT-2026-07-28.md:52-55`), but “real health evidence was provided” is guaranteed only if the future revalidation coordinator is a trusted producer/verifier and raw success receipts are not exposed to untrusted callers.

**Required revision before ratification:** retain the two public authorization entry points and shared probe-result pipeline, but (a) freeze an explicit authority matrix rather than assume enum ordering; (b) replace caller assertion/free text with authenticated principal plus issued authority evidence; (c) initially admit only the needed non-automatic `QUARANTINED` case; (d) extend the durable grant/aggregate validity model for administrative origin, circuit revision, and authority escalation; (e) freeze budget window/scope/atomicity and add per-circuit unresolved-probe plus retry pacing; and (f) centralize common authorization mechanics in one internal helper. Required tests include forged higher-class assertions, MANUAL/POLICY/SECURITY cross-class cases, circuit escalation after grant, multi-member projection recomputation, claimed-but-unresolved contention/expiry, concurrent budget reservation, and rollback/unknown outcomes. With those corrections, Option C's overall shape is preferable; as written, it is not ready for ratification.

No further Tier 4 work queued for now, at the user's direction -- most of the remaining ~45 unbacked actions have turned out to be either real per-domain design work (`report-error`'s health-authority bridge, `elect-leader`'s capability-scoring, `status`'s cross-domain aggregation) or host-level tooling this session has now repeatedly and consistently found doesn't belong in peerhub's domain model at all (8 confirmed instances across three separate scouting rounds: `freshness-sweep`, `append-log`, `archive-file`, `preflight`, `context-hash`, `profile-validate`, `transient-scan`, `update-signatures`). The clearest remaining tractable item, if picked back up, is `report-error`'s append-and-count half (the auto-quarantine half needs the same health-authority-bridge design work already identified as blocking).
- **Host-level / P:-environment-specific** (5 actions, **worth questioning whether these belong in peerhub's domain model at all**): `directive-add/list/clear`, `credit-status`, `credit-consume` — these read like they're about the P: portable environment's own host integration, not a generic peerhub concept. Resolve "does peerhub own this or does it stay host-side" before designing, not after.
- **Alert** (1 action): `alert-raise` — standalone, undesigned.

### Alert scouting (2026-08-30): `alert-raise` is a real, small room-coordination gap — not health/error alerting and not host tooling

Research-only round (cx.deepthink; no implementation). **Verdict: (b), a genuine new but small/self-contained PeerHub gap.** It is not already covered by `OperationalErrorService`, `FeedbackService`, or `HealthService`, and it is not a host-side false friend: the action mutates the shared room/session record and delivers peer-to-peer inbox messages. The catalog already maps it to `coordination.alert.raise`, but there is no translation/service branch, so it still reaches the generic known-but-unbacked result (`peerhub/application/legacy.py:183`, `peerhub/application/legacy.py:1642-1645`).

**Real legacy behavior, including two important corrections to the ledger's interpretation.** The current handler accepts an implicit current room, `agent`, `severity`, and `msg`; the CLI defaults to `agent="unknown"`, `severity="P1"`, and an empty message, and only `P0`/`P1` (case-insensitive) pass validation (`P:/_sys/core/hub.py:8977-8981`, `P:/_sys/core/hub.py:12139-12140`; parity ledger: `docs/design/PHASE1-PARITY-LEDGER-BATCH5-2026-08-20.md:140-142`). It is mutating and deliberately non-idempotent. Under the `state` lock it reads `state.json`, snapshots every key in `state.members`, creates a fresh short alert ID, and overwrites the one `state.alert_active` slot with `{id, ts, severity, from, msg, status:"OPEN", ack_pending:[all member keys]}`; it also sets the human-readable `state.blocked` marker and `updated_at`, then atomically rewrites `state.json` (`P:/_sys/core/hub.py:8983-8997`; `_write_state` is the validated atomic `state.json` writer at `P:/_sys/core/hub.py:1089-1091`). A later raise replaces the prior active slot rather than deduplicating or appending another alert.

After the state commit, the handler prints its P2P/banner output and calls `action_send` once for every member other than the raiser, with body `[CRITICAL-ALERT] {severity}: {msg}`, `msg_type="ALERT"`, and `priority="CRITICAL"` (`P:/_sys/core/hub.py:8999-9005`). Those are local durable mailbox deliveries — `action_send` appends recipient-addressed records to `mailbox.json` and writes their per-message Maildir copies (`P:/_sys/core/hub.py:1503-1524`) — not an external pager, human notification API, or host integration. State commits before fan-out, so a send failure can leave the alert durable with only a partial recipient fan-out; there is no cross-file transaction (`P:/_sys/core/hub.py:8994-9005`).

The first ledger correction: its claim that `_log_p2p("ALERT-RAISE", ...)` persists a new `log.jsonl` event is not true of the current requested source. `_log_p2p` only formats a line to stderr (`P:/_sys/core/hub.py:1131-1142`), and `action_alert_raise` calls no append/journal helper (`P:/_sys/core/hub.py:8997-9005`). The ledger makes the stronger `log.jsonl` claim at `docs/design/PHASE1-PARITY-LEDGER-BATCH5-2026-08-20.md:144,153`; that effect should not be ported from the ledger alone.

The second correction: “blocks all governance workflows” is a docstring/ledger interpretation, not an enforced legacy gate. In real source the alert only writes `state.blocked`; that field is rendered by `status` and included in ask context (`P:/_sys/core/hub.py:1701-1704`, `P:/_sys/core/hub.py:2435-2440`), while no command admission path reads it. Generic `update-status` can clear the marker, and `new-topic`/`clear-room` clear it as a side effect (`P:/_sys/core/hub.py:1608-1615`, `P:/_sys/core/hub.py:3561-3567`, `P:/_sys/core/hub.py:3593-3599`). None of those paths changes `alert_active`. There is likewise no alert ACK/resolve action, and `ack_pending` has no reader or mutator anywhere in the current `hub.py`; `status:"OPEN"` and `ack_pending` are inert creation-time fields (`P:/_sys/core/hub.py:8985-8995`; the CLI action inventory at `P:/_sys/core/hub.py:11905` contains `alert-raise` but no alert acknowledge/resolve command). Therefore legacy is **durable raise-and-notify with overwrite-on-next-raise**, not fire-and-forget, but it has no functioning `OPEN -> ACKNOWLEDGED -> RESOLVED` lifecycle and no real circuit breaker.

**Existing-substrate verdict — useful building blocks exist, but no service already covers the concept.**

- `OperationalErrorService` is a per-`(peer_key, pattern)` append-and-count series; at or above a pinned threshold it creates an immutable `quarantine-review` request and explicitly leaves health circuit state to the health domain (`peerhub/governance/operational_errors.py:1-7`, `peerhub/governance/operational_errors.py:102-111`, `peerhub/governance/operational_errors.py:178-201`, `peerhub/governance/operational_errors.py:224-249`). `alert-raise` has no pattern counter, threshold, review request, or peer-health subject, and it must notify the live room immediately. Reusing this service would invent escalation semantics and still miss both the room marker and fan-out.
- `FeedbackService` is a multi-item journal with sequential `GAP-*` IDs, per-item `status`/`owner`, list, and resolve operations (`peerhub/governance/feedback.py:36-49`, `peerhub/governance/feedback.py:111-170`, `peerhub/governance/feedback.py:172-227`). Its superficially similar severity/detail fields do not make it an alert: it has no active-room singleton, live-member snapshot, blocking marker, or notification. Encoding an alert as feedback would erase the action's coordination semantics.
- `HealthService` owns policy-derived health/admission circuits, not arbitrary emergency messages. Opening a circuit requires a typed failed `HealthStageObservation` trace plus `EvidenceSubject` and `PolicyReceipt`, then updates the affected health scope and projections atomically (`peerhub/health/service.py:259-279`, `peerhub/health/service.py:925-1002`). Recovery is receipt-fenced and circuit-specific (`peerhub/health/service.py:1004-1045`, `peerhub/health/service.py:1081-1184`). A free-form P0/P1 room alert has none of that evidence or subject identity, and legacy does not actually change health/admission state. Calling HealthService would repeat the authority-model error already rejected for forced `peer-quarantine`.
- The real reusable pieces are coordination substrate. `RoomParticipationCoordinator.list_active_sessions(room_id)` returns the heartbeat-fresh live sessions that most closely correspond to legacy `state.members` (`peerhub/dispatch/room_session.py:311-349`); this is a better recipient source than `RoomsService.list_participants()`, whose own contract says it is the room's immutable participant snapshot (`peerhub/governance/rooms.py:46-61`). `RoomsService.send_message()` already persists one private room-scoped `inbox-message` per recipient, with `message_type`, resource reference, and shared correlation ID (`peerhub/governance/rooms.py:209-296`). `LessonBroadcastCoordinator` is the structural precedent for an application-layer coordinator that records one governed campaign and then fans out through `RoomsService` (`peerhub/application/lesson_broadcast.py:30-52`, `peerhub/application/lesson_broadcast.py:63-128`), although its immutable-participant recipient rule must not be copied for this live-member action.

The older gap-6 draft is not a ready-made parity design. It proposed evidence-derived/deduplicated alerts and a broad `OPEN -> ACKNOWLEDGED -> UNDER_REVIEW -> ... -> RESOLVED` lifecycle (`docs/design/HUB-REPLACEMENT-GAP6-GOVERNANCE-2026-08-24.md:104-114`, `docs/design/HUB-REPLACEMENT-GAP6-GOVERNANCE-2026-08-24.md:155-170`), while real legacy is non-deduplicated, accepts only severity/message/raiser, and implements no lifecycle after creation. The later ratification only settled policy for ownership and when an alert requires a proposal (`docs/design/HUB-REPLACEMENT-PRE-TDD-FINAL-RATIFICATION-2026-08-26.md:60-62`); it did not supply the missing schema, raise mechanics, or parity boundary. Treat that richer alert registry as possible future native scope, not as evidence that this standalone compatibility action is already covered.

**Critique-ready ratified candidate (not implemented and not declared final by this research pass).** Place `AlertRaiseCoordinator` in `peerhub/application/alert_raise.py`, because it coordinates three owners — governance targets, live room sessions, and inbox delivery — just as `LessonBroadcastCoordinator` lives in `application/` for cross-domain fan-out. Constructor: `AlertRaiseCoordinator(broker, *, rooms, room_sessions, clock, ids)`. Method: `raise_alert(*, room_id: str, raiser_instance_id: str, raiser_profile_id: str, severity: str = "P1", message: str = "") -> AlertRaiseResult`. Normalize severity to uppercase and reject anything outside `{P0,P1}`; deliberately permit the empty message because legacy does.

Persist one current-slot target, `target_id="room-alert:{room_id}"`, rather than one independently active target per raise. Its complete `TargetState.state` candidate is `{"kind":"room-alert", "scope":room_id, "schema_version":1, "room_id":room_id, "alert_id":..., "severity":"P0|P1", "message":..., "status":"OPEN", "raised_by":{"instance_id":...,"profile_id":...}, "raised_at":..., "ack_pending":(<snapshot of all live member identities, including the raiser if present>), "blocked":f"{severity} Alert: {message[:40]}...", "updated_at":...}`. This preserves the real singleton overwrite, visible block text, and inert compatibility fields without inventing acknowledgement semantics. Allocate the new alert ID/time once per logical call and retry a stale-revision CAS on this shared current slot with the existing bounded 16-attempt pattern: legacy's state lock serializes concurrent raises and allows every caller to commit, so simply surfacing the first CAS loss would be an avoidable parity regression. Refresh the live-session/revision snapshot on each CAS attempt and retain the winning snapshot for `ack_pending` and delivery, so a contended retry does not notify stale membership.

Only after the alert target commits, use that same winning live-session snapshot, deduplicate by `(instance_id, profile_id)`, exclude the exact raiser identity, and call `RoomsService.send_message()` per recipient with `message_type="ALERT"`, `body=f"[CRITICAL-ALERT] {severity}: {message}"`, `resource_ref="room-alert:{room_id}"`, and `correlation_id=alert_id`. One small additive message-substrate change is needed for exact state parity: `RoomsService.send_message(..., priority: str | None = None)` and an optional `priority` field on `inbox-message`, because its current signature/state has no priority slot (`peerhub/governance/rooms.py:209-222`, `peerhub/governance/rooms.py:266-288`); alert delivery supplies `"CRITICAL"`. `AlertRaiseResult` should return `alert_id`, `alert_target_id`, `room_id`, `recipient_profile_ids`, and `inbox_message_target_ids`, following the multi-write result precedent at `peerhub/application/lesson_broadcast.py:17-27`.

Explicit non-goals for the first parity slice: no dedup key, evidence trigger, alert classes, proposal creation, external paging, health-circuit mutation, governance-command admission gate, ACK/resolve/suppress methods, or claim that mailbox read acknowledgement clears `ack_pending`. A later native alert-lifecycle round may add those under the already-ratified ownership policy, but folding them into `alert-raise` now would turn a small faithful gap into the different, much larger gap-6 concept.



#### Critique (ag.deepthink, 2026-08-30)

Independent read-only verification pass over the proposed `alert-raise` candidate design.

1. **Legacy citations**: CONFIRMED. Direct inspection of `P:\_sys\core\hub.py` verifies the claims:
   - `action_alert_raise` (lines 8983-9005) blindly overwrites `state["alert_active"]` inside `_get_lock()`. No deduplication exists.
   - `_log_p2p` (lines 1131-1142) prints strictly to `sys.stderr` and does not write to any persistent log file.
   - `state.blocked` is set in `action_alert_raise` (line 8996) and occasionally cleared by other commands, but it is purely informational (appended to system prompt context or printed to stdout). No command gate ever reads `state.get("blocked")` to prevent execution.
2. **Peerhub substrate**: CONFIRMED. 
   - `RoomParticipationCoordinator.list_active_sessions()` exists at `peerhub/dispatch/room_session.py:311` and returns live heartbeat-fresh session snapshots. This is definitively the correct recipient source for live alerting, as opposed to `RoomsService.list_participants()` which returns the static immutable roster.
   - `RoomsService.send_message()` (in `peerhub/governance/rooms.py:209-296`) lacks a `priority` field in its signature and `inbox-message` state schema.
3. **Architectural Placement**: CONFIRMED. `peerhub/application/alert_raise.py` is the correct layer. It coordinates three discrete components (governance broker for the singleton target, room sessions for live recipients, and rooms service for fan-out), exactly matching the precedent set by `LessonBroadcastCoordinator`.
4. **CAS-retry Singleton Pattern**: CONFIRMED. Because `room-alert:{room_id}` is a shared singleton slot, concurrent callers will race. In legacy `hub.py`, the filesystem lock serialized these writes, guaranteeing every alert would overwrite the slot and send messages. If peerhub failed on the first CAS conflict without retrying, concurrent alerts would be dropped. The 16-attempt bounded retry correctly preserves legacy's guaranteed-overwrite semantics.
5. **Additive Priority Field**: CONFIRMED. Adding `priority: str | None = None` to `send_message()` is a safe additive change. Existing callers in `lesson_broadcast.py`, `api.py`, and `cli.py` will omit it and continue defaulting to `None`.
6. **Scope Creep / Gaps**: CONFIRMED NO CREEP. The candidate design correctly initializes `ack_pending` with the session identities but explicitly refuses to build an ACK/resolve mechanism. Legacy `hub.py` initialized this array but never built a way to clear it; the proposed design accurately captures this inert state. 

**Overall Verdict**: READY FOR IMPLEMENTATION. The design accurately maps the exact legacy behavior (and its quirks) into peerhub's native substrate without introducing scope creep or inventing new semantics.

**Implementation update (2026-08-30): DONE.** Implemented exactly as ratified in `peerhub/application/alert_raise.py`, including the bounded 16-attempt shared-slot CAS retry, winning live-session snapshot, inert `ack_pending`, exact raiser exclusion, distinct-identity fan-out, `CRITICAL` inbox priority, command/API/runtime/CLI wiring, and end-to-end legacy translation. This is new executable coverage, so the strictly-correct `LEGACY_CATALOG` count advances from 45/90 to 46/90.

### Scouting round (2026-08-28): mostly confirmed genuinely undesigned, one real find

Research-only pass (no implementation) explicitly looking for more "Tier 1 in disguise" candidates in Tier 4, cross-referencing every plausible-sounding action name against real service method signatures and lifecycle semantics (not just matching nouns). Result: Tier 4's difficulty assessment above holds up under scrutiny -- most near-matches were investigated and explicitly rejected on real evidence, not just assumed hard.

**One genuine find, now DONE (45 of the 90 `LEGACY_CATALOG` actions backed, up from 44)**: `proposal-list` -> `GovernanceBroker.list_targets("consensus-round", None)`. `peerhub consensus list` is real; wired through `LegacyTranslator`/`ApplicationAPI` as `governance.proposal.list` (READ_ONLY, `{"proposals": [...]}` result shape, reusing the `lessons-list` pattern correctly). Independently re-verified by the terminal: proposed 2 rounds, drove one to `quorum_reached` via real votes, listed -- both appeared regardless of phase, confirming the real `list_targets` scan is used, not the active-only helper. Legacy behavior is a pure read (proposal status + vote counts); native consensus targets already store participants/votes/status/resolution. Needs only a read-only descriptor + legacy branch + CLI + result encoder -- use the broker's `list_targets` directly, NOT `list_active_consensus_rounds()` (that helper deliberately excludes resolved rounds, which `proposal-list` needs to include). This is now the single best next "quick win" in the whole backlog.

**Rejected near-matches, with real reasons** (don't re-propose these as quick wins without new evidence):
- `lesson-inject` -- looked cheap (`list_active_lessons()` exists) but legacy also filters by minimum severity + workspace OS/shell/task-type applicability, then applies item/character limits and critical/sticky priority ordering. See `docs/design/HUB-REPLACEMENT-GAP8-LESSON-INJECT-2026-08-30.md` for the fully ratified design proposal.
- `proposal-add`/`proposal-vote` -- legacy voting accepts `ABSTAIN`/`NEED_MORE_INFO` and auto-resolves/escalates; native `ConsensusService.cast_vote()` only accepts `agree`/`disagree` and stops at quorum. Real semantic gap, not an alias.
- `health-precheck`/`check-gate`/`discover` -- the closest-sounding native methods (`freeze_admission_snapshot()`, `RoutingService.select_route()`) are MUTATING and persist decisions; the legacy actions are read-only inventory/ranking checks. Wrong mutability class, not a match.
- `peer-quarantine`/`peer-recover` -- native quarantine is evidence/policy-derived through `classify_and_open_circuit()`; legacy directly forces RED/GREEN state. Different authority model.
- `lease-status`/`lease-sweep` -- these are about OS PROCESS leases (PID liveness, process-tree termination), not room-role duty leases or execution-attempt session leases despite the name overlap. Genuinely different concept.
- `broker-submit`/`drain`/`status` -- legacy describes a deferred file-replacement queue (pending/done/error directories); the real `GovernanceBroker` does synchronous CAS-governed mutation + an effect outbox. Materially different lifecycle.
- Artifact/file-lock/role actions -- native artifacts are dispatch-attempt-scoped (die with the attempt); legacy file-locks and role assignments are durable, non-expiring records. Identity/lifecycle mismatch.
- `model-status` -- closest utility-level follow-up after `proposal-list`, but needs a small new native model-inventory read (the real `TelemetryPresenter.collect_live_snapshot()` doesn't have the authoritative model-pin/capability data the ledger requires) before it's wireable, not pure wiring today.

Recommended pickup order for whoever continues Tier 4: `proposal-list` (ready now) -> `model-status` (needs a small inventory-source decision first) -> `lesson-inject` (needs the applicability/rendering schema decision first) -> everything else, which needs real per-domain design work (health/routing, artifacts/locks, etc.), not quick wins.

**Correction (2026-08-28): `model-status` re-scoped up from "small inventory-source decision" to real design work.** Investigating it directly (not just re-reading the backlog's own summary) found the inventory it needs -- `PeerProfileBinding`, the sole owner of a peer instance's configured model ID and reasoning effort per `ARCHITECTURE.md` §6.1a -- is a fully *ratified* type (converged in `docs/design/peerhub-architecture-debate.md`, round 9) that was never actually implemented: no SQLite table, no service, `grep -rn "PeerProfileBinding" peerhub/` returns zero class definitions, only doc references and a contract.py comment pointing at it. So `model-status` needs a real new foundational SSOT component built (per an already-ratified spec, not a new design debate), not "small" wiring. Deferred, at the user's direction, in favor of continuing the Tier 4 survey into the still-fully-undesigned domains below.

**Update (2026-08-31, model-status / PeerProfileBinding planning): Ready for an implementation round.** **Implemented, DONE -- see the "Where things stand right now" section at the top of this doc (50/90).**
Research (ag.deepthink) confirmed the `PeerProfileBinding` schema and `model-status` legacy behavior (legacy `_sys/core/hub.py:11748-11773`).
The ratified Round 9 design (`docs/design/ARCHITECTURE.md` §6.1a, `docs/design/peerhub-architecture-debate.md` Round 9) mapped to native `TargetState`/service conventions yields a concrete, implementation-ready plan:
1. **TargetState Schema:** Create a new mutable target `peer-profile-binding:{node_id}:{profile_id}`. (It cannot be embedded inside `peer-node:{node_id}`'s state because its `revision` must be frozen per request attempt without racing node-level updates, matching its distinct conceptual ownership in `ARCHITECTURE.md` §4.1).
   - `kind`: `"peer-profile-binding"`
   - `scope`: `node_id` (so `list_targets("peer-profile-binding", node_id)` easily fetches a node's bindings)
   - `schema_version`: 1
   - State fields: `binding_id` (`target_id`), `node_id` (str), `profile_id` (str), `model_id` (str), `reasoning_effort` (str | None), `updated_at` (int), `updated_by` (str).
2. **Service Placement & Integration:** Add to `PeerRegistryService` (`peerhub/application/peer_registry.py`), keeping it adjacent to `peer-node`. Add three boilerplate broker methods: `bind_profile(*, node_id, profile_id, model_id, reasoning_effort=None, actor_id) -> MutationSubmission`, `get_profile_binding(node_id, profile_id) -> TargetState`, and `list_profile_bindings(node_id=None) -> Sequence[TargetState]`. `register_node` DOES NOT create bindings — it creates the node, `bind_profile` adds bindings (respecting `GovernanceBroker`'s single-target mutation bounds).
3. **model-status mapping:** Legacy `action_model_status` reads `orchestration.json` `hub_nodes` and `_peer_effective_health(peer)` (`hub.py:11760`). Native `model-status` (in `legacy.py`) will translate by calling `PeerRegistryService.list_nodes()`, then `list_profile_bindings(node.state["node_id"])` for each node, joining with the `HealthService` projection to replicate the legacy output (`peer`, `status`, `profile`, `model`, `effort`, `cost`, `context`, `capabilities`).
4. **Scope assessment:** This is small enough for one implementation round (matching `feedback.py`'s scope). It requires ~3 broker methods in `PeerRegistryService`, a new `BindProfileCommand` in `api.py`, CLI wiring (`peerhub node bind`), legacy translator wiring for `model-status`, and tests. No missing pieces or blocking ambiguities remain.


### Health / admission / routing scouting (2026-08-28): 11 previously-unscrutinized actions, 4 real gaps + 7 false friends, 0 quick wins

Research-only round (cx.deepthink; no implementation) covering the 11 members of this ~18-action cluster not already covered by the 7 rejections above (`health-precheck`, `check-gate`, `discover`, `peer-quarantine`, `peer-recover`, `lease-status`, `lease-sweep` -- those stand). Sample of the cited evidence independently re-verified by the terminal directly against source (not just trusted): `register_adapter_factory()`'s reject-on-conflict docstring, `HealthScopeMembershipSnapshot`'s "Injected immutable" docstring, and `DutyLeaseCoordinator.create_lease()`'s `InvalidMutationError("duty lease already active")` guard all check out exactly as cited.

**Meta-finding: peerhub has no node-registration/node-listing concept at all.** `HealthScopeMembershipSnapshot` (`health/contract.py:971`) is an *injected, immutable* membership set with no registration/listing service (can even be constructed empty); `RoutingService` (`routing/service.py`) consumes externally composed candidates and never discovers or mutates configuration; `register_adapter_factory()` (`adapters/registry.py:84`) registers Python adapter *implementations* at import/test time, is process-local and unlocked, and rejects replacing an existing registration -- none of these is a persisted configured-peer population. This means `register-node`, `list-nodes`, and the already-rejected `discover` share one real underlying gap, not three separate ones.

**REAL GAP (4):**
- `register-node` / `list-nodes` -- need a new shared `peer-registry:workspace` aggregate + `PeerRegistryService` (CAS-registered nodes, exact no-op detection for identical re-registration, order-preserving list). Open design question flagged by cx, not yet resolved: whether a registered node must reference an already-registered adapter kind/profile, or whether peerhub needs a constrained generic-command adapter -- storing arbitrary legacy `invoke` text alone wouldn't integrate safely with the current adapter model.
- `freshness-sweep` -- not health-state maintenance; legacy cadence-gates three external drift checks, records partial failures without aborting later ones, and can emit proposal artifacts. None of `HealthService`/`RoutingService`/the lease coordinators own external tool/config/policy drift checking. Proposed as an application-layer `FreshnessSweepCoordinator` over `GovernanceBroker.submit()`, independent of the registry/health work.
- `elect-leader` -- peerhub has no native leader-election concept at all (confirmed at `telemetry/presenter.py:541`). `RoutingService.select_route()` only does boolean eligibility with equal unit weights, not capability/effort scoring; `DutyLeaseCoordinator` has useful structural pieces (exclusivity, challenge metadata, monopoly guard) but creates an immediately-`ACTIVE`, heartbeat-expiring *room/role* lease, not a workspace-scoped leadership claim. Needs a `LeadershipService` + `LeaderElectionCoordinator`, and an explicit ratification of workspace-vs-room scope (legacy is workspace-global; native duty authority is room-scoped).

**FALSE FRIEND (3):** `assign-role`/`release-role`/`role-status` (legacy models durable, health-gated role assignments with warn-on-absent semantics; `DutyLeaseCoordinator` models expiring, fencing-epoch'd room/role leases with strict rejection semantics -- different lifecycle and identity model entirely; `role-status` becomes a small wiring item, but only after a real durable role-assignment domain exists). Note: `health-update`, `health-check`, `peer-status`, and `health-sweep` were previously considered false friends here but have been reclassified (see Update below).

**Recommendation (cx's, endorsed on review): don't schedule a wiring batch from this group yet.** Build the shared peer-registry core first (unlocks `register-node`/`list-nodes` directly and removes a stated prerequisite from `health-check`/`peer-status`/`health-sweep`/leader election) -- but that itself needs the adapter-kind-safety design question resolved first, and per this session's standing architecture-before-implementation rule, needs an independent critique pass before it's ratified, not just one peer's research. Durable role assignments (`assign-role`/`release-role`/`role-status`) is the next-smallest coherent domain after that. Leadership should follow only once registry + health-query + capability-scoring contracts exist. `freshness-sweep` is independent of all of the above.

**Update (2026-08-29): `register-node`/`list-nodes` and `assign-role`/`release-role`/`role-status` both DONE, in that order, exactly as recommended above.** See the "Where things stand right now" section at the top of this doc for both rounds' summaries. Remaining in this cluster: `freshness-sweep`/`elect-leader` (real gaps), and the 7 already-rejected items. Note: `health-update`/`health-check`/`peer-status`/`health-sweep` were investigated and documented in `docs/design/HUB-REPLACEMENT-GAP5-HEALTH-CLUSTER-2026-08-30.md` -- they are neither trivial nor entirely untranslatable, but blocked on core evidence-producer and read-time freshness gaps. Note 2 (2026-08-30): `elect-leader` and capability-matching were investigated but are explicitly BLOCKED and NOT READY for implementation pending 7 concrete design prerequisites (see `docs/design/HUB-REPLACEMENT-GAP6-CAPABILITY-MATCHING-2026-08-30.md`).

### `peer-registry` RATIFIED (2026-08-28): critique round (cc.deepthink) overturns cx's singleton, resolves the adapter-kind-safety question

Independent critique of cx's research above, per this session's standing research-then-critique discipline. Four of cc's citations independently spot-checked by the terminal directly against source (`InvocationPlan`'s docstring word-for-word, the three real adapters' single declared `ProfileDescriptor.profile_id` each, `resolve_peer_target`'s `ProfileNotFoundError` on an unmatched `profile_id`, `create_room`'s `expected_revision=0` CAS pattern) -- all checked out exactly as cited. hub.py itself was intermittently broken by an unrelated stalled Engram merge at the P:\ root sandbox during this round (unrelated to peerhub, fixed by the user's `git merge --abort`, see session notes) -- this critique dispatch is what confirmed the fix worked.

**Adapter-kind-safety question, resolved: reference an already-registered adapter kind + `profile_id`. No generic-command concept.** Traced legacy's real behavior end to end in `hub.py`/`hub_peer.py`: `register-node` writes only to `nodes.json`; `_load_nodes()` drops any custom node whose id collides with orchestration's `hub_nodes`/`retired`; dispatch gates on `is_routable()`, which returns `False` for exactly the set a custom node can occupy. So legacy's own `invoke`/`invoke_args`/`memory`/`timeout` fields are **write-only** in the shipped system -- never read by dispatch, `peer-status`'s live probe, or anywhere else (confirmed by grep). A "constrained generic-command" concept would be a net-new arbitrary-command-execution primitive legacy itself doesn't have, and has no seam in peerhub: `InvocationPlan.argv` is "mechanically generated from a resolved `PeerProfileBinding` + `AdapterRequest` … never independently specified by a caller" (peerhub/adapters/contract.py docstring, verified). Field disposition: `invoke` is **re-typed** into `peer_kind` via the existing `_CLI_ALIASES` vocabulary (`claude`/`codex`/`agy` -> `cc`/`cx`/`ag`) rather than dropped, since legacy's own `get_adapter()` already treats it as an adapter-kind selector, not a literal command; `invoke_args`/`memory`/`timeout` are rejected fail-closed (explicit error, not silently accepted-and-discarded) -- matching a real prior house precedent of failing closed on invented Claude-style `invoke_args`/`memory` defaults (`_sys/ai/backlog.json:568`); `tier` is stored display-only, explicitly with no authority (native capability grants are per-dispatch via `CapabilityGrantDecision`, never registry config); `node_type` is stored as validated free text (matching the existing `ArtifactSpec.access_mode`/`lifecycle` house pattern of not inventing an enum vocabulary), with `"virtual"` rejected since peerhub has no such adapter.

**Schema, corrected from cx's singleton sketch: one `peer-node:{node_id}` target per node, not a `peer-registry:workspace` singleton.** Two reasons: the parity ledger's own comparator for `list-nodes` says ordering is explicitly NORMALIZED/out of contract, so `node_order` isn't needed (`list_targets` already gives deterministic `ORDER BY target_id`); and a singleton is the minority pattern in this codebase (5 of 6 broker domains use per-entity targets) reserved for genuine cross-entity invariants like `arbiter-budget:workspace`'s shared count/window -- node registration has no such invariant, so a singleton would force an unnecessary CAS-retry loop and make `list_targets` unusable. State: `kind: "peer-node"`, `scope: None`, `schema_version: 1`, `node_id`, `peer_kind`, `profile_id`, `adapter_version` (snapshotted at bind time), `tier`, `node_type`, `registered_at`, `registered_by`, `updated_at` -- deliberately omitting `invoke`/`invoke_args`/`memory`/`timeout`/`node_order`. `list_nodes()` must union derived base rows for the `_CLI_ALIASES` roots (`cc`/`ag`/`cx`, matching the ledger's own "3 configured nodes" positive fixture) with registered rows, base winning on collision -- the faithful port of legacy's `{**custom, **base}` merge with `configured` taking precedence; `register_node` rejects a `node_id` colliding with a base adapter/alias rather than silently allowing a shadowed row.

**Placement:** `peerhub/application/peer_registry.py` (not `governance/`) -- validating `peer_kind`/`profile_id` needs `peerhub.adapters.registry`, which would pull real adapter implementations into the governance layer; `ArbiterReviewCoordinator` is the precedent for an application-layer service owning a governance target.

**CAS/idempotency, corrected from cx's sketch:** verified uniform across five real domains (rooms/lessons/tasks/consensus/arbiter_review) -- every service mints a fresh `request_id` per call as the idempotency key, making the broker's deterministic-idempotency-key path structurally unreachable; all real concurrency safety is `expected_revision` CAS (`0` on create, `current.revision` on read-modify-write). Do not invent a deterministic key for peer-registry -- that would make a *differing* re-registration raise `IdempotencyPayloadMismatchError`, contradicting legacy's actual semantics, which is a blind upsert (identical re-registration is a no-op only as a side effect of writing the same bytes back; a *differing* one silently overwrites). "No-op" should mean **state-stable re-submission** (matching `mark_read`'s real precedent: revision still increments, only the visible state stays byte-identical), not "no write happens at all." `duty_lease.create_lease`'s reject-on-conflict semantics are NOT valid precedent here -- it isn't even a `TargetState`/broker domain, and its behavior is backwards from legacy's upsert model.

**One real bug found and fixed in passing, unrelated to peer-registry:** `arbiter_review.py`'s `_DEFAULT_PROFILE_ID` was `"cc.deepthink"`, which does not exist as a declared profile on the real Claude adapter (only `"cc.standard"` is declared) -- every enabled arbiter review would have failed with `ProfileNotFoundError`, latent only because the arbiter ships disabled by default. Fixed same-day (commit `1879970`), independently reproduced and verified (full suite 1151 passed, 0 new pyright errors).

**Two items recorded, neither blocking implementation:** the `fix-register-node-pos-01` ledger fixture (`--invoke python`) is now a stated, deliberate parity break (rejected rather than accepted) and should be annotated in the ledger as a deviation, not left to be discovered as a future test failure; `telemetry/presenter.py` independently uses `cx.deepthink`/`ag.deepthink` display-profile names that also don't match any real declared `ProfileDescriptor.profile_id` -- flagged as a separate, not-yet-investigated question (is the telemetry dashboard's profile vocabulary supposed to diverge from the adapter registry's, or is this the same class of bug as the arbiter default -- undetermined, needs its own look before assuming either way).

**Ready to hand to an implementation round.** Service signatures ratified: `PeerRegistryService.__init__(broker, *, clock, ids)`, `register_node(*, node_id, peer_kind, profile_id=None, tier=4, node_type="agent", actor_id) -> MutationSubmission`, `get_node(node_id) -> TargetState`, `list_nodes() -> Sequence[TargetState]`.

**Update (2026-08-29): DONE, exactly as ratified above.** See the "Where things stand right now" section at the top of this doc for the implementation round's summary. `register-node`/`list-nodes` now translate and execute end-to-end (39/90).

## Tier 5 — Pre-TDD-phase backlog (from the README, predates this session's TDD work, still real)

These were never part of the gap-1..7 design/TDD tracking; they're infra/ops-level items with their own already-stated deferral triggers, carried forward here so they don't get lost:

- **Health/quota tracking's periodic background polling (`TelemetryWorker`)** — awaiting a user decision on the process-host model (poll-on-demand vs. daemon).
- **Windows-native Brokered Read-Only Reducers** — blocked pending a policy call on required OS privileges.
- **Alembic runtime cutover** — ratified HOLD; revisit only if peerhub adopts SQLAlchemy ORM or is about to become the primary dispatch path.
- **Formal multi-peer consensus voting machinery (Primitive B)** — deferred until the first `r10_requires_finalized_for` decision class is actually routed to peerhub. (Note: `ConsensusService` now built this session covers propose/vote/resolve for peerhub's OWN governance rounds — this deferred item is about a *different* thing, formal Primitive B voting machinery for external decisions; don't conflate the two when picking this up.)
- **Durable response transcripts for broadcast** — deferred until a dispatch-layer durability mechanism is ratified.
- **Capability-lease enforcement evidence** (claiming positive enforcement in adapter receipts) — deferred until a machine-owned launcher, plan-bound digest, empirical negative probe, and post-plan corroboration gate all exist.
- **Parallel fan-out** — deferred, blocked on measuring real SQLite write contention.
- **Phase 4 shadow-by-ownership-cluster validation** + same-revision comparison/rollback proof — not started.
- **Crash-linkage recovery** (resuming an interrupted round after a coordinator crash) — not started.
- **Detailed per-vendor error-taxonomy mapping, PTY transport** — deliberately out of scope for the current adapter slice.
- **Shadow-mode validation** (routing a subset of real traffic through peerhub alongside `hub.py` for comparison before cutover) — not started; `hub.py` remains the authoritative system for real multi-peer coordination work today.

## Known drift caught while writing this doc

The README's "24 of the 90 ... action count" line needed a correction during the last implementation round — a running tally had silently drifted stale (stuck at "18" for a few rounds after a commit had already pushed it to 19) because updates were manual and scattered across README + the TDD progress doc + inline commit messages. This backlog doc doesn't eliminate that risk by itself, but centralizing "what's left" in one place makes drift easier to catch on the next audit pass. If updating this doc, prefer running `grep -c "if call.action ==" peerhub/application/legacy.py`-style direct verification over incrementing a remembered number.

Also stale, not yet fixed: README line "None of this design work has been implemented yet" (under the "hub.py-replacement design phase" paragraph) — written before the TDD phase started and now inaccurate; most of the design work it refers to (gap-2, gap-4, gap-5, gap-6, half of gap-3) has since been implemented. Needs a README pass, not fixed as part of writing this backlog doc.


### Host-integration / Tooling scouting (2026-08-30): 5 actions scoped, 5 false friends, 0 real gaps

Research-only round (ag.deepthink; no implementation) covering the 5 actions previously flagged as potentially host-level specific (`directive-add`, `directive-list`, `directive-clear`, `credit-status`, `credit-consume`).

**Meta-finding: None of these 5 actions belong in peerhub's domain model.** They are all host-environment integrations that manipulate either local uncoordinated files for the P: environment or upstream proprietary billing/quota tokens.

**REJECTED FALSE FRIENDS (5):**
- `directive-add`, `directive-list`, `directive-clear`: These read and append to `_sys/ai/runtime-directives.jsonl` (as specced in `docs/design/PHASE1-PARITY-LEDGER-BATCH4-2026-08-20.md`, lines 212-242). This is a local, lock-free, unstructured file used by the host agent environment to inject transient prompt instructions. Peerhub already has a robust, coordinated, consensus-governed mechanism for prompt injection: **Lessons** (`lessons-propose`, `lesson-inject`, backed by SQLite `command_ledger`). Replacing `runtime-directives` within peerhub would redundantly recreate lessons. These remain local host-tooling actions.
- `credit-status`, `credit-consume`: These interact with `CodexAccountClient` to read and irreversibly consume proprietary "rate-limit reset credits" on an upstream service API (`docs/design/PHASE1-PARITY-LEDGER-BATCH5-2026-08-20.md`, lines 313-350). While peerhub *observes* quotas (via read-only polling in `telemetry/quota_polling.py` to inform `RoutingService`), actively *redeeming upstream account billing/quota tokens* is a highly peer-specific proprietary host integration. Peerhub coordinates peers; if a peer's quota is exhausted, peerhub routes around it. Redeeming a token to wake the peer up is an upstream account management action, not a peer coordination concept.

### Locks (`file-lock`/`file-unlock`/`lock-status`) RATIFIED (2026-08-30): a genuine new durable lock domain, NOT built on DutyLeaseCoordinator

Research (ag.deepthink) against real Parity Ledger entries (`docs/design/PHASE1-PARITY-LEDGER-BATCH4-2026-08-20.md` §§4-6) confirms legacy file-locks are durable and non-expiring. They track a simple `locked_at` timestamp, do not require heartbeats, and are explicitly idempotent upon re-entry by the same owner (preserving the original timestamp). `DutyLeaseCoordinator` is strictly for room-scoped, TTL-expiring, heartbeat-renewed leases with automatic recovery. Forcing file-locks into `DutyLeaseCoordinator` would be the exact same "wrong reuse" trap that broke leadership. A new, distinct lock domain is genuinely needed.

**Placement**: `peerhub/governance/file_locks.py` (a workspace-global coordination registry, not an application-layer routing concern like duty leases).

**Schema**: A per-resource `file-lock:{name}` TargetState family.
State: `name` (the locked resource), `owner` (the holding peer/agent identity), `scope` (default "file"), `locked_at` (timestamp, preserved on idempotent re-entry), and `status` (`ACTIVE|RELEASED`).

**Methods**:
- `lock_file(name: str, owner: str, scope: str = "file")`: CAS on `file-lock:{name}`. If absent or `RELEASED`, create/update as `ACTIVE` with `locked_at = now`. If `ACTIVE` and `existing.owner == owner`, return existing unchanged (strictly idempotent, preserves original `locked_at`). If `ACTIVE` and `existing.owner != owner`, raise a lock conflict error.
- `unlock_file(name: str, owner: str | None = None)`: CAS on `file-lock:{name}`. If absent or `RELEASED`, no-op/warn. If `ACTIVE` and `owner` specified but `existing.owner != owner`, raise an ownership mismatch error. Otherwise, mutate to `status="RELEASED"`.
- `list_active_locks()`: Scans `file-lock:*` targets for `ACTIVE` locks to format the TSV output for `lock-status`.

**Cross-domain impact on `status`**:
Building this locks domain will unblock the locks-reporting portion of the legacy `status` action. However, as noted in this document's configuration/telemetry scouting section, `status` is a genuine multi-domain aggregation read (rolling state/mailbox/task-registry/locks/consensus into one report). While locks are a prerequisite, `status` will still require its own dedicated cross-domain wiring and design work once all its dependencies exist.

#### Critique (cc.deepthink, 2026-08-30): core model CONFIRMED against real source, but NEEDS REVISION on four points before implementation

Independent read-only verification pass. Every citation below was re-derived directly from `P:/_sys/core/hub.py` source, not from the parity ledger; where the ledger and source disagree, source wins and the disagreement is called out. The handlers are `action_file_lock` (`P:/_sys/core/hub.py:11391-11405`), `action_file_unlock` (`P:/_sys/core/hub.py:11407-11424`), `action_lock_status` (`P:/_sys/core/hub.py:11426-11435`), backed by `_file_locks_path` -> `ai_root/file_locks.json` (`P:/_sys/core/hub.py:8667-8668`).

**1. Load-bearing legacy claims: four CONFIRMED, one CONFIRMED-WITH-CORRECTION.**

- **No TTL / no expiry -- CONFIRMED, exhaustively.** An unrestricted grep for `file_locks` across all ~12,000 lines of `hub.py` returns exactly five sites: the path helper (`:8667-8668`), the three handlers' uses of it (`:11395`, `:11411`, `:11427`), the `file_locks` filelock resource name (`:11396`, `:11412`), and the `status` read (`:1711`). There is no sweeper, no expiry field, no renewal action, and no other reader anywhere. Reinforcing evidence the research pass did not cite: `new-topic`/`clear-room` clear `state["blocked"]` (`P:/_sys/core/hub.py:3560-3567`, `P:/_sys/core/hub.py:3592-3599`) but never touch `file_locks.json` -- locks survive room resets. They are genuinely workspace-global and durable, not room-scoped.
- **No heartbeat / no crash recovery -- CONFIRMED.** No renew path exists. `lease-status`/`lease-sweep` operate on the separate `leases` filelock resource (`P:/_sys/core/hub.py:579`) and never reach `file_locks.json`. The "not `DutyLeaseCoordinator`" conclusion is correct and well-founded.
- **Immediate rejection, not a queue, on conflicting owner -- CONFIRMED.** `P:/_sys/core/hub.py:11399-11401`: `if existing and existing.get("owner") != owner:` -> stderr + `sys.exit(1)`. No wait, no queue, no steal, and no force/override flag on the *lock* side.
- **Idempotent same-owner re-entry preserving `locked_at` -- CONFIRMED on the timestamp, REFUTED on "return existing unchanged".** `P:/_sys/core/hub.py:11402` is `data[name] = {"name": name, "owner": owner, "scope": scope or "file", "locked_at": existing.get("locked_at") if existing else _now()}`. The `locked_at` preservation is exactly as described. But the record is *unconditionally rewritten*, so `scope` is last-write-wins: the same owner re-locking with a different `--scope`/`--section` **updates the scope** while keeping the original `locked_at`. The candidate's "return existing unchanged (strictly idempotent)" would silently drop that scope update. The ledger's "yielding identical state" (`docs/design/PHASE1-PARITY-LEDGER-BATCH4-2026-08-20.md:64`) is only true for a byte-identical request and should not be read as "no write occurs". **Revision required** (see 6.3).
- **Exact `lock-status` TSV -- CONFIRMED, and pinned precisely here** (`P:/_sys/core/hub.py:11426-11435`). Empty registry *or* missing file: a single stdout line `No active file locks.`, exit 0 -- **not** a bare header. Non-empty: header line `name<TAB>owner<TAB>scope<TAB>locked_at`, then one row per lock built as `f"{name}\t{item.get('owner','')}\t{item.get('scope','')}\t{item.get('locked_at','')}"` -- four columns, that order, missing fields rendered as the empty string. Iteration is `data.items()`, i.e. JSON insertion (acquisition) order, not sorted. Read is lock-free.

**2. Placement in `peerhub/governance/file_locks.py` -- CONFIRMED, on three independent grounds.** (a) The legacy handler reads *only* `file_locks.json`: no `nodes.json`, no health evidence, no `state.json`. There is literally nothing to compose, so the domain is self-contained by construction. (b) `LEGACY_CATALOG` already records the method namespace as `governance.lock.acquire` / `governance.lock.release` / `governance.lock.status` (`peerhub/application/legacy.py:187-189`) -- the same "recorded namespace matches placement" argument `FeedbackService`'s own module docstring makes (`peerhub/governance/feedback.py:1-9`). (c) Locks are workspace-global and survive room resets, so they are not an application-layer routing/room concern. It does **not** need `PeerRegistryService` -- see 3.

**3. Bare `owner: str` -- CONFIRMED; registry validation REFUTED.** Legacy's only owner validation is non-emptiness (`P:/_sys/core/hub.py:11392`). The CLI supplies `args.peer or args.agent or ""` (`P:/_sys/core/hub.py:12226`), and `--agent` (`P:/_sys/core/hub.py:11912`) is unconstrained free text never cross-checked against `nodes.json` or any registry. `LeadershipService` validates its leader claim because *legacy leadership actually performs that check*; the lock handler performs no such check, so adding one would be an invented narrowing that rejects legitimate legacy callers -- the same reasoning this session already applied to `node_type` and to feedback's `status` (line 185 above). Use `require_text(owner, "owner")` and stop there. The same holds for `scope`: free text, not an enum. One concrete trap: legacy normalizes with `scope or "file"`, and the CLI passes `""` (`P:/_sys/core/hub.py:12226`, `args.scope or args.section or ""`), so a bare Python default `scope: str = "file"` is **insufficient** -- an explicitly-passed empty string must also normalize to `"file"`.

**4. CAS-without-retry -- CONFIRMED.** `file-lock:{name}` is per-resource, and the only callers that can contend are ones targeting the same named resource -- precisely the case legacy resolves by *rejecting* rather than merging. Contrast the two sanctioned retry exceptions: `operational-error-series` and `room-alert:{room_id}` are shared aggregate/singleton slots where multiple *unrelated* callers legitimately race and legacy's state lock lets every one of them commit, so an unretried CAS loss would silently drop a report or an alert (lines 174 and 246 above). Nothing is lost here: two racing writers on one lock name are either the same owner (the lock ends up correctly held either way) or different owners (one *must* lose, by design). This matches the per-entity `role_assignment`/`feedback`/`peer_registry` precedent exactly. **One honest asymmetry to record rather than paper over**: a same-owner *concurrent* double-lock, which legacy serializes so both exit 0, will surface `StaleRevisionError` to one caller in peerhub. That is the identical accepted deviation `role_assignment` already carries; document it, do not add a retry loop to hide it.

**5. Unlock semantics -- CONFIRMED, and there IS an admin-override path that the design reproduces but never states.**

- Absent lock: `[HUB:WARN] {name} is not locked` on **stdout**, bare `return`, **exit 0** (`P:/_sys/core/hub.py:11414-11416`). Confirmed.
- Explicit owner mismatch: exit 1, `[HUB:ERROR] {name} is locked by {existing.get('owner')}, not {owner}` (`P:/_sys/core/hub.py:11418-11419`). Confirmed.
- **The override, answering the critique question directly:** the guard is `if owner and existing.get("owner") != owner`. `owner` defaults to `""` and the CLI passes `args.peer or args.agent or ""` (`P:/_sys/core/hub.py:12228`), so `file-unlock --name X` with **no `--peer`/`--agent`** unconditionally force-releases *anyone's* lock, exit 0, with no authority check and no audit trace. So: an unrelated peer supplying its own identity is always rejected, but any caller willing to omit the identity flag can force-unlock. It is not a `--force` flag -- it is the absence of an argument, which makes it trivially reachable. ag's `owner: str | None = None` reproduces this correctly, but because the design text never calls it out, an implementer could very plausibly "harden" it into always-require-owner and break parity. **State it explicitly, with a dedicated test**: `unlock_file(name, owner=None)` against another peer's ACTIVE lock must succeed. Recommended strictly-additive improvement: since peerhub carries `actor_id` on every mutation (legacy does not), record the forcing actor as `released_by` in the RELEASED state -- parity-preserving, and it closes legacy's audit hole.
- **REFUTED as a legacy fact: there is no `RELEASED` state in legacy.** Unlock is `data.pop(name, None)` (`P:/_sys/core/hub.py:11421`) -- the entry is *deleted*. `lock-status` has no status filter because released locks do not exist to filter. The RELEASED tombstone is an invention. It is a *defensible* one (it matches `role_assignment`'s RELEASED-but-still-queryable precedent, and peerhub's target store is not delete-oriented), but the design must label it as a deliberate deviation and carry its three consequences: (i) `list_active_locks()` **must** filter `status == "ACTIVE"` or `lock-status` prints stale rows legacy could never print -- the design does say this, correctly; (ii) re-locking a RELEASED name must reset `locked_at = now`, because legacy's re-lock after unlock sees `existing is None` and takes the fresh `_now()` branch -- the design says this, correctly; (iii) `status`'s `**Locks**: N active` line (`P:/_sys/core/hub.py:1711-1714`) counts `len(locks)` over a registry in which every entry is by definition active, so peerhub must source that count from the ACTIVE-filtered set, not the raw target count -- and must suppress the line entirely when the count is zero, matching legacy's `if locks:`.

**6. Gaps and defects found.**

1. **BLOCKING -- the state schema omits `kind`, so `list_active_locks()` would silently return nothing.** `target_kind` is derived from `state["kind"]` and `target_scope` from `state["scope"]` at persist time (`peerhub/persistence/sqlite_governance.py:101-104`), and `list_targets` filters on the `target_kind` column (`peerhub/persistence/sqlite_governance.py:60-81`). The proposed state lists only `name`/`owner`/`scope`/`locked_at`/`status` -- with no `"kind": "file-lock"`, every target persists with `target_kind = ""` and `broker.list_targets("file-lock", None)` returns an empty tuple. `lock-status` would report `No active file locks.` forever, with no error. Every sibling domain includes it: `role_assignment.py:217-219`, `operational_errors.py:187-189`, `alert_raise.py:94-96`. **Add `"kind": "file-lock"` and `"schema_version": 1`.**
2. **BLOCKING -- the domain's `scope` field name collides with the persistence partition key.** Per the same `sqlite_governance.py:102-104` derivation, putting legacy's free-text lock scope (`"file"`, or an arbitrary `--section` value) into `state["scope"]` writes it into the indexed `target_scope` partition column. Every sibling either sets a genuine coordination scope (`alert_raise`: `room_id`) or `None` (`feedback`, `role_assignment`, `operational_errors`). Locks are workspace-global and have no partition scope. **Set `"scope": None` and carry legacy's value under a distinct key such as `"lock_scope"`, mapping it back to the `scope` TSV column at presentation time.**
3. **Real parity miss -- `lock_file` cannot early-return-unchanged on same-owner re-entry.** Per finding 1 above, legacy updates `scope` on re-lock while preserving `locked_at`. The design's "return existing unchanged" makes a same-owner scope update a silent no-op. Re-submit when the normalized scope differs (preserving `locked_at`), or simply always re-submit with the preserved `locked_at`.
4. **Unstated-but-load-bearing -- the ownerless force-unlock** (finding 5). Must be written into the design text so it survives implementation.
5. **`list_active_locks()` ordering diverges, but is already sanctioned.** peerhub's `list_targets` is `ORDER BY target_id ASC` (`peerhub/persistence/sqlite_governance.py:66-69`), i.e. sorted by `file-lock:{name}`; legacy iterates acquisition order. The ledger already declares the comparator `NORMALIZED (ISO timestamps, lock entry iteration ordering)` (`docs/design/PHASE1-PARITY-LEDGER-BATCH4-2026-08-20.md:104`), so name-ASC is **within** the sanctioned normalization -- but say so in the design rather than leaving it to be discovered as a fixture failure, per the `fix-register-node-pos-01` lesson (line 305 above).
6. **`locked_at` format is host-local naive time in legacy.** `_now()` is `datetime.now().strftime("%Y-%m-%dT%H:%M:%S")` (`P:/_sys/core/hub.py:1123-1124`) -- no timezone, second granularity. Take UTC from the injected `Clock` per house convention (line 185 above), and record it as a deliberate deviation; the ledger's comparator normalizes ISO timestamps anyway.
7. **`name` is a path and must be stored verbatim.** The ledger's own fixtures use `docs/spec.md` and `src/main.py`, and the CLI accepts `--name` or `--file` (`P:/_sys/core/hub.py:11934-11935`). `file-lock:docs/spec.md` is safe as a `target_id` -- IDs are opaque and `kind` is a separate column, nothing splits on `:` -- but note explicitly that legacy performs **no** path normalization, so `docs/spec.md`, `docs\spec.md`, and `./docs/spec.md` are three distinct locks. Do not "improve" this on a Windows-hosted workspace; normalizing would be a parity break.
8. **Return shapes are unspecified, and peerhub services do not print.** The "no-op/warn" wording has no peerhub equivalent. Follow the `RoleReleaseResult`/`RoleReleaseDisposition` precedent (`peerhub/application/role_assignment.py:24-33`): return a disposition (`RELEASED` / `NOT_LOCKED`) so the CLI emits `[HUB:WARN] {name} is not locked` at exit 0. Likewise `lock_file` should distinguish ACQUIRED from RE-ENTERED even though both print the identical `[HUB] FILE-LOCK {name} | owner={owner}` line.
9. **Error types unspecified.** Do not reuse `ExclusiveClaimConflictError` -- it is outbox-specific and its constructor takes an `event_id` (`peerhub/core/errors.py:458-476`). Define a domain `FileLockConflictError(InvalidMutationError)` following the `RoleAssigneeUnavailableError` precedent (`peerhub/application/role_assignment.py:36`), and use plain `InvalidMutationError` for the unlock ownership mismatch, matching `role_assignment.py:269,276`.
10. **No scope creep found.** The design invents no queue, no steal, no TTL, no priority, and no lease semantics. It is appropriately minimal; the only genuine invention is the RELEASED tombstone, which is justified.

**Overall verdict: NEEDS REVISION -- small and mechanical, no further research round required.** The core model is right and independently confirmed against real source: durable and non-expiring, per-resource targets, no-retry CAS, `governance/` placement, and a bare unvalidated `owner` string. Two defects would produce real bugs if implemented as written (missing `kind` silently empties `list_active_locks()`; `scope` overloads the persistence partition column), one is a genuine parity miss (same-owner re-lock must update `scope`), and one is an unstated but load-bearing legacy behavior (the ownerless force-unlock). Fix those four in the design text -- plus the smaller items 5-9 as implementation notes -- and this is ready to hand to an implementation round.

**Implementation update (2026-08-30): DONE.** Implemented in `peerhub/governance/file_locks.py`, `peerhub/application/api.py`, `peerhub/application/legacy.py`, and `peerhub/cli.py` exactly per the critique's fixes (hard-delete on release, same-owner lock scope update, unstated admin-override, disposition enums, domain-specific errors). Verified end-to-end with tests (`tests/integration/test_file_locks.py`). The strictly-correct `LEGACY_CATALOG` count advances from 46/90 to 49/90 (`file-lock`, `file-unlock`, `lock-status`).

### Manual-quarantine authorization policy REVISED (2026-08-30)

**Scope Correction:** This revised proposal addresses the deadlock specifically for manually-quarantined circuits. In accordance with the critique, we are explicitly removing the scope expansion that would have bypassed `COOLDOWN` for automatic circuits. The `authorize_administrative_recovery()` method will exclusively target non-automatic `QUARANTINED` circuits (i.e., those with `MANUAL` or higher authority class). Automatic circuits will continue to use the existing `authorize_recovery()` path unchanged.

**Design Updates:**

1. **Explicit Precedence Ladder Logic:**
   The `QuarantineAuthorityClass` enum does not natively provide dominance ordering. We will introduce a new pure comparison function in `peerhub/health/model.py` (e.g., `def dominates(a: QuarantineAuthorityClass, b: QuarantineAuthorityClass) -> bool:`) that explicitly encodes the hierarchy: `AUTOMATIC < MANUAL < POLICY < SECURITY`. This freezes the authority matrix rather than relying on enum declaration order.

2. **Durable Administrative Authorization Marker and Recomputation Fix:**
   To resolve the blocking bug where `_aggregate_for_pair` in `peerhub/health/service.py` reverted the projection, `RecoveryProbeGrant` will be extended to store `authorization_mode` (e.g., `AUTOMATIC` vs `ADMINISTRATIVE`) and `authorized_circuit_revision`. 
   The `valid_live_grant` check in `_aggregate_for_pair` must be modified to:
   - Require `live_grant.authorized_circuit_revision == circuit.revision`.
   - Treat the grant as `PROBE_AUTHORIZED` if it's an `AUTOMATIC` grant on a `RECOVERY_REQUIRED` circuit, **or** if it's an `ADMINISTRATIVE` grant on a `QUARANTINED` circuit. This explicitly instructs the recomputation logic to respect the administrative bypass and not silently revert to `QUARANTINED`.

3. **Authenticated Authorization Mechanism:**
   We will remove the caller-asserted `target_authority_class` string parameter. Instead, `authorize_administrative_recovery()` will accept an `AuthenticatedSubject` (from `peerhub/core/identity.py`) and a typed authority capability object (e.g., `QuarantineAuthorityCapability`) issued from a trusted application boundary. The capability maps the verified principal to a permitted `QuarantineAuthorityClass`. This provides real authorization matching the precedent set by role assignment and leader election, rejecting unsupported claims fail-closed.

4. **Budget Window and Per-Circuit Pacing:**
   The budget will be implemented as an anchored 5-hour window via an `AdministrativeRecoveryBudgetManager` (mirroring `ArbiterBudgetManager`), not just a loose limit field.
   - **Exhaustion:** If the budget is exhausted, the method will reject the attempt with a clear `budget_exceeded` error (no silent failures).
   - **Per-Circuit Pacing:** We will add a per-circuit single-flight lease/unresolved-attempt rule. If an administrative probe fails, a specific administrative cooldown (separate from the budget) must elapse before another administrative authorization can be attempted on that same circuit, effectively blocking operators from spam-retrying a failing probe.

5. **Shared Probe Pipeline:**
   `authorize_recovery()` and `authorize_administrative_recovery()` will remain distinct public APIs to prevent accidental sweeping. Both will delegate to a shared internal helper for transaction atomicity, fault injection, and grant/projection construction, avoiding duplicated core logic while keeping the entry points secure.

#### Final Verification (cx.deepthink, 2026-08-30)

Independent verification verdict: **REVISION STILL REQUIRED BEFORE RATIFICATION.** The revision genuinely fixes three of the five findings, but the two remaining gaps are authorization and failure-safe pacing -- the controls that make this an administrative security boundary rather than merely another typed call. This is a focused design correction, not a request for another research round.

1. **CONFIRMED -- explicit `dominates(a, b)` is a sufficient precedence fix.** `QuarantineAuthorityClass` remains an unordered string enum (`peerhub/health/contract.py:157-163`), so a pure function backed by an explicit, exhaustive rank map is the correct way to freeze the newly chosen `AUTOMATIC < MANUAL < POLICY < SECURITY` policy without depending on declaration order. The implementation must use the unambiguous direction `dominates(capability.authority_class, circuit.quarantine_authority_class)`, fail closed for non-enum inputs, and test every pair. This is a new cross-class policy choice rather than a fact supplied by the enum; with that choice now explicit, the original mechanical objection is resolved. The older frozen rule only says non-automatic quarantines require their designated authority (`docs/design/PHASE1-KICKOFF-R1.md:56-67`), so those pairwise tests remain load-bearing.

2. **CONFIRMED -- the grant marker resolves the projection-recomputation deadlock, provided the existing fences are retained.** The named code is real: `_aggregate_for_pair()` obtains the sole live grant and currently recognizes it only when the circuit is open, its receipt matches, and cooldown evaluates to `RECOVERY_REQUIRED` (`peerhub/health/service.py:558-601`); `_recompute_pair()` then installs that aggregate result over the reducer seed (`peerhub/health/service.py:681-743`). Adding a typed authorization mode plus the circuit revision captured at authorization lets the aggregate accept `ADMINISTRATIVE + QUARANTINED` while preserving the existing open-circuit and receipt-equality checks. Circuit changes increment `HealthCircuitSnapshot.revision` (`peerhub/health/model.py:487-505`, `peerhub/health/model.py:644-663`), so the revision equality invalidates an administrative grant after escalation or any probe result. This fixes the original multi-member recomputation failure rather than relocating it. Migration/persistence must add both fields and treat pre-migration grants conservatively; the claimed-but-unresolved lifecycle is a separate, still-open issue under point 4.

3. **REFUTED -- `AuthenticatedSubject` plus a named capability does not yet constitute real authorization.** `AuthenticatedSubject` carries only `principal_id` and `evidence_source`; its provider protocol resolves identity, and `require_authenticated_subject()` checks only the Python type (`peerhub/core/identity.py:14-40`, `peerhub/core/identity.py:69-96`). It carries no roles, authority classes, or resolver hook. A repository-wide search finds no `QuarantineAuthorityCapability` definition outside this proposal. The cited role-assignment and leadership precedents do not supply one: both accept a free-text `actor_id`; role assignment resolves the *target peer* and health, while leadership resolves the candidate node (`peerhub/application/role_assignment.py:153-180`, `peerhub/application/leadership.py:455-486`). The revision therefore replaces one caller assertion with an unspecified caller-supplied object. Before ratification it must define an injected trusted issuer/policy (or equivalent validator), the capability's principal/evidence/class/issuer-or-receipt binding, the exact subject-to-capability validation, supported issuers/classes, and the audit fields persisted on the grant. Unsupported `POLICY`/`SECURITY` authority must remain fail-closed.

4. **REFUTED -- the budget duration is now fixed, but the reservation and per-circuit attempt contracts remain underspecified.** The anchored five-hour window and `budget_exceeded` result are concrete improvements, and the numeric limit is a real positive `HealthPolicy` field (`peerhub/health/contract.py:190-205`, `peerhub/health/contract.py:258-265`). However, "mirroring `ArbiterBudgetManager`" does not choose the required sequencing across two persistence domains. The real arbiter manager reserves by a 16-attempt broker CAS and explicitly transitions `RESERVED` to `CONSUMED`, `RELEASED`, or `UNKNOWN` (`peerhub/application/arbiter_review.py:242-317`, `peerhub/application/arbiter_review.py:319-415`); the revision does not say when administrative recovery reserves/consumes/releases/marks unknown relative to the health-unit-of-work grant commit, or whether the budget instead becomes a health-owned row in that same transaction. The per-circuit rule is also only a goal: today the unique index covers an *unconsumed* grant (`peerhub/persistence/migrations/0006_recovery_probe_single_flight.sql:1-10`), `get_live_recovery_probe_grant()` stops finding it immediately after claim (`peerhub/persistence/sqlite_health.py:576-594`), and no claimed-probe expiry/abandon path exists. Ratification still needs the lease key/state, start and expiry anchors, timeout, success/failure/NOT_STARTED/uncertain transitions, abandoned-claim recovery, and the source/duration of the post-failure administrative cooldown. Without those rules, concurrent authorization after claim and permanent lockout versus premature retry are both valid implementations of the prose.

5. **CONFIRMED -- scope is now correct.** Non-automatic open circuits deterministically evaluate to `QUARANTINED` with no retry boundary, while only `AUTOMATIC` circuits follow `COOLDOWN` to `RECOVERY_REQUIRED` (`peerhub/health/model.py:694-735`). The existing public authorization method admits only `RECOVERY_REQUIRED` (`peerhub/health/service.py:1081-1133`). Restricting the new administrative entry point to non-automatic `QUARANTINED` circuits and leaving automatic `COOLDOWN` on `authorize_recovery()` fixes the earlier scope expansion exactly.

**Status: NOT YET RATIFIED.** Close points 3 and 4 by specifying the authority issuer/validator and the reservation/attempt-lease state machines. Points 1, 2, and 5 are ready as written; the distinct public entry points plus shared health-transaction helper remain the right implementation shape.

### Manual-quarantine authorization policy -- final resolution attempt (2026-08-31)

**Gap 1: Single-operator premise**
FINDING: The single-operator premise is TRUE. Investigation of `peerhub/core/identity.py` confirms that the system models identity via `AuthenticatedSubject`, which is populated by `LocalProcessCallerIdentityProvider` using the OS process owner (`psutil.Process().username()`). There is no concept of multiple distinct human principals with different privilege levels or RBAC roles anywhere in the identity model. The caller is simply "the authenticated local operator".

RESOLUTION: Gap (1) is cleanly resolved by this reframing. We can simplify `authorize_administrative_recovery()` to require only a valid `AuthenticatedSubject` as proof of authorization, eliminating the need for a separate `QuarantineAuthorityCapability`. The `QuarantineAuthorityClass` (AUTOMATIC < MANUAL < POLICY < SECURITY) will strictly describe the properties of the *quarantine itself* (e.g., higher-class quarantines cannot be lifted by lower-class administrative overrides) rather than the caller's privileges. Since there is only one operator class, any authenticated operator is permitted to spend from the administrative recovery budget. The design is now RATIFIED, READY FOR IMPLEMENTATION.

**Gap 2: Budget/lease lifecycle**
FINDING: For a short-lived, single-use probe grant, a full heartbeat model (like `DutyLeaseCoordinator`) is unnecessary overhead. The pattern that fits best is a fixed-timeout single-flight lease.

RESOLUTION: The `RecoveryProbeGrant` will move through the following concrete state transitions:
1. `GRANTED`: Created with a fixed expiration timestamp (e.g., `now + 5_minutes`). This serves as the single-flight lease for the circuit.
2. `CLAIMED`: A worker atomically CAS-updates the grant to `CLAIMED` when starting the probe.
3. `SUCCEEDED` / `FAILED`: The worker applies the probe result, consuming the grant permanently and updating the circuit state.
4. `EXPIRED`: If a worker crashes between `GRANTED`/`CLAIMED` and execution, the expiration timestamp will elapse. A new grant cannot be issued while an unexpired one exists, preventing parallel probe spam. Once expired, the grant is considered exhausted (failing closed) and the circuit remains quarantined. The operator must request a new authorization (spending another budget slot) to retry. This provides safe partial-failure recovery without requiring resume mechanisms.
