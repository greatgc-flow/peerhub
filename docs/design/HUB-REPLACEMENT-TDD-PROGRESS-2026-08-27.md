# hub.py-Replacement TDD Progress (2026-08-27, overnight session)

> Status doc, not a design doc — records what's actually implemented and tested, as of the commits listed below, so anyone picking this up doesn't have to reconstruct it from git log. Supersedes nothing; `HUB-REPLACEMENT-PRE-TDD-FINAL-RATIFICATION-2026-08-26.md` remains the design-closure record. For "what's left, organized by how ready it is to pick up," see `docs/design/PEERHUB-BACKLOG-2026-08-27.md` instead — that's the consolidated backlog, this doc stays the chronological implementation log.

## Execution dispatcher investigation

Investigation completed before implementation (2026-08-27). The README's typed boundary is real: `peerhub/application/api.py` defines `ApplicationAPI`, and `peerhub/client.py` defines `Client`. `Client.submit(Command)` reconstructs a strict `CommandEnvelope` and calls `ApplicationAPI.submit()`. `ApplicationAPI` has a real `method -> CommandDescriptor` registry, performs protocol/method/availability/idempotency/decode/auth validation, then invokes the descriptor's `handle` callable and encodes its result.

That dispatcher is not wired to the legacy translator or the five domain services. `ApplicationAPI._register_builtins()` registers only the existing dispatch admission/request/lease operations. The `SubmitDispatch`, `SubmitManyDispatch`, and `SubmitCoordinatorDispatch` classes in `peerhub/application/legacy.py` are translation targets, but no descriptors for `dispatch.submit`, `dispatch.submit_many`, or `dispatch.submit_coordinator` are registered. A repository-wide search also found no consumer that takes `TranslatedCommand` and calls `Client.submit()` (or `ApplicationAPI.submit()`). Therefore the three pre-existing backed legacy actions do not execute through this typed path; their historical working behavior is a separate legacy/hub dispatch path, not evidence of a peerhub `TranslatedCommand` execution path.

Conclusion: a real generic dispatcher exists at `ApplicationAPI.submit()`, but its handler-registration surface is currently unconnected to `LegacyTranslator` and the domain services. The clear, smallest supported path is to add descriptors to this existing registry and invoke it through `Client.submit()`; no new dispatcher framework is needed. Before broad wiring, this round will prove the path with the three consensus commands only, with an end-to-end test that translates and executes `consensus-propose` against the real consensus service. The remaining twelve translated commands remain explicitly follow-up work.

**Update (same day, follow-up round): the remaining 12 actions are now wired too.** `_register_task`/`_register_lesson`/`_register_room`/`_register_duty` were added to `ApplicationAPI` mirroring `_register_consensus` exactly, each service composed into `Runtime`, and all 15 of the 15 legacy-backed actions now execute end-to-end through `Client.submit()` -- not just translate. This took 3 dispatch-and-verify rounds (see "Verification discipline" items 11-13 below) because the first round silently reused an unvalidated `**e.params` splat instead of real per-field decode validation, and the second round's test additions had a `FakeIdSource` collision bug. Both were root-caused by the terminal's independent pytest/pyright runs, not caught by cx's own self-report.

## What's real now

29 commits (`e60c4c4`..`39b46c9`), all independently verified by the terminal (full `pytest -q` + `pyright` after every commit, not just cx's own report — see "Verification discipline" below). Test suite: **1080 passed**, 1 known pre-existing unrelated failure (`test_generate_manifest.py`'s committed hub.py hash snapshot, stale before this session started) + occasional pre-existing flakiness in one real multi-threaded CAS-race test under full-suite load (`test_two_real_callers_race_at_attempt_creation_one_loses_cleanly`, passes cleanly in isolation, unrelated to any change this session).

**A complete 3-layer pass now exists for all 5 real domains** (consensus, task, lesson, room, duty-lease):
1. **Native service code**: `peerhub/governance/{consensus,rooms,tasks,lessons}.py` + `peerhub/dispatch/{duty_lease,terminal_duty}.py`.
2. **Native CLI**: `peerhub consensus|task|lesson|room|duty` — real, runnable commands with `--json` output.
3. **Legacy-name translation**: `LegacyTranslator` backs 18 of ~90 `LEGACY_CATALOG` actions (up from 3 at session start) — every legacy action for every domain built this session: `ask`/`ask-all`/`ask-coordinator` (pre-existing) + `consensus-propose`/`consensus-vote`/`consensus-check`/`consensus-sweep` + `task-checkpoint`/`task-status`/`task-failover`/`approval-request` + `lessons-propose`/`lessons-activate`/`lessons-retire`/`lessons-list` + `new-topic`/`clear-room` + `leader-claim`/`leader-yield`/`terminal-handoff`/`terminal-heartbeat`.

**Update: layers 2 and 3 are now both real execution paths, not just translation.** `ApplicationAPI` now registers `CommandDescriptor`s for all 15 of the 15 legacy-backed actions across all 5 domains (`_register_consensus`/`_register_task`/`_register_lesson`/`_register_room`/`_register_duty`), each composed from a real service instance held by `Runtime`. A legacy action name now goes: `LegacyTranslator.translate()` → typed `Command` → `Client.submit()` → `ApplicationAPI` decode/validate → real service call → persisted `TargetState` (or, for duty-lease actions, a real lease snapshot — duty leases deliberately don't go through the `TargetState`/broker substrate, see `duty_lease.py`'s own design note). One real end-to-end test per domain proves this (`tests/integration/test_stage2_boundary.py`). ~75 of ~90 `LEGACY_CATALOG` actions outside these 5 domains remain untranslated and unwired — that's the next real gap, not this one.

| Gap | Status | Real modules |
|---|---|---|
| **Increment 0** | Complete | `list_targets` on the broker (migration 0025); test-harness survey (no new code needed — `tests/fakes.py` already covers it) |
| **gap-2** consensus | Complete | `peerhub/governance/consensus.py` — propose, cast_vote, final_call_ack, mark_timeout, request_escalation, resolve, abandon |
| **gap-3** rooms/threads | Core + terminal-duty integration | `peerhub/governance/rooms.py` — create_room, create_thread, append_message, clear_room; `peerhub/dispatch/terminal_duty.py` — TerminalDutyService (thin wrapper over gap-4's DutyLeaseCoordinator) |
| **gap-4** duty-lease | Complete | `peerhub/dispatch/duty_lease.py` — create_lease, renew_lease, close_lease, expire_and_recover_lease, validate_lease_fence, AP-20 monopoly guard; migrations 0026/0027 |
| **gap-5** task lifecycle | Complete | `peerhub/governance/tasks.py` — create, claim_start, checkpoint, request_approval, approval_granted/rejected, request_failover, complete, fail, cancel |
| **gap-6** governance/lessons | Complete | `peerhub/governance/lessons.py` — propose, approve, activate, retire, supersede, quarantine, record_delivery_pending/complete |
| **gap-7** diagnostics | Read-path + row formatters | `peerhub/governance/activity.py` — list_active_{consensus_rounds,tasks,lessons}; `peerhub/telemetry/domain_rows.py` — format_consensus_row/format_task_row(_narrow)/format_duty_row |
| **gap-1** compat/cutover | Native CLI complete for 5 domains; 15/~90 legacy actions translate (wire-layer only, no execution dispatcher yet) | `peerhub consensus\|task\|lesson\|room\|duty` CLI; `peerhub/application/legacy.py`'s `LegacyTranslator` for 15 actions |

## What's NOT done yet (real, not hypothetical, gaps)

- **gap-1**: all 15 legacy-backed actions across the 5 covered domains now execute end-to-end (see update above) — **but ~75 of ~90 `LEGACY_CATALOG` actions remain entirely untranslated and unwired** (session/messaging/governance-proposal/health/routing actions outside the 5 covered domains). That's now the biggest remaining piece of real, not-yet-started gap-1 work.
- **gap-3**: the rest of the original command list beyond room/thread/message/clear-room/terminal-duty — `init-session`/`end-session`/`send`/`mark-read`/`new-topic`/`thread-react`/`thread-promote`/`terminal-close`/`append-handoff`/`checkpoint`/`context-fill`.
- **gap-7**: the formatters exist but are NOT wired into `collect_live_snapshot()`/the live `peerhub diag` render loop yet — they're pure, tested, unconnected functions.
- **gap-2**: `SessionLeaseCoordinator`-backed coordinator-lease reuse (ratification item 16) was never actually implemented — consensus rounds don't yet claim/renew a coordinator lease during their lifecycle.
- No real cross-domain integration tests (e.g. a task's `request_approval` creating a real governance approval flow that a consensus round could gate on) — each domain's tests are self-contained.
- 10 more legacy actions from `LEGACY_CATALOG` map to domains with real native services but weren't translated this session: `consensus-sweep`, `arbiter-review`, `lessons-list` (governance domain has read-only listing already via `activity.py`), plus whatever's covered by the ~75 fully-untranslated actions overlapping domains already built.

## Verification discipline (why every commit's message says "independently verified")

**cx's own sandboxed `pytest` was blocked by a Windows temp-directory ACL restriction in nearly every one of the ~13 rounds this ran across** — it could type-check with `pyright` in its own sandbox, but could almost never actually execute the tests it wrote. This was not a rare edge case; it was the norm. The terminal ran the real test suite after every single round from an unrestricted environment, and this caught real bugs cx's own "verification" never could have:

1. `TargetState.state` freezes JSON arrays to tuples — `== []` assertions failed against the real `== ()` (recurred in 2 separate increments before cx's fresh sessions stopped making it).
2. `MutationSubmission` has no `.target_id` — real field is `.receipt.target_id` (wrong attribute name, never checked against the real dataclass).
3. Migration 0025 and 0026 were both missing `PRAGMA user_version` bumps at first — silently left the schema version one behind after "applying" the migration.
4. `duty_lease.py`'s first refactor called `unit.get_duty_lease()` *after* `unit.commit()` on the same unit of work — raised `RuntimeError: SQLite unit of work is already finished`, a real regression the refactor introduced.
5. `SqliteUnitOfWork` was missing `release_duty_lease`/`insert_duty_recovery_receipt` forwarding methods entirely — `close_lease` raised `AttributeError` on first real use, undetectable without actually running it.
6. `domain_rows.py`'s first version had 18 real pyright errors (3 `reportPrivateUsage`, 15 `Any`-typing) — cx's own "targeted pyright: 0 errors" claim was checking a stale/different scope; a real run found them all.
7. The CLI's `_run_consensus` first version pre-checked `paths.database_path.exists()` and rejected every command including `propose` (a write op that should initialize a fresh workspace) -- `create_runtime()` already handles initialization, the manual guard was both redundant and wrong.
8. The CLI's `--json` output crashed with `TypeError: Object of type mappingproxy is not JSON serializable` -- `dict(target.state)` only converts the top level, frozen nested dicts inside it are untouched. Fixed once with a recursive `_json_safe()` helper, reused correctly in every subsequent CLI round.
9. **Recurred twice across the 5 CLI-wiring rounds**: optional argparse flags defaulting to `""` instead of `None`/the real service-method default (`room_id`, `resume_token`, `scope_kind`) -- each one a silent semantic divergence from what the same Python call with its real default produces, not a crash, so each one needed an explicit test assertion to catch, not just "does it run." By the 4th and 5th rounds (room, duty), cx's own dispatch-time signature review avoided the bug entirely -- explicit warnings citing the exact prior recurrence worked.
10. `peerhub duty`'s first version had 2 unused imports (`reportUnusedImport`), silently inflating `cli.py`'s pyright count from the 10-error baseline to 12 -- caught only because the terminal compares the ABSOLUTE error count against a known baseline every round, not just "pyright exit code."
11. The task/lesson/room/duty execution-dispatcher round 1 used a generic `d(e, cls) -> Any: return cls(self._submission(e), **e.params)` shortcut instead of per-field typed decode functions like `_register_consensus` uses -- this splats the raw untrusted wire envelope directly into typed dataclass constructors with zero validation, defeating the entire point of a `CommandDescriptor`'s decode step. It surfaced as 33 real pyright errors (`reportArgumentType`/`reportUnknownArgumentType`) in the duty block specifically (where handlers access `c.room_id` etc. as explicit attributes pyright can check) -- but the identical unvalidated-decode risk was silently present in task/lesson/room too, just invisible to static analysis there because those handlers read fields via `getattr(c, k)` in a dict-comprehension. cx's own environment reported this as "pydantic resolution issues," not the real cause -- the terminal's own `pyright` run (which works fine in this environment) was needed to find and correctly diagnose it. Fixed in round 2 by writing explicit per-field `text()`/`integer()`/`strings()` decode helpers for every action, mirroring the consensus pattern.
12. Round 2's four new end-to-end tests included 2 that silently relied on the local `FakeIdSource.new_id()` returning a namespace-constant string (`f"{namespace}-123"`, never varying per call) -- fine for a test with exactly one governed-mutation submission, but the task and room tests each make 2-3 submissions in sequence (task: create + claim_start + checkpoint; room: create_room + new-topic), so the 2nd/3rd submission's `command_id` collided with the 1st's already-persisted row, raising `sqlite3.IntegrityError: UNIQUE constraint failed: mutation_requests.command_id`. Root-caused by the terminal reading the local fake directly (not by trusting cx's "15 passed" partial report, which happened to not include these 2). Fixed by making the fake keep a per-namespace counter so every call returns a genuinely unique (but still deterministic) id, while still producing valid UUIDv4 specifically for the "outbox-event" namespace.
13. A stray UTF-8 BOM (`\xef\xbb\xbf`) ended up at the very start of `api.py` after cx's round-2 edit -- harmless to CPython's tokenizer (tests/pyright both still passed), but inconsistent with the rest of the codebase; stripped directly by the terminal as a trivial hygiene fix rather than spending a 4th delegation round on a 3-byte removal.

**None of these would have been caught by trusting cx's self-report.** This is the concrete argument for why every commit in this sequence carries independent terminal-side verification, not just a peer's word — consistent with this whole session's standing "verify, don't just trust a green report" discipline.

## Recommended next steps (not a commitment, just the natural continuation)

1. **`domain_rows.py` / `peerhub diag` wiring — investigated further, resolved into a genuine open question, not a to-do.** `peerhub diag`'s `TelemetryPresenter.collect_live_snapshot()` (peerhub/telemetry/presenter.py) already receives a real `workspace_root` (from `--workspace`, defaulting to cwd) -- so a workspace path *does* reach it, that part isn't the gap. Read the function directly: it's built entirely around reading `_sys/ai/orchestration.json`, `_sys/data/temp/ag_statusline_stdin.log`, etc. -- P:'s own flat-file peer-CLI quota/context/session telemetry conventions. This is a categorically different concern from what `domain_rows.py`'s formatters render (`format_consensus_row`/`format_task_row`/`format_duty_row` -- a peerhub workspace's own governed-domain state: active consensus rounds, tasks, duty leases). One shows "how much quota do my AI peer CLIs have left"; the other would show "what's the state of my governance domains in this workspace." They aren't in tension technically -- there's no missing plumbing -- the real open question is a product decision: should `peerhub diag` show BOTH in one unified view, or should governed-domain listing get its own CLI surface (e.g. `peerhub status --domains`)? That's a genuine design choice, not a wiring gap, so it stays undecided rather than picked unilaterally. Flag for the user or a dialectical round when this becomes a priority.
2. Continue `LegacyTranslator` coverage (translation + execution wiring, following the now-proven `_register_*` pattern) for the ~75 remaining `LEGACY_CATALOG` actions outside the 5 domains already covered (session/messaging/governance-proposal/health/routing).
3. gap-3's remaining session/handoff commands (`init-session`/`end-session`/`send`/`mark-read`/`thread-react`/`thread-promote`/`terminal-close`/`append-handoff`/`checkpoint`/`context-fill`).
4. ~~A real cross-domain integration test or two~~ — **done**: `tests/integration/test_cross_domain_approval.py` proves a task's governance approval gated by a real consensus round's resolved outcome, both the GRANTED and REJECTED paths, asserting the task's `TargetState` and its separate `approval:<id>` `TargetState` independently. Investigated first whether gap-3's remaining 8 session/continuity actions (`init-session`/`end-session`/`mark-read`/`thread-react`/`thread-promote`/`terminal-close`/`append-handoff`/`context-fill`) were ready to implement instead — confirmed via direct doc read that they still carry real open design questions never closed by the 52-item ratification, so implementation on those stays blocked pending a design round, per the standing "architecture before implementation" rule.
5. **Extended**: scanned `LEGACY_CATALOG` for more actions mapping to already-implemented, zero-new-design methods. Backed and wired 3 more: `approval-request` -> `TaskService.request_approval`, `consensus-sweep` -> `ConsensusService.mark_timeout`, `lessons-list` -> `activity.list_active_lessons` (read-only, returns a sequence, wrapped as `{"lessons": [...]}` per the `Mapping[str, JsonValue]` result contract). 18 of ~90 `LEGACY_CATALOG` actions now translate AND execute end-to-end (up from 15). Deliberately left `arbiter-review` and `peer-quarantine` alone -- no clean 1:1 existing method for the former, and the latter belongs to a health/admission domain with no built service yet.
6. **gap-3's remaining 6 open design questions, ratified via a real cc/cx dialectical round** (docs/design/HUB-REPLACEMENT-GAP3-SESSION-CONTINUITY-2026-08-24.md, bottom "RATIFIED (2026-08-27)" section): thread-reaction storage model, `context-fill`'s JSON envelope shape, the 6-section startup set with real ported legacy `_LIMITS` values, handoff-projection necessity, `terminal-close` scope, and room-retention policy. Also separately ratified: `peerhub diag --domains` (flag, not a default-view merge or a new command). All 16 of gap-3's original open questions are now closed.
7. **Architecture gap found while starting `terminal-close`, now RATIFIED AND IMPLEMENTED**: the gap-3 doc's earlier reconciliation assumed `SessionLeaseCoordinator` (peerhub/dispatch/session_lease.py) could back `init-session`/`end-session`. Confirmed wrong -- it's execution-attempt-scoped, not a general room-participation session. One cc/cx critique round (docs/design/HUB-REPLACEMENT-GAP3-SESSION-CONTINUITY-2026-08-24.md, bottom "RATIFIED (2026-08-27): room-participation sessions" section) ratified a NEW dedicated `RoomParticipationCoordinator`. **Now built**: `peerhub/dispatch/room_session.py` -- `open_session()` (idempotent: a compatible duplicate returns the existing session unchanged; an expired or fingerprint-drifted one is atomically retired as `EXPIRED`/`ABANDONED` and replaced by a new `session_generation`, linked via `resume_parent_session_id`), `heartbeat()`, `end_session()`, `get_session()`/`get_active_session()`. Migration 0028 (`room_participation_sessions` + `room_session_events` tables, partial-unique-index on ACTIVE rows only so multiple distinct participants can hold concurrent sessions in the same room). Fencing is DB-level optimistic-concurrency (`UPDATE ... WHERE ... AND heartbeat_expires_at = :expected_value`), not a full dispatch-style fence-tuple. Lifecycle transitions (open/resume/retire/end) emit real append-only events in `room_session_events`; heartbeats don't. `room.session_bindings` deliberately left untouched (explicit comment in the coordinator) -- still a rebuildable projection for a future round to wire up. 7 real integration tests (tests/integration/dispatch/test_room_session.py) cover idempotent open, fingerprint-drift replacement, expiry-driven resume, heartbeat fence rejection (wrong generation AND wrong owner separately), end-session fencing, and concurrent-participants-in-one-room. **Clean first-try implementation** -- unlike almost every other round this session, the terminal's independent verification (full `pytest -q`: 1100 passed, only the known pre-existing failure; `pyright`: 0 new errors, repo-wide baseline unchanged at 136 pre-existing unrelated errors) found zero bugs to send back. CLI wiring, legacy translation, and execution-dispatcher registration for `init-session`/`end-session`/`session.heartbeat` remain explicit follow-up work, matching how every other domain was built (service+persistence+tests first, wiring later).

**Update (same day, follow-up round): CLI/legacy/dispatcher wiring done too.** `RoomParticipationCoordinator` composed into `Runtime`; `peerhub session open|heartbeat|close` is a real, runnable CLI command with real per-argument help text; `init-session`/`end-session` are backed in `LegacyTranslator` (`coordination.session.open`/`coordination.session.close` -- the two catalog entries reserved since the original inventory, never backed until now) and execute end-to-end through `ApplicationAPI`; `session.heartbeat` is registered as `coordination.session.heartbeat`, a native-only operation with no legacy name (confirmed `LEGACY_CATALOG` never had one). **Count correction**: a running-tally drift crept in over several rounds (this doc said "18" for a few rounds after `terminal-close` alone had already pushed it to 19) -- a direct recount against the real `LegacyTranslator.translate()` source (`grep -c 'if call.action =='`) gives the accurate total: **24 of the real 90 `LEGACY_CATALOG` entries** now translate AND execute end-to-end (`ask`/`ask-all`/`ask-coordinator` pre-existing + `consensus-propose`/`vote`/`check`/`sweep` + `init-session`/`end-session` + `new-topic`/`clear-room` + `leader-claim`/`yield` + `terminal-heartbeat`/`handoff`/`close` + `task-checkpoint`/`status`/`failover` + `approval-request` + `lessons-propose`/`activate`/`retire`/`list`). Another clean implementation round -- independent verification (pytest -q: 1105 passed, only the known pre-existing failure; pyright: 0 new errors across every touched file, `cli.py` unchanged at its 10-error baseline) found no bugs, and a full manual CLI smoke test (open -> heartbeat -> close against a real workspace, not just unit tests) confirmed the real lifecycle end-to-end, including the fence correctly advancing `heartbeat_expires_at` and terminating in `ENDED`.
8. **`terminal-close` implemented** (the first of gap-3's ratified items): `TerminalDutyService.close_terminal_duty()` (peerhub/dispatch/terminal_duty.py) validates the fence then releases the duty lease -- mirroring `handoff_terminal_duty`'s existing fence-check-then-close pattern, but without creating a successor lease. Wired through `LegacyTranslator` (`terminal-close` -> `TerminalCloseCommand`) and `ApplicationAPI._register_duty` (`coordination.terminal.close`), following the exact established decode/handle/encode discipline. The ratified optional `--close-session` half is deliberately NOT exposed anywhere yet (no CLI flag, no legacy field, no API param) -- safer than accepting the flag and silently ignoring `true`, since session lifecycle itself is still blocked on item 7 above. Real bonus find: the pre-existing `peerhub duty close` CLI command was calling `DutyLeaseCoordinator.close_lease()` directly, bypassing fence validation entirely -- now routed through the new service method, closing a real latent gap. One trivial pyright regression (an import that became unused once the CLI stopped constructing `DutyLeaseCloseRequest` directly) fixed directly by the terminal rather than a round-trip.
9. **`peerhub diag --domains` implemented** (the ratified gap-7 decision): adds a governed-domain state section (consensus/task/lesson, via `peerhub/governance/activity.py`'s listing functions + `peerhub/telemetry/domain_rows.py`'s formatters) to `peerhub diag`, gated entirely behind the new `--domains` flag -- default output is untouched, `--domains --json` adds a `"domains"` key. Governance-collection failures degrade to a `governance state unavailable: <reason>` section rather than crashing the rest of `diag`. Duty-lease listing is explicitly OUT of scope this round -- duty leases are room-scoped with no cross-room enumeration query yet, noted as its own follow-up rather than invented speculatively. Manually exercised end-to-end against a real workspace (propose a real consensus round, confirm default `diag` shows nothing extra, confirm `--domains` and `--domains --json` both show it correctly) -- not just unit-tested. Three real bugs found by the terminal's independent verification, none caught by cx's own report: (a) the `--domains` argparse flag was never actually registered on the `diag` subparser -- all the handling logic existed but the flag itself didn't, so `--domains` failed with "unrecognized arguments"; (b) the failure-degradation test's `monkeypatch.setattr(cli_module, "create_runtime", fail)` had no effect because the domain-collection code re-imported `create_runtime` locally inside the closure (`from peerhub.runtime import RuntimeContext, create_runtime`), shadowing the patched module-level name with a fresh, unpatched one -- removed the redundant local import (both names were already imported at module scope); (c) the new test's own fixture ordering called `main(["diag", ...])` before setting up governance data with an explicit `workspace_home_id="diag-home"`, but `diag` already touches the same workspace DB via the pre-existing `_refresh_usage_projections()` (even without `--domains`) and auto-detects an identity for it -- creating a real `WorkspaceIdentityMismatchError` once the test tried to force a different identity onto an already-initialized DB; fixed by reordering the test to populate governance data first, letting every later call auto-detect the same persisted identity. A 4th, smaller pyright-only issue (`_render_domain_section`'s untyped `Any` parameter cascading into 10 `Unknown`-type errors) was fixed directly by the terminal with a proper `Mapping[str, Any]` signature, matching the size of prior single-function typing fixes rather than warranting another round-trip.

## Verification discipline, continued (cross-domain approval test round)

14. cx's first pass at the cross-domain test referenced a `runtime_setup` pytest fixture that was defined locally inside `test_stage2_boundary.py` — invisible to a sibling test module. Both new tests failed at setup with `fixture 'runtime_setup' not found`. Fixed by extracting `FakeClock`/`FakeIdSource`/`FakeAdmissionProvider`/`runtime_setup` into a new `tests/integration/conftest.py` (cx's own fix, correctly preserving the round-3 counter-based `FakeIdSource`).
15. That extraction broke 2 *other*, pre-existing tests in `test_stage2_boundary.py` that instantiate `FakeClock()`/`FakeIdSource()` directly rather than through the fixture — `NameError: name 'FakeClock' is not defined`, since conftest.py auto-discovery only applies to fixture functions, not arbitrary class names. Fixed directly by the terminal (a one-line `from tests.integration.conftest import FakeClock, FakeIdSource` import) rather than spending a 4th round-trip on a single import line.

## Verification discipline, continued (3-action legacy-catalog batch: approval-request/consensus-sweep/lessons-list)

16. `lessons-list`'s first version had its `encode_result_fn` return a bare `list` instead of a `Mapping` — `ApplicationAPI.submit()`'s generic post-handler code unconditionally calls `encoded_res.get("command_id")` on whatever any descriptor's `encode_result_fn` returns, for every domain, no exceptions. A bare list has no `.get()`, so this raised `AttributeError`, swallowed into a generic `CommandFailure(message='Internal server error')` by the outer try/except — invisible without either reproducing directly (which the terminal did, calling `desc.handle()`/`client.submit()` directly to get the real traceback) or reading the `submit()` source to spot the implicit contract. Fixed by wrapping the list under a named key (`{"lessons": [...]}`) — every `encode_result_fn` in this file must return `Mapping[str, JsonValue]`, never a bare sequence, and that's now the documented convention for the next read-list descriptor someone adds.
17. That same round left 6 new pyright errors (2 redundant `cast()` calls after isinstance-narrowing, 2 `Any`-cascade errors from an unannotated list accumulator, 1 more redundant cast, and 1 real `Mapping[str, JsonValue]`-shape mismatch from the bare-list encoder above) against a 0-error baseline for the touched files. All were mechanical/typing-only (no logic changed) and fixed in one follow-up round with an explicit `list[str]` annotation and an `encode_lessons` function with a real `-> Mapping[str, JsonValue]` return type wrapping the inner list via `cast(JsonValue, lessons)`.

## Tier 1 backlog round (`docs/design/PEERHUB-BACKLOG-2026-08-27.md`)

Following the consolidated backlog doc's own readiness tiers: completed all 3 real Tier 1 items and correctly declined the 4th on finding a real semantic mismatch, rather than forcing it.

- **`terminal-close --close-session`** (completes the seam deliberately left open in the original `terminal-close` commit): `RoomParticipationCoordinator.end_session()` wired in. Duty-close and session-close are reported as two fully independent outcomes (`{"duty_close": {...}, "session_close": {...}}`) -- a session-close failure is caught and reported, never allowed to undo or hide a successful duty-lease release. Verified manually end-to-end against a real workspace, not just unit tests: the happy path (both close cleanly) and the partial-failure path (duty released, session-close fails with a clear `RecordNotFoundError` reason, CLI reports exit code 2) both behave exactly as ratified.
- **`terminal-duty-sweep`**: new `DutyLeaseCoordinator.sweep_expired_leases(role, ...)` plus a new cross-room `list_expired_duty_leases` persistence query (the existing `get_active_duty_lease`/`get_latest_duty_lease` only ever looked at one room+role at a time). Iterates the existing, already-proven `expire_and_recover_lease()` per candidate rather than reimplementing expiry logic. Deliberately does NOT touch room sessions or attempt any task replay, matching the gap-3 doc's "no automatic task replay on expiry" rule exactly as scoped.
- **`thread-append`**: wired to the pre-existing `RoomsService.append_message()` -- pure mechanical wiring, no new code beyond the translator branch and descriptor.
- **`lesson-broadcast`**: investigated and deliberately left unbacked. Real legacy behavior discovers every other room member and sends each one a mailbox message; `LessonService.record_delivery_pending(lesson_id, peer_id, ...)` only marks ONE peer's delivery pending -- a genuine cardinality mismatch the backlog doc's own "confirm before wiring, don't assume" caution was written to catch. A new regression test (`test_legacy_lesson_broadcast_stays_unbacked_without_broadcast_semantics`) documents this is intentional, not a gap. Moved to the backlog's Tier 3 (needs a real design decision on which layer owns "discover room members").

26 of the 90 `LEGACY_CATALOG` actions now translate and execute end-to-end (up from 24). Another clean-verification round: independent terminal checks (pytest -q: 1115 passed, only the known pre-existing manifest-snapshot failure; pyright: 0 new errors, `cli.py` unchanged at its 10-error baseline, repo-wide baseline unchanged at 136 pre-existing unrelated errors) found no bugs beyond what cx itself had already caught and declined (the `lesson-broadcast` mismatch).

## Tier 2 round: `thread-react`

First real Tier 2 item -- ratified design, needs new component code, not just wiring (`docs/design/PEERHUB-BACKLOG-2026-08-27.md`'s Tier 2 table; full ratified reasoning in `docs/design/HUB-REPLACEMENT-GAP3-SESSION-CONTINUITY-2026-08-24.md`).

`RoomsService.react()`/`unreact()` (peerhub/governance/rooms.py) write two things per call: an immutable `reaction-event:<event_id>` `TargetState` (never updated after creation, `action: "ADD"|"REMOVE"`) and a CAS-updated `reaction-state:<message_id>:<actor_key>:<reaction_type>` projection `TargetState` (current status, rebuildable from the event history, matching the exact operational-state-vs-projection split already ratified for `handoff.md`/`message_projection`). Reachable through `peerhub room react|unreact`, `LegacyTranslator` (`thread-react` now carries an `action: "ADD"|"REMOVE"` field, defaulting to `"ADD"` for backward compat), and `ApplicationAPI`.

**Real bug caught by the terminal's manual end-to-end smoke test, not by any unit test**: the first implementation round wired the ADD path everywhere but left `unreact()` completely unreachable from any external interface -- no `peerhub room unreact` CLI subcommand existed at all, `ThreadReactCommand` had no field to express REMOVE, and `ApplicationAPI`'s `coordination.thread.react` handler was hardcoded to call `s.react(...)`. This was invisible to cx's own unit-test-focused verification (the service-layer tests for `unreact()` passed fine in isolation) and only surfaced because the terminal manually drove the CLI through a real react → unreact → react round-trip against an actual workspace. Fixed in a follow-up round: `action` field threaded through the wire command, translator, and API decode/dispatch; a real `peerhub room unreact` CLI subcommand added. Independently re-verified twice -- once via cx's own direct CLI invocation, once via the terminal's own separate manual round-trip -- both showing the same `ACTIVE → REMOVED → ACTIVE` sequence through the real CLI.

One trivial regression from the fix: a pre-existing test (`test_thread_react_translates_all_reaction_fields`, written the round before) asserted an exact `encode_params()` dict without the new `action` key -- fixed directly by the terminal (added `"action": "ADD"` to the expected dict) rather than a round-trip, since it's a 1-line test-literal update, not a design or logic question.

27 of the 90 `LEGACY_CATALOG` actions now translate and execute end-to-end (up from 26). Full suite: pytest -q 1122 passed (only the known pre-existing failure), pyright 0 new errors (repo-wide baseline unchanged at 136).

## Tier 2 round: `room.session_bindings` wiring

Second Tier 2 item, not a `LEGACY_CATALOG` action so doesn't move the translated-action count. Design decision made by the terminal (the ratification deliberately left the exact mechanism open "for a future round to wire up" -- this was that round): implemented as an ON-DEMAND rebuild, not a synchronous push from `RoomParticipationCoordinator.open_session()`/`heartbeat()`/`end_session()` -- keeps the two coordinators independent, matching the session-lifecycle ratification's own insistence that they stay decoupled.

`governance.activity.rebuild_room_session_bindings(broker, room_id, active_sessions)` (peerhub/governance/activity.py) reads the current room `TargetState`, replaces ONLY the `session_bindings` field via a CAS-governed submit, preserves every other field. `RoomParticipationCoordinator.list_active_sessions(room_id)` (new) supplies the session list without either coordinator depending on the other. Exposed as `peerhub room rebuild-session-bindings --room-id ROOM_ID`.

Manually verified end-to-end against a real workspace (not just unit tests, on both cx's side and the terminal's own separate run): opened 2 sessions from different participants in the same room, rebuilt, both appeared as bindings; closed 1, rebuilt again, only the survivor remained; every other room field (title, participants, thread_ids, retention, status) stayed untouched by the rebuild. Clean round -- pytest -q 1123 passed (only the known pre-existing failure), pyright 0 new errors (`cli.py` unchanged at its 10-error baseline, repo-wide baseline unchanged at 136).

Tier 2 is now 2 of 4 closed. `context-fill` and `checkpoint`/`append-handoff` (the handoff-projection generator) remain -- the biggest, most interdependent pieces left in the backlog's Tier 2.
