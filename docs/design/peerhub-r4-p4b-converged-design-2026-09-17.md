# R4/P4b architecture convergence — RATIFIED (one-time substitute-voice exception)

**Status: RATIFIED, 2026-09-19 — cc + ag.deepthink (proposal author) + ag.effort (substitute third vote).** This is a **one-time named exception** to standing project policy DIR-006, which normally requires unanimous agreement from three distinct vendor peers (cc/Claude, ag/Gemini, cx/Codex-OpenAI). cx was real-vendor-rate-limited until 2026-09-19T17:16:00 (`_sys/codex/health.json`, confirmed real usage cap). `ag.opus` was tried first as the substitute third vote and hit two consecutive real vendor RESOURCE_EXHAUSTED (429) errors on its own quota pool (confirmed via `_sys/antigravity/health.json`); `ag.effort` was used instead, with the user's explicit, deliberate authorization to substitute an `ag` tier for `cx` on this one decision only -- DIR-006's general 3-distinct-vendor requirement is unchanged going forward for other decisions. `ag.effort` read all source documents and source files fresh and independently (not assuming ag.deepthink's or cc's conclusions), rendered **RATIFY WITH AMENDMENT**, and every citation in its verdict was independently spot-checked against the real files by cc before this status was recorded. The full verdict, including the 2 amendments applied below (consensus migration scope, `authorizer_behavior` semantics), is preserved in `docs/reviews/` history; ag.opus's earlier voluntary pre-review (non-binding, since ag.deepthink already had a voice) is at `docs/reviews/r4-p4b-ag-opus-prereview-2026-09-19.md`.

**Implementation may now begin**, per this ratification.

**Implementation progress (2026-09-19):** Step 1, the template domain (`operational-error`), is done. `peerhub/application/governance_authorizer.py` (new) elevates `verify_credential_for_actor` into a `GovernanceAuthorizer` injected once into `ApplicationAPI` (`peerhub/application/api.py`), replacing the old tautological `caller.client_id == cmd.submission.client_id` stub with the same asserted/verified duality described in "Converged mechanism" below; wired in production via `peerhub/runtime.py`, reusing the existing `_verify_dctx_credential` closure. `CommandEnvelope` gained an optional `credential_id` field (`peerhub/core/protocol.py`) for future verified-path domains -- unused by this round, since `operational-error` stays `ASSERTED_ONLY`. The CLI's `error report` entrance (`peerhub/cli/__init__.py:_run_error`) now builds a `CommandEnvelope` via `Client.submit(ReportErrorCommand(...))` instead of calling `OperationalErrorService.report_error()` directly. `docs/design/peerhub-production-call-map-R1.json`'s `error report` entry updated: `gateway` is now `APPLICATION_API_GATEWAY`, new `authorizer_behavior: "ASSERTED_ONLY"` field added. New integration test `tests/integration/application/test_error_report_cli.py` proves both the happy path and that the gateway's authorization check actually runs (a real client/submission `client_id` mismatch is rejected with exit code 2). Full suite: 1701 passed (was 1699), 0 regressions. Remaining domains (`feedback`, `artifact`, `file-lock`, `task`, `room`, `lesson`, `role`, `leadership`, `duty`, `recovery`, then `consensus` last) not yet migrated.

Inputs: ag.deepthink's initial proposal (2026-09-17, produced to ag's own sandbox, not separately committed), `docs/design/peerhub-r4-p4b-independent-review-cc-2026-09-17.md` (cc's critique), and ag's response to that critique (2026-09-17, agreeing with all 3 corrections). This document is the synthesis of all three.

## Converged mechanism

**"The common verifier and exact grants"** = elevate the already-shipped, already-tested `verify_credential_for_actor()`/`resolve_actor_for_credential()` (`peerhub/persistence/dispatch_context.py`) from a consensus-specific dependency into a `GovernanceAuthorizer` injected once into `ApplicationAPI`, replacing the current stub check at `ApplicationAPI.submit()` (`peerhub/application/api.py` ~line 531-532, which today only does a near-tautological `caller.client_id == cmd.submission.client_id` — the code's own comment calls this "a skeleton").

**Enforcement shape, generalized from the already-shipped consensus pattern (`ConsensusService.cast_vote`'s `credential_id: str | None`), not invented new:**
- If the envelope carries a `credential_id`: it must verify against `envelope.actor_id` via the injected authorizer, or the gateway rejects with an authorization error. (Extends today's per-domain check to every domain, uniformly, at one place.)
- If no `credential_id` is present: the call remains a valid **asserted** actor claim, UNLESS the specific command/round has opted into `verified_required` (a per-domain, per-risk-tier policy decision, not a gateway-wide default) — matching the already-shipped `consensus propose --verified-required` shape exactly.
- **Exact grant** means: when verification does apply, the verified identity must equal the claimed `actor_id` exactly — no partial match, no downgrade. It does NOT mean every mutating command universally requires a credential starting with R4. That decision is explicitly deferred by the original design (`peerhub-dctx-proposal-1-2026-09-13.md` Increment 3: "Decide separately, with migration telemetry, whether asserted mutations remain supported, require `--allow-asserted-actor`, or are removed in the next major version").

**Why this resolves the "human CLI usage" question without a new concept:** a human at a terminal has no `dispatch_requests` admission row and therefore no credential to present — their calls are asserted, which stays valid for normal-risk work by design. No local-developer-credential mechanism is needed.

## Migration plan

1. **Template domain: `operational-error`** (minimal dependencies). Rewrite its CLI entrance to build a `CommandEnvelope` and call `Client.submit()` instead of instantiating `OperationalErrorService` directly. Update `docs/design/peerhub-production-call-map-R1.json`'s entry: `gateway` becomes `APPLICATION_API_GATEWAY` (was `DIRECT_CLI_BYPASS`); `authorizer_behavior` (new field, see below) reflects its actual enforcement, which for a first migration is expected to start at `ASSERTED_ONLY` (routing changes; enforcement behavior does not, until separately decided per-domain). A real integration test reaching `peerhub.cli:main` proves the migrated path still works.
2. **Subsequent domains, in order:** `feedback`, `artifact`, `file-lock`, `task`, `room`, `lesson`, `role`, `leadership`, `duty`, `recovery` — each following the exact same template: route through `ApplicationAPI`/`Client`, update its call-map entry, prove via a real integration test. No domain regresses relative to its current behavior at migration time (asserted calls that worked before keep working).
3. **Last: `consensus` domain (including `vote`, `proposal-vote`, `proposal-add`, `propose`, `arbiter-review`, and service-level `final_call_ack`).** These already have real, shipped, tested domain-level verification (Increment 1). Once the gateway-level authorizer exists and is proven on the 10 domains above, `ConsensusService`'s internal `CredentialVerifier` calls in both `cast_vote` and `final_call_ack` can be retired in favor of the gateway doing it uniformly. Every mutating consensus leaf command in the call map (including `consensus arbiter-review`, `gateway: DIRECT_CLI_BYPASS` today) must be migrated to `APPLICATION_API_GATEWAY`. This domain is done last specifically because it's the one place a mistake could regress an already-shipped security control. *(Amendment added 2026-09-19 by ag.effort's ratification vote: the original text named only `cast_vote` implicitly via "consensus/proposal-vote" and omitted `final_call_ack` and `arbiter-review`, both of which use the same `credential_verifier` dependency and must not be missed during migration.)*

**Explicitly out of scope for the migration step itself** (separately gated, per domain, evidenced by migration telemetry — not simultaneous with routing changes):
- Removing duplicate identity defaults (e.g. `proposal-vote --voter` defaulting to `"cc"` — tested and shipped in v0.4.0, `tests/integration/application/test_proposals.py::test_cli_proposal_vote_defaults_to_cc_when_voter_and_credential_both_omitted`).
- Deciding whether asserted mutations remain supported long-term, require an explicit opt-out flag, or are removed in a future major version (Increment 3's own deferred decision).

## Production call-map schema addition

Add a new typed top-level field per command, alongside the existing free-text `"authorizer"` (which continues to describe *how*, e.g. "GovernanceAuthorizer via verify_credential_for_actor"):

```json
"authorizer_behavior": "ASSERTED_ONLY" | "VERIFIED_WHEN_PRESENTED" | "VERIFIED_REQUIRED" | "NOT_APPLICABLE"
```

- `NOT_APPLICABLE`: read/observation commands.
- `ASSERTED_ONLY`: mutating command routed through the gateway, but no domain-specific `verified_required` policy exists yet (the expected state for most commands immediately after migration).
- `VERIFIED_WHEN_PRESENTED`: a credential is checked if given, but not required (matches consensus `vote` today without `--verified-required`).
- `VERIFIED_REQUIRED`: the domain/round has opted into mandatory verification (matches `consensus propose --verified-required` today).

This makes R4's exit evidence ("identical protection on all entrances") mechanically checkable per-command instead of inferred from routing alone — `gateway` and `authorizer_behavior` are independent facts, since a migrated command can legitimately still be `ASSERTED_ONLY`.

*(Amendment added 2026-09-19 by ag.effort's ratification vote)* **Schema semantics note:** `authorizer_behavior` records the command's *static entrance capability declaration* in the call map, not a per-invocation dynamic state. For example, `consensus vote` is declared `VERIFIED_WHEN_PRESENTED` because its entrance accepts both uncredentialed claims and credentials, even though a specific round with `verified_required=True` will dynamically reject uncredentialed calls at runtime — the static declaration and the runtime outcome for one call are different facts. For `CONDITIONAL_MUTATION`/`CONDITIONAL_MUTATION_EXTERNAL` commands (e.g. `status` with a write-refresh branch), `authorizer_behavior` documents the policy governing the mutation branch specifically, not the read-only branch.

## Deferred to implementation time, not blocking design closure

- Exact CLI output-formatting approach through `Client.submit()`'s generic `CommandOutcome` vs. today's direct, domain-shaped return values (ag's uncertainty #2) — a per-domain decision made when that domain is actually migrated.

## Third-voice questions — answered by ag.effort's ratification vote (2026-09-19)

These 4 questions were originally addressed to cx. Since cx remained rate-limited (and ag.opus, tried first as a substitute, hit its own real vendor quota exhaustion), `ag.effort` answered them fresh and independently as the substitute third voice; all citations below were spot-checked against the real files by cc before this ratification was recorded.

1. **Asserted/verified duality — agreed, no universal verification at R4.** Requiring verification universally would violate Increment 3's explicit "decide separately, with migration telemetry" deferral and would break all human-driven local CLI usage (no `dispatch_requests` row to mint a credential from).
2. **Default-removal as a separate, later-gated step — agreed.** `tests/integration/application/test_proposals.py::test_cli_proposal_vote_defaults_to_cc_when_voter_and_credential_both_omitted` (shipped, v0.4.0) locks this in as intentional; removing it during routing migration would be a real breaking change.
3. **`authorizer_behavior` enum/migration order — no objection**, plus a refinement now folded into this document above: the field is a *static per-command capability declaration*, not a per-invocation dynamic state.
4. **Citation corrections found** — the 2 gaps ag.opus's earlier pre-review found (`final_call_ack` and `consensus arbiter-review` not explicitly named in the migration step) are real and are now fixed above. No other citation errors found in the converged design or cc's review; a stale line-number citation in the original D-CTX proposal (predating the `CredentialVerifier` addition) was noted but is not a substantive error.

If cx later reviews this design (once its own rate limit clears), any objection should be raised as a normal follow-up correction, not treated as blocking further work that already proceeded under this ratification.
