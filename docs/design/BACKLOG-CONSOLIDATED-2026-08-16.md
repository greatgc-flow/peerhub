# Peerhub Consolidated Backlog (2026-08-16)

Corrected/extended by the terminal after independent verification against
the actual source (2026-08-16) -- see "Terminal corrections" at the
bottom for what changed from the first pass and why.

## AWAITING-USER-DECISION
- **Health/quota tracking (T2) -- `TelemetryWorker` process-host decision.** 6 of 7 increments done (all polling functions, write-path wiring, `peerhub status` CLI surface). The last piece (periodic background polling) can't proceed because peerhub has no persistent daemon process today. 3 options documented in `HEALTH-QUOTA-TRACKING-DESIGN-2026-08-16.md`'s end section, recommendation: poll-on-demand from `peerhub status` itself (no new process model needed) rather than a new `peerhub daemon` subcommand. **Update 2026-08-19 (found by a direct `diag`/`--live` audit, deeper than previously known): even the recommended poll-on-demand path was never actually implemented.** `grep -rn "record_usage_observations\|poll_claude_usage\|poll_codex_usage\|poll_agy_usage" peerhub/ --include="*.py"` outside `telemetry/quota_polling.py` itself returns **zero hits** -- these fully-built, tested, fail-closed-correct functions have no caller anywhere in the live package. Concretely:
  - `peerhub diag --fresh`: the `--fresh` flag is defined in `cli.py`'s argparse but is **never referenced** in `_run_diag()` or `TelemetryPresenter.collect_live_snapshot()` -- it is a complete no-op today.
  - `TelemetryPresenter` is constructed in `_run_diag()` with no `usage_projections` argument (`presenter = TelemetryPresenter(use_color=..., workspace_root=...)`), so `self._usage_projections` is always `None` and CC/CX's quota pools are always rendered as honestly-absent (`--`), never real data -- not because data is stale, but because nothing ever calls the poll functions to produce it. `peerhub status --peer/--all`'s quota table has the correct read-side wiring (`uow.list_usage_projections(peer)`) but the same problem: nothing ever calls the write side, so the table is permanently empty in any real deployment.
  - `--live` mode's 2s refresh loop re-renders the same static snapshot; AG's data happens to refresh each iteration because it re-reads a raw file (`ag_statusline_stdin.log`, kept fresh by `agy`'s own statusline hook calling `peerhub statusline` during real usage -- a real, working mechanism, just architecturally inconsistent with how CC/CX would need to work). CC/CX would need actual re-polling, but there is nothing to re-poll at any interval since the write side was never wired at all.
  - **Fix requires a small design decision, not just a patch**: wire `--fresh`/`status`/`diag` to call `poll_claude_usage()`/`poll_codex_usage()` and `record_usage_observations()` before reading back projections (closes the on-demand path as originally recommended); for `--live` specifically, pick a re-poll cadence that does NOT hit real provider APIs every 2s (measured earlier this session: `claude.cmd /usage` alone takes ~9.4s real latency) -- e.g. re-poll on a longer interval (60s+) independent of the render-refresh interval.
  - **Also needed, not previously called out**: a full parameter/flag test suite for the CLI with EMPIRICAL (measured) coverage, not just mocked unit tests -- the existing test suite (1012 passing as of commit `259512b`) did not catch either the dead `--fresh` flag or the orphaned polling pipeline, because unit tests mock the underlying pollers and never exercise whether `_run_diag()`/`_run_status()` actually invoke them end-to-end. A `--live`/`--fresh`/`--json` end-to-end test matrix against real (or realistically faked, but wired-through) data sources is the concrete gap.
  - **Update 2026-08-19, later same day (`a87507c`): reproduction half done, fix not yet landed.** `tests/unit/cli/test_quota_wiring_e2e.py` (ag.opus) is committed and empirically proves the gap: `TestFreshFlagIsDeadBug`/`TestQuotaPipelineHasNoCallersBug` pass today (confirming the bugs exist), `TestQuotaWiringFixed` (5 tests) currently FAIL by design -- this is an intentional TDD red-state commit, **`pytest -q` on the full suite will show 5 failures in this one file until the fix lands; this is expected, not a regression.** The actual fix (wiring `_run_diag()`/`status --peer/--all` to call the poll functions, plus the `--live` cadence decision) was attempted next but every available delegation channel became unavailable mid-session before it could land: ag.opus hit genuine 3P-pool exhaustion again minutes after finishing the test file; cx.deepthink/cx.effort both hit a real ~24h rate-limit (confirmed via `diag --fresh` showing X-pool genuinely at 99.99x, not a false positive); cc.deepthink/cc.effort could not be reached at all -- `hub.py ask --to cc.*` fails 100% on this portable root due to a real path-quoting bug (`&` and a space in `D:\Engram&Peerhub\PortableDev (v2.1)`, see session memory `reference_hub_cc_dispatch_path_quoting_bug.md`), and `peerhub ask cc --capability-tier WORKTREE_WRITE`/`GIT_MUTATE` both fail with `CapabilityLeaseViolation` since this fresh workspace clone has no measured capability-lease enforcement evidence yet (fail-closed by design, see `reference_peerhub_ask_no_mutation_evidence_fresh_workspace.md`). **Next session: make `TestQuotaWiringFixed`'s 5 tests pass** (the test file itself documents exactly what's expected); re-check whether the two CC-delegation blockers above are still live before attempting `cc.*` again.
  [Size: small-to-medium implementation once the cadence decision is made; the decision itself is small]
- **Windows-native Brokered Read-Only Reducers -- BLOCKED, needs a policy call.** Precondition 1 (spawn-privilege availability) empirically failed hard: the current non-elevated account has none of the 3 required Windows privileges at all (`SeIncreaseQuotaPrivilege`/`SeAssignPrimaryTokenPrivilege`/`SeImpersonatePrivilege`). Options: (a) accept running peerhub with elevated privileges for this mechanism (real security/operational posture change), or (b) commission a redesign search for a different Windows-native primitive not requiring these privileges (e.g. job objects). Not peer-ratifiable -- see `WINDOWS-BROKERED-REDUCERS-DESIGN-2026-08-16.md`'s "2026-08-17 Precondition Investigation Results" section for full evidence. [Size: architecture]
- Inter-agent message monitoring (MAPLE-Guard pattern) - Requires explicit user decision on whether/when to schedule. [Source: HUB-REPLACEMENT-ROADMAP Cross-cutting, 2026-08-15 entry] [Size: architecture]
- Adaptive/structural loop termination vs. string-based stop signals - Requires explicit user decision on whether/when to schedule. [Source: HUB-REPLACEMENT-ROADMAP Cross-cutting, 2026-08-15 entry] [Size: architecture]
- **Engram/peerhub full separation -- ratified plan exists, execution not started.** User's explicit direction (2026-08-19): fully separate Engram (portable dev environment, `github.com/greatgc-flow/Engram`) from peerhub (peer/profile communication, `github.com/greatgc-flow/peerhub`); "unnecessary" governance features (`collab_rate`, R:6-R:10 consensus, leader/terminal-duty rotation, mailbox, lessons/proposals, broker/credits) may be dropped, not ported. Two independently-derived plans converge on the same end state and were reconciled 2026-08-19:
  - `P:\workspace\engram_peerhub_separation_proposal.md` (cx.deepthink, ~270 lines, 12-phase safety-gated execution sequence) -- **adopted as the authoritative execution plan** for the Engram-side work given materially higher rigor (correctly identifies that `protocol.json` must be deleted whole and last, not piecemeal, to avoid missing-key crashes elsewhere; that the scheduled-task/process caller check is TEST NEEDED not proven; that `.ai` governance state needs quiesce+checksum+archive before removal).
  - `_sys/antigravity/config/brain/2c8ac180.../engram_streamlining_and_cleanup_plan.md` (AG, prior session, references peerhub v0.1.1) -- simpler 2-phase framing, kept as corroborating precedent.
  - `P:\workspace\engram_peerhub_separation_RATIFIED_scope.md` -- the reconciliation note; also documents a real sequencing risk both plans missed: the terminal was still delegating implementation work via legacy `hub.py ask` at the time of this decision, so deleting `hub.py` before migrating the terminal's own delegation path to `peerhub ask`/`peerhub broadcast` would cut off the terminal's ability to delegate further work mid-execution.
  - **2026-08-19 scoped-down execution**: only the Peerhub-side prerequisite work (the proposal's section 3, rows 3.1-3.10) was executed this session -- commits `974754e` and `259512b` (Engram-path decoupling in presenter.py/quota_polling.py/cli.py/statusline.py, version bump to 0.1.8). **The Engram-side deletion/doc-rewrite cluster (sections 4/5/7/8/9/10 of the proposal, ~150+ files across `_sys/core`, `_sys/cli`, tests, `docs-v2`, root docs) was explicitly NOT attempted** and remains fully unstarted -- needs a dedicated future session following the proposal's 12-phase sequence (freeze plan -> make peerhub independent [done] -> update Engram's pin -> migrate direct callers while hub.py remains available -> deprecate aliases -> quiesce and prove no legacy writers -> archive historical evidence -> atomic cutover -> remove active legacy state -> fresh-install validation -> residual scan -> release).
  - Section 3's remaining rows not done in the 2026-08-19 session: 3.1 (profile/adapter config descriptors for all supported profiles, not just `*.standard`), 3.2 (`--prompt-file`/stdin support on `peerhub.cli`), 3.3 (empirical AG PTY execution/cancellation verification). Row 3.9 (no-hardcoded-Engram-path test) is partially done -- `TestNoHardcodedPaths` exists in `tests/unit/telemetry/test_presenter.py` (drive-letter grep from `974754e`, room-ID grep added in `259512b`) but is not yet a package-wide exhaustive check. Two open items still flagged by the proposal's own section 12 for round-2 critique before the Engram-side work begins: (a) whether rows 3.1-3.3 are sufficient for every current Engram caller once they land; (b) whether any environment-only rule is trapped inside `operational_guard_matrix.py`/`snapshot.py`/old docs and must be extracted before those files are deleted.
  - `P:\_sys\claude\config\CLAUDE.md` (the user's own global assistant config, outside both repos) still describes the collab_rate-driven "Multi-Peer Collaboration Protocol" and was explicitly marked out-of-scope for this work by both the terminal and cx's proposal (row 9.4) -- needs a coherent replacement concept ("use peerhub for deliberate peer communication; no collab-rate/R-level/Final Call/leader gate") written separately by the user/terminal, not as part of either repo's cutover.
  [Size: architecture, multi-session]

## SCHEDULED-DEFERRED
- Alembic runtime cutover (Increment 2) - **Already ratified 2026-08-11 as HOLD with named triggers** (not an open user decision -- 3-way dialectical ratification: do not implement Option 1 or Option 2 until a trigger fires; if one does, Option 1 -- freeze bespoke v19 as the schema floor -- is the ratified default). [Source: HUB-REPLACEMENT-ROADMAP Phase 2, "Increment 2 (runtime cutover) — RATIFIED 2026-08-11: HOLD, WITH NAMED TRIGGERS"] [Size: architecture]
- Durable response transcripts - Deferred with named trigger. [Source: HUB-REPLACEMENT-ROADMAP Phase 3 Broadcast] [Size: architecture]
- Parallel fan-out - Deferred with named trigger (blocked on measuring SQLite write contention). [Source: HUB-REPLACEMENT-ROADMAP Phase 3 Broadcast] [Size: architecture]
- Capability-lease enforcement-evidence prerequisites - Zero code, trigger-gated on machine-owned launcher evidence (4 named prerequisites). [Source: HUB-REPLACEMENT-ROADMAP Cross-cutting & CAPABILITY-LEASE-DESIGN ERRATA Section 8] [Size: architecture]

## UNSCHEDULED-READY
- ~~CLI Ctrl-C cancellation-ladder wiring (T4)~~ - **FIXED AND COMMITTED 2026-08-16 (`98586ff`).** `peerhub ask` runs the dispatch on a background thread; on interrupt the CLI now calls the live `ProcessSupervisor.begin_cancellation()` (SOFT_CANCEL -> TERMINATE_TREE -> KILL_TREE) via a `cancellation_hook` threaded through `execute_direct_ask()`/`dispatch_and_execute()`, with a bounded poll for the narrow race where the interrupt lands before the supervisor exists. Two issues caught only by independent re-verification, not the implementer's own report: a false "pyright clean" claim (4 real errors, fixed by removing the `Optional` type structure that caused them rather than silencing); and a pre-existing CLI test that encoded the old "not implemented" behavior and broke on the intentional message change (only the new test file had been run, not the full suite) -- updated to assert the new behavior. Full suite independently re-run: 961 passed, pyright clean.
- ~~2 broken real-agy adapter integration tests~~ - **FIXED AND COMMITTED 2026-08-16 (`a81dc9e`).** Confirmed two genuinely unrelated bugs, not one: `test_real_agy_adapter_shells_out` had a stale decoder-event-count assertion (fixed to check both `SESSION_IDENTITY`/`ASSISTANT_TEXT` events precisely); `test_real_agy_adapter_via_pipe` had the stale keyword-arg `TypeError`, which once fixed surfaced two further latent bugs in shared test routing helpers (a profile mismatch and a peer_kind mismatch), both fixed via optional overrides that don't change any other test's default behavior. Full suite independently re-run by the terminal: 960 passed; the only slow-test failure (`test_peerhub_facts.py::test_real_peer_versions_match_the_shipped_contracts`) is an unrelated, genuine ag CLI version drift (1.1.12->1.1.13) correctly caught by the drift-detection tool itself, not caused by this fix.
- ~~Tool-call parsing (peers that invoke their own tools mid-response)~~ - **Stale entry, actually DONE (`9ff7813`, T1 increment 4).** `DecoderEventKind.TOOL_CALL` exists (verified against source: `peerhub/adapters/contract.py`, wired in `peerhub/adapters/codex_adapter.py`), normalizing Codex's `command_execution` items. Codex-only: Claude/Agy don't expose tool calls in their current `--output-format json` mode (only via unused `stream-json`). [Source: HUB-REPLACEMENT-ROADMAP-2026-08-09.md line 581]
- ~~Health/quota tracking equivalent to diag.py (T2)~~ - **DESIGN RATIFIED 2026-08-16**, moved to Implementation-Ready-Pending-Canary below.
- Crash-linkage recovery (resuming an interrupted round after a coordinator crash) - Deferred as increment 4. [Source: HUB-REPLACEMENT-ROADMAP Phase 3 Broadcast] [Size: architecture]
- Phase 4 shadow-by-ownership-cluster validation - Not started. [Source: HUB-REPLACEMENT-ROADMAP Phase 4] [Size: architecture]
- Phase 4 same-revision comparison + rollback proof - Not started. [Source: HUB-REPLACEMENT-ROADMAP Phase 4] [Size: architecture]
- ~~Phase 4 scoping investigation (T7a)~~ - **SCOPING MEMO DONE 2026-08-17 (`ce38e13`)**: `docs/design/PHASE4-SCOPING-MEMO-2026-08-17.md` maps hub.py's real dispatch entry point (`_action_ask_inner`, `P:\_sys\core\hub.py:6154`), the existing `scope_key`/`session_id` as a ready-made ownership-cluster identifier, what same-revision comparison plumbing is missing, and why a fire-and-forget shadow tap before the blocking subprocess call is inherently safe to disable. Every citation independently re-verified against real source line-by-line before trusting it. This is investigation only, NOT the ratified design pass itself -- Phase 4's real design pass remains unstarted and still gated behind ConsensusRound/Primitive B (see BLOCKED below). [Source: HUB-REPLACEMENT-ROADMAP Phase 4] [Size: architecture]

## BLOCKED
- ConsensusRound / Primitive B - Blocked gate; must be built before first R:10 decision is routed to peerhub, and blocked on durable response transcripts. [Source: HUB-REPLACEMENT-ROADMAP Phase 4] [Size: architecture]

## Items Not in Peerhub's Own Roadmap
- ~~Implement 3-Tier Context Partitioning (`EvidenceArtifact`)~~ - **DESIGN RATIFIED 2026-08-16**, moved to Implementation-Ready-Pending-Canary below.
- ~~Deploy Windows-native Brokered Read-Only Reducers~~ - **DESIGN RATIFIED 2026-08-16**, moved to Implementation-Ready-Pending-Canary below.
- **CLI Ctrl-C cancellation-ladder wiring** - genuinely absent from the roadmap doc's own text (confirmed by direct grep for "ctrl-c"/"cancellation-ladder" -- zero matches); the underlying TODO exists only in `peerhub/cli.py`. Now added above under UNSCHEDULED-READY.

## Implementation-Ready (design ratified, 2026-08-16 sequential detailing pass)
- **Health/quota tracking (T2)** - `HEALTH-QUOTA-TRACKING-DESIGN-2026-08-16.md`. **Canary PASSED 2026-08-17** (`acb6635`). **Increment 1 DONE (`e41d22b`)**: usage/readiness projection persistence layer (migration `0024`). **Increment 2 DONE (`947e899`)**: `poll_claude_usage()`. **Increment 3 DONE (`ef5a409`)**: `poll_codex_usage()` -- caught and root-caused a real process leak (broken PPID chain defeating `taskkill /T`) via the implementer's own leak-detection test, fixed with a `psutil`-based failsafe, independently re-run 3x clean by the terminal. 977 passed, pyright clean. **Increment 4 DONE (`6d50b58`)**: `poll_agy_usage()`. **Increment 5 DONE (`7cc8fe1`)**: `record_usage_observations()` wires all 3 polling functions' output into the increment-1 persistence layer (observation always appended; projection upserted with bounded-CAS-retry only for MEASURED results, never clobbered by absent/error/stale). 985 passed, pyright clean. **Increment 6 DONE (`bd878c7`)**: `peerhub status --peer/--all` quota table, `list_usage_projections()`. 987 passed, pyright clean. **6 of 7 increments done. Last piece (`TelemetryWorker` background scheduling) hit a real architectural gap (`3793441`): peerhub has no persistent daemon process for a periodic worker to live in at all. 3 options documented (poll-on-demand recommended) -- moved to AWAITING-USER-DECISION below, not implemented.**
- ~~EvidenceArtifact / 3-Tier Context Partitioning~~ - **Claude + Codex DONE, all 2026-08-16**: increment 1 (`baa6a04`, data layer), increment 2 (`d744ce9`, Claude adapter), increment 3 (`89e6d2a`, Codex adapter, including the RESUME branch). Independently re-verified each time (969 passed, pyright clean as of increment 3). agy adapter wiring was never scoped in the ratified design doc at all (only Claude + Codex) and would need its own small design check first if wanted.
- ~~Windows-native Brokered Read-Only Reducers~~ - **BLOCKED 2026-08-17 (`4b78f1c`)**, moved to AWAITING-USER-DECISION below -- precondition 1 (spawn-privilege availability) empirically failed hard (the account has none of the 3 required privileges at all), meaning the ratified mechanism can't be implemented without either accepting elevated privileges or a redesign search for a different Windows primitive. Not a peer-ratifiable choice.

## Terminal corrections (2026-08-16, after independent verification)
The first pass (ag.deepthink) miscategorized the Alembic cutover as
AWAITING-USER-DECISION; it is actually already ratified (moved to
SCHEDULED-DEFERRED above). It also flagged "T2/T4/T7a" as a single
vague item sourced from the dispatch prompt rather than the repo. Of
the three: T2 is just the already-listed "Health/quota tracking" item
(merged, not a separate gap); T4 (Ctrl-C wiring) is a real, fully-specified,
previously-untracked gap (added above with its real source citation);
T7a has no concrete artifact anywhere and is treated as simply
not-yet-started under Phase 4, not a distinct tracked item. A direct
`TODO`/`FIXME` grep across `peerhub/` and `tests/` found exactly one
hit (`peerhub/cli.py`, the T4 item) -- no other untracked source-code
TODOs exist.
